.DS_Store
._*
Thumbs.db
node_modules/
.netlify/
archive/
design-handoff/

# Throwaway harnesses. Twice now one of these has ridden along in a commit
# because `git add -A` is indiscriminate — the name is reserved so it can't again.
_tmp_*

# the Browser-pane dev launcher, which points at an absolute local path
.claude/launch.json

# local-only dev env (never a real secret, but never committed either)
.env

# netlify dev drops this
deno.lock
metrics/

# AI-handoff bundles: built for pasting into another assistant, never committed
tmp/ai-handoff/
