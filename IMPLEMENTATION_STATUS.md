# Implementation status

Repo-owned ledger for execution state, evidence, blockers and decisions. **Chat history
is not durable project memory.**

This file answers *"what is happening right now?"*. It does not repeat what the other
documents own: `MYSET-MASTER-OVERVIEW.md` is what MySet is, `INVARIANTS.md` is what must
never break, `docs/decisions/` is why a design is the way it is, and `docs/sessions/` is
what happened on a given day.

**Last reviewed:** 2026-09-26 (Stripe's own fee on every night — EVS-005, a fourth amendment to decision `0095`: `moneyForShow` reads each payment's balance transaction (the session list asked with `data.payment_intent.latest_charge.balance_transaction` expanded, the `stripe_fee` lines, other currencies at the transaction's own rate) and files `money.fees {usd, charges, missing}` with the night; the index row carries `stripeFees` (exact or null, in `rowSig`); the register's rows carry it; the shows table uses it and marks ≈ only where it is not on file yet; older nights are asked once by the register, two a ring, the fee alone written and only when the takings match, never spending the morning-after ask; **live as `5a25263`** (PR #104): the backfill began at the 08:27 UTC fold and on 08:52 held 6 of 12 money-known nights exact — 20 Sep's $35 night reads Stripe's own $2.67 where the estimate said $2.28 (17% low: more foreign cards than the model's 13%); then what each show cost — EVS-004, a third amendment to decision `0095`: three cost columns after Merch on the shows table (Server · Stripe · Total, one group), the server from Netlify's per-day meters for that night or their average, at the top-up pack's price, Stripe an estimate at the model's published card rates, a Costs tile, a Costs block in the night's drawer; `_showcosts.mjs`, served beside the rows in shows.json, the register untouched; **live as `315f525`** (PR #102, deploy ready 06:41 UTC; production's shows.json prices all 16 counted nights, ≈ $0.48 server and ≈ $5.93 Stripe); then a Studio tab opens at its top — UX-058: the Gigs tab kept the old tab's scroll offset, clamped to the bottom of its short loading page, and stayed there when its data arrived; `setTab` in both Studios now scrolls to the top on a tab change; **live as `beed75b`** (PR #98, merged 2026-09-26 06:21 UTC; production serves `studio.js?v=df44350a` and `venue-studio.js?v=4e87826b` with the new `setTab`, checked 06:22); then the money model in the /mediadash look — EVS-003, a second amendment to decision `0095`: `finance/model.html` on the same tokens and system type as the shows page, bold ink column headings, total and sum rows in a light orange, a fifteen-row window on every table, both axes named on every chart, the notes in a dark band at the foot; the shows page gains the same headings and totals, a dot-grid hero with a lit divider over the main table, the breakdowns two to a page stacked full width, and its rules in the dark band; **live as `16137f3`** (PR #99, deploy ready 06:18 UTC; production's /moneymodel carries `frameAll`, the dark band and the titled axes, no Google Fonts; /moneymodel/shows carries `PER = 2` and the divider); then EVS-002 **live as `5e978ac`** (PR #97): the shows dashboard in the /mediadash look — EVS-002, an amendment to decision `0095`: black glass and the system's SF type, the main table's column groups (Votes: all / bought / $ · Tips: count / $ · Merch: items / $), columns shaded in alternate bands with each group one band, a totals row pinned under the headers, every dollar figure above zero green, no table window taller than fifteen rows, both axes named on every chart (the by-country chart's category axis read “$0” — fixed), the nine breakdown tables in a two-up carousel turned by arrows, the night in a side drawer; `finance/shows.html` only, suite green; on branch `ux/shows-stark`; then EVS-001 **live as `a8cd3c9`** (PR #96, merged 2026-09-25): the first ten-minute ring built the register on production ~9 minutes after the deploy, and at 2026-09-26 02:30 UTC it held 36 rows — 16 counted / 2 unused / 13 not counted / 5 silent, last night's Seaflower show among them — from a cron fold of 544 ms over 15 reads. Before that, 2026-09-25: every show on the platform — EVS-001, decision `0095`, INVARIANT 0gi — the register (`_register.mjs`, month shards + head, one writer under a lock, folded by `registercron.mjs` every ten minutes from `regdirty` marks), the dashboard at `/moneymodel/shows` behind the model's passcode, the model's live feed (`/moneymodel/live.json`, meters kept from the seed), one night rule in `_nightrule.mjs` shared by the stats page, the Sheet and — by test on two production snapshots — the tracker; the archive now files country, zone, plan, who started/ended, request and RSVP counts; suite 55 files, 0 failed; then the founder's three levers from 0088, all three **live as `ebdce9e`** (PR #94, merged 2026-09-25, verified by content on production) — PERF-003 the service worker, PERF-004 app.css inline in every fan page (decision `0094`, INVARIANT 0gh), PERF-005 the community page's shared read and personal call (decision `0093`, INVARIANT 0gg); `node tools/overview.mjs --tests` 3,571/0, uicheck 242 ✓ / 2 ✗ (the same two stale profit figures as origin/main), sheetcheck 39/0, the app's browser on the mock: no page asks for `/app.css`, the community page fires both reads and paints the feed from the shared one; a page seen tonight is shown from the phone's copy — PERF-003, decision `0091`, INVARIANT 0ax rewritten: `sw.js` shows a page fetched in the last six hours from its copy and re-fetches it behind, older pages wait for the network, a stamped file is never re-asked; **live as `ebdce9e`** (PR #94) — `test/sw.mjs` 33/0 with every rule mutation-checked, the suite 3,535/0, the app's browser on the mock: second open 0 bytes transferred, page whole; a fresh-context review's four real findings (the money model stored without its gate, a reload landing on the copy, an unguarded cache lookup, a vacuous assertion) fixed in the tree; before that, the same day: later: MKT-001, the Instagram dashboard at /mediadash, decision `0092`, **live as `4f95289`** (PR #92) — see its row; before that: loaders that never freeze and lyrics as verses — UX-057, decision `0090`, INVARIANT 0gf — **live as `ee39bb1`** (PR #90, merged 2026-09-25 06:22 UTC; verified by content on production ~06:24: `/leave.js` serves `msLeaveFill1`, `/perryidyll` the `<i><b></b></i>` bars, `/perryidyll/vote` `introfill1` + `lyr-st`, `/studio.html` `bbfill`, `studio.js?v=85eaccb7` `verses()`); then the first-open speed pass — PERF-002, decision `0088`, INVARIANT 0gd — and the fan pages share one script — PERF-001, decision `0087`, INVARIANT 0gc — both **live as `68efdb4`** (PR #85, merged 2026-09-25 03:51 UTC; live on production ~30 s later): verified by content (`/fan.js` `immutable`, the vote page's tokens and logo screen, one `<img>` per slot, no blocking head script, both front-door reads through the door) and measured AFTER: the artist page fetches 0 originals (was 4, ~400 KB); `what=artists` / `mapconfig` / `places` answer from the durable cache at 0.42–0.46 s (were 1.15–1.98 s cold, never cached). Before that: `public/fan.js` carries the sheet, the toast, the share sheet, the date words, the strips and the rsvp memory for all nine fan pages, 716 page lines gone; **in the working tree on branch `perf/fan-script` (a worktree from origin/main `a2f1e27`), not committed, not pushed** — the suite 3,478/0, sheetcheck and uicheck green in real Chrome; before it, 2026-09-20: tips shared on their own — In-app tips, My cut of tips, Total / My cut on the profit block, Tonight = tips + paid votes — UX-056, decision `0086` — **live as `84f0b6b`** (PR #80) **and `2f6a095`** (PR #82: Total profit is the act's whole night, before the splits; votes bought count as tips); the founder's book back-filled from his calendar, 64 shows, rounded and timed; before it, 2026-09-18: Artist Diaries phase 1 — DIA-001, decision `0085`, INVARIANT 0gb, `test/diaries.mjs` — **live as `a87ff85`**, PR #78; then: hours and minutes as two number-pad boxes — UX-055, decision `0084`; swipe-to-reveal and hold on every list row — UX-054, decision `0083`; the founder's Money-tab list — UX-053, decision `0082`: every payment celebrated, the tab folds, the zoom-out found and fixed, *Delete show* hides a night; on its branch, merge SHA in the push log once it lands; before it, 2026-09-15 night — the first gig is the onboarding and a night's money is everything tagged to it, MON-001 / UX-051, decision `0081`, INVARIANT 0ga; before it: the SSD mirror rebuilt and the diverged checkout found; the Stripe key is restricted, PER-007; the tip sheet's custom field, UX-050 `98e2320`; payouts follow the plan — PAY-001, decision `0080`; the Stripe platform profile acknowledged by the founder, PER-014 — the gate that had failed every *Start with Stripe*; before it: the artist page's doors — UX-047/048/049, decisions `0074`/`0075`, INVARIANT 0fw — **live as `a69154f`**, PR #51; before it: email + password, ACC-001, decision `0070` — built on `auth/password-door` off `44f3a21`; merge SHA in the push log once it lands. Before it: the shop's second look, UX-045, live as `ab0a05e` (PR #39); the data foundations, DAT-001, decisions `0066`–`0069`, live as `51d7584` / `b6edb37` / `8444c6e`)
**Current phase:** Phase 3 — scale preparation, on a product that is already live
**Current focus:** **UX-053 — the founder's Money-tab list (decision `0082`)**: the tip burst fires for votes bought too and stays brief over a lyrics or chord chart; *$x from in-app tips* under the profit (hero and editor, `tipped` on the index row, back-filled by the heal); a currency chooser (a label, never a conversion); the gig form's pay open by default, runs included; the hours boxes on the normal keyboard; the editor's ✕ clear of the readout and a save that lands at the top of the tab; the pinch-out zoom found on the live site (a by-show chart with 21 labels laid out at 441px) and fixed; *Past shows*; every long list folds 5 → +20; *If something broke* last; *Delete show* = record removed + calendar night hidden (`eventHide`) + filed night hidden, never destroyed (`POST /api/history {action:'hide'}`). `tools/mock.mjs` now serves a book, thirty nights, a ledger and payments so the tab can be looked at. Before it: **MON-001 / UX-051 — the first gig batch (decision `0081`)**: the founder's friend's line (get them to a first successful gig; the aha is money in the bank) built as a trimmed first run ending on a printed sign, a pinned *Your first gig* card, example rows on an empty first board, a loud first tip, tonight's money always in view, one morning-after letter — and the Money tab's routing bug closed (a tip after the show is still that night's money; the tile and profit are one figure). Before it: **PAY-001 — payouts follow the plan (decision `0080`)**, and the two things 2026-09-15 found: the first live *Start with Stripe* was refused by Stripe until the founder acknowledged the platform profile (PER-014, done), and the artist saw Stripe's raw message with a dashboard URL (now one MySet sentence, the reason in the error log). Before it: **UX-047 / UX-048 / UX-049 — the artist page's doors (decisions `0074`, `0075`)**: the founder asked (2026-09-14, off the back of the artist-page playbook) for *Votes cast* on the numbers' line with *Songs* gone and `10k`/`10.1k`/`1m`; a **Book** button and a **Merch** button under the numbers — and with Book "a messaging system within the app" (Requests → General on reply, Business / Casual / Spam, read/unread, Report and Block, a notification and a bubble on the Studio menu); **View calendar** under *See more* with dots and a night's floating card; and in the Studio's Gigs tab a **tour dates poster** (PDF/PNG/JPEG) with a booking link, giving the page **View tour dates** with *Download* and *Grab your tickets*. Built the same day in a worktree off `origin/main` — nine readers, a refuted contract, the server core and its suite by hand, two lanes for the Studio and the page, six refuting reviewers, two fixers, the gate — see the three rows and the session note `2026-09-14-book-merch-messages-calendar-tour.md`. **Live as `a69154f` (PR #51)** on the founder's "ship it", verified by content 50 s after the merge. Before it: **ACC-001 — email + password, the standard door (decision `0070`)**: the founder asked (2026-09-14) for "the fully standard shape where the email address is the username", with the screen he specified — a pink-orange-ringed box, *Welcome back*, Email over Password, a filled pink-orange *Sign in*, *Forgot your password?*; *New here? Join the MySet family* and a ringed *Create account*; the Studio code as small grey underlined text at the foot opening a window — and, once in by a code, the option in Settings to create a password that replaces it. Built in both Studios: `_cred.mjs` (scrypt, salted, per email row, `cred_<owner>_<hash>`), `passwordSignIn` / `passwordSet` / `passwordClear` on `/api/auth` and `/api/venueauth`, `pw` on `list`, a per-address lockout, the Settings row and sheet (current password or a fresh six-digit code to change; a change signs the address's other devices out), a one-time offer after a code sign-in. The six-digit code stays the way an account is made and the whole of "forgot"; the Studio code stays as it was, in the window. ACCOUNTS.md §11, INVARIANT 0fu. Suite exit 0 / **3,047 ✓ / 0 ✗** (new `test/password.mjs` 50 ✓); uicheck 239 ✓; sheetcheck 39 ✓; headless shots of the screen in both themes, the window, join, forgot, the venue screen. Before it: **DAT-001 — the data foundations (decisions `0066`–`0069`)**: the founder asked (2026-09-13) that "literally ALL data is tracked for every artist and every show — forever", and for the foundations an Instagram-sized MySet would need now. Built the same night, all six from the storage report plus the two losses its audit found: (1) every vote, play and dollar of a night filed in an append-only event log `evt_<aid>_<showId>` as it happens — the vote row now carries its cast time, the three take-off paths (`dropSongVotes` / `refundSongVotes` / `wipeBoard`) hand back what they remove and `admin.mjs` files it with the play (`vote` / `refund` / `drop` / `reset`), the archive files the standing votes, the money and the end by replacing the head's state; the cast pays nothing extra (`test/cost.mjs` 5 reads / 2 writes); `/api/history?log=<showId>` reads it for the owner (0066, INVARIANT 0fq); (2) a version before every overwrite of `profile_`, `lists_`, `ev_` and the library (`casKeep` → `ver_<key>_<ts>` write-once + `vers_<key>`; one per 30 s; the library only when `libChanged`) (0067); (3) the capped feed and feedback list spill into `postsarch_` / `fbarch_` instead of dropping their oldest — the audit found 200+ posts lost their photos and clips for ever; `keysFor` / `keysForVenue` enumerate the archives and their images (0068, 0fr); (4) `mirrorcron` every 20 min → one pass a day copying what changed (by etag) to R2 under `backup/<key>`, never shards / sessions / secrets, time-boxed with a cursor (0069, 0ft); (5) `tools/backup.py --restore DIR --store NAME` (refuses `myset`) + `--wipe`, **rehearsed against the real store**: 209 of 209 keys restored and read back equal, 287 MB, 580 s, then wiped; (6) INVARIANT 0fs: an id once minted is never reused. `_append.mjs` is the one append-only chunked log under all of it (head + write-once parts, computable keys, race-safe spill). Suite exit 0 / **2,932 ✓ / 0 ✗** (46 files, new `test/foundations.mjs` 60 ✓); session `2026-09-14-data-foundations.md`. Before it: **UX-044 (decision `0064`) is live** (`406f3f4`, PR #33, 2026-09-13) — merch on its own page. `/<slug>/shop` and `/v/<slug>/shop` (`public/shop.html`, one file), entered from a ringed finder-sized card under the name and **above** the tip button on the community page (drawn only when there is something to sell; the rail is retired); a static two-column grid with two drifting strips (the room's starred posts on the page, *More from {First}* in the product sheet; a Picks strip after the grid behind `?picks=1` for the founder to compare); sizes with per-size sold-out, a sold-out flag that keeps the price, postage as a flat Stripe shipping rate with MySet's fee on the price alone, the item photo on Stripe's page; `/api/confirm` returns the order with a 4–5 character pickup code; `pay.mjs` takes `from:'shop'`; the Studio's Merch store is its own Menu row with the code first on every order, the Venue Studio mirrors it. The founder inspected it on `node tools/mock.mjs` (new: a mock API and static server for every page and state) and asked for five changes, made the same session: **Make a request** (`_wishes.mjs`, `action:'wish'` on `/api/community`, *Requests from the shop* with Done/Undo in both Studios, INVARIANT 0fp — no way back to the fan), the *Community ↗* crumb at the top of the shop, earlier orders at the foot without a date, the promise line *shipped to your door — options vary per item*, and *What fans are saying*. Shipped on his "ship it"; verified live by content (`/perryidyll/shop` carries `data-ask>Make a request` and `class="pgrid`, `/perryidyll/community` carries `.shopcard`, `/studio.js` carries *Requests from the shop*, the community read carries `limits.wish`). Gate on the final bytes, rebased onto `15beca1`: suite exit 0, 45 suites, 2,867 ✓ / 0 ✗; uicheck 228 ✓; sheetcheck 39 ✓; overview current. Renumbered on the rebase: UX-040 → UX-044 (taken by #28), INVARIANTS 0fn → 0fo and 0fo → 0fp (0fn taken by 0065). Before it: **UX-043 / PL-003 (decision `0065`) is live** (`81a48f3`, PR #31, 2026-09-13) — the business dashboard for the paid plans: for every show the artist logs the total pay from the venue, splits (5 / 10 a show by plan) and their own cut, cash tips, merch, costs (5 / 10), four kinds of time and gear; the Money tab shows profit in green with MySet's fee named in dollars, the revenue mix, hours and *Stage time rate* / *Full evening rate* (total or my cut, before or after the fee) for any period; a paper-white report at `/report` for a date range or picked shows; the filed night learns its gig (`key`) and whether its money is known (`source`); the unconnected-owner ledger bug closed for artists and venues (INVARIANT 0fn). Suite exit 0 / **2,577 ✓ / 0 ✗** (45 files) on the rebased tree; uicheck 171 ✓; sheetcheck 12 ✓; the report's headless check 35 ✓; the live `/biz.js` and `/studio-money.js` compared equal to the tree; session `2026-09-13-business-dashboard.md`. Renumbered from 0063 / UX-040 after #28 and #30 took those. Before it: **UX-042 (decision `0063`, #30)** — the current song has to be ended before another can start. Before it: **UX-039 (decision `0062`)** — band names everywhere and the hero video with a drifting *Watch more* strip, PR #27. Before it: **UX-038 live** (PR #25 `466a907`, 2026-09-13): the artist page's link pills drift left to right under *Listen, follow, & support*. Before it: **Batch seven (UX-037, PL-002; decision `0061`)** — the plan cards' second pass: the whole library is live to the room on every plan (free `featured` unlimited, the cap machinery kept), Bar Star's library 200, Bandcamp and GoFundMe links in the Profile form and on the artist page's *Listen & follow* strip (`_profile.mjs` allows `*.bandcamp.com`, `gofundme.com`, `gf.me`), the Rock Star *(coming soon)* line rewritten as the business dashboard, the installed Studio reloads 60 ms after a theme toggle so iOS repaints the band under the clock — suite exit 0 / 0 ✗ / 2,303 ✓, uicheck 113 ✓, sheetcheck 12 ✓, headless shots of the plan sheet, Profile tab and link strip; session `2026-09-13-batch-seven.md`. Before it: **Batch six (UX-036, PL-001; decision `0060`)** — the plan cards in the founder's words with four rules moved in the code (free room 50, free library 100, hiding a post and the filed nights on Bar Star, deleting for good gone), `--accent-ink` the logo pink-orange so every accent word follows, every *Directions* pill pink-orange on the soft gradient ground, *Tonight* ringed as one box on the front door, "View on MAP", the pack sheet says "<First> will receive through Stripe Connect", the tip sheets "Sent via Stripe Connect" under "Give <First> some love", the install card "Runs faster, is free, takes ~6 seconds", the proof carousel resumes 1 s after a hand scroll, theme.js replaces the theme-color meta so iOS repaints the band under the clock, *Comped until* off the founder's account — suite exit 0 / 0 ✗, uicheck 113 ✓, sheetcheck 12 ✓, headless shots; session `2026-09-13-batch-six.md`. Before it: **The pink-orange pass (UX-035, decision `0059`)** is live as `740d852` (PR #22) — every thin ring on the site paints the brand gradient (`--grad`, pink-red left to orange right, the Search button's fill) by a masked `::after` or, on a bordered box, by its border box; `--accent-2` is `#FF5650`, the logo's pink-orange, so the audience's words, the section headings, the edge glow and the Studio's headings follow; the artist and community pages' menu is two pink-orange bars with pink-orange icons in the pop — built in a clean worktree off `origin/main` (`0000bc8`) in Efficient Mode, suite exit 0 / 0 ✗ / 2,290 ✓, uicheck 113 ✓ (now asserting the gradient), sheetcheck 12 ✓, headless shots of four pages; session `2026-09-13-pink-orange-pass.md`. Before it: **Product Dev 4 batch (UX-031–UX-034)** is live as `0000bc8` (PR #21) — the show row is one centred card on the front door, the artist page and now the venue page (RSVP on the venue's *What's on* too — `venue.mjs` rows carry `eventId` + `rsvp`), the thin *Directions* pill from the map sheet under the time and date in place of the pin; the map sheet drags closed from its head with a grab bar; orange rings on both chips under the finder, a hairline on the feed's search field, "Search by name, venue, date, time…"; both Studios' tab bar in a second pass (1px top line, content nudged down in a 56px bar, 13px labels, tighter, the Live icon unclipped); the Gigs tab's *Coming up* rows laid out like the Setlist tab's song cards — built in a clean worktree off `origin/main` (`7859b75`) in Efficient Mode, suite exit 0 / 0 ✗ with `test/rsvp.mjs` 48 ✓, uicheck 113 ✓, sheetcheck 12 ✓, harnesses at 390/320px both themes; session `2026-09-12-product-dev4-batch.md`. Before it: **Product Dev 3 batch (UX-027–UX-030)** is live as `50509c5` (PR #18) — RSVP moved to the right edge of every show row on the front door and the artist page (the Directions pin under the date, "N going" centred under the pill; Tickets a line under the day), a filed night renamable by a tap on the Money tab (decision `0057`, `POST /api/history {action:'rename'}`, `titleByHand`, INVARIANT 0fm), *All songs* a real Use choice that turns the Live checklist step green, the vote page's edge glow made byte-identical to the Studio's (`test/structure.mjs` pins it), the artist page's photos enlargeable in a lightbox, the Community button orange, the hero line / finder ring / ink headings / folded-map icon on the front door, "+ Add a song" centred — built the same way as batch two (clean worktree off `origin/main`, four implementers × three refuting reviewers × a fix pass), suite exit 0 / 0 ✗ / 2,277 ✓ across 44 files with the new `test/histname.mjs`, uicheck 113 ✓, sheetcheck 12 ✓, harnesses at 390 and 320px in both themes; session `2026-09-12-product-dev3-batch.md`. Before it: **Product Dev 2 batch (UX-022–UX-026)** is live as `cd8cbc7` (PR #17) — the Studio tab-bar facelift, "What the room said" moved under merch on Profile, "Select setlist" with All songs as a real choice, the plans renamed Hobbyist / Bar Star / Rock Star (decision `0055`, ids unchanged; Stripe product names are PER-011), the artist page's proof strip as a slow marquee without the rating card, the pinned card's orange ring / orange heading / red countdown ring, anonymous **RSVP** with a public count on every show card (decision `0056`, `/api/rsvp`, `rsvp_<owner>`), addresses off every event card, two spacing fixes — built in a clean worktree off `origin/main`, five implementers each reviewed by three refuting reviewers, suite `44 passed, 0 failed` with the new `test/rsvp.mjs`, uicheck 113 ✓, sheetcheck 12 ✓, headless-Chrome harnesses for the Studio (42 ✓), artist page and front door at 390px in both themes; session `2026-09-12-product-dev2-batch.md`. Before it: **Airbnb batch two (UX-015–UX-021)** is live — merged as `d980ec4` (PR #11, 2026-09-12 night), verified on myset.vip by content 40 s after the merge — and was green (`44 passed, 0 failed`; uicheck 113 ✓; sheetcheck 12 ✓) — four button tiers + one chip recipe in `app.css` and `lock.css`, `docs/design-system.md`, the artist pill and proof carousel, the vote dock and review sheet, the community composer sheet, the venue listing, bottom tabs / Today / first run / Money chart in both Studios, the front door's next-week button — decision `0051`, session `2026-09-12-airbnb-batch-two.md`. It awaits the founder's go to open the PR (decision `0045`). Before it: two Phase 3 items landed on `main` the same evening. The **shared-board Before it: **UX-039 (decision `0062`)** — band names everywhere and the hero video with a drifting *Watch more* strip, PR #27. Before it: **UX-038 live** (PR #25 `466a907`, 2026-09-13): the artist page's link pills drift left to right under *Listen, follow, & support*. Before it: **Batch seven (UX-037, PL-002; decision `0061`)** — the plan cards' second pass: the whole library is live to the room on every plan (free `featured` unlimited, the cap machinery kept), Bar Star's library 200, Bandcamp and GoFundMe links in the Profile form and on the artist page's *Listen & follow* strip (`_profile.mjs` allows `*.bandcamp.com`, `gofundme.com`, `gf.me`), the Rock Star *(coming soon)* line rewritten as the business dashboard, the installed Studio reloads 60 ms after a theme toggle so iOS repaints the band under the clock — suite exit 0 / 0 ✗ / 2,303 ✓, uicheck 113 ✓, sheetcheck 12 ✓, headless shots of the plan sheet, Profile tab and link strip; session `2026-09-13-batch-seven.md`. Before it: **Batch six (UX-036, PL-001; decision `0060`)** — the plan cards in the founder's words with four rules moved in the code (free room 50, free library 100, hiding a post and the filed nights on Bar Star, deleting for good gone), `--accent-ink` the logo pink-orange so every accent word follows, every *Directions* pill pink-orange on the soft gradient ground, *Tonight* ringed as one box on the front door, "View on MAP", the pack sheet says "<First> will receive through Stripe Connect", the tip sheets "Sent via Stripe Connect" under "Give <First> some love", the install card "Runs faster, is free, takes ~6 seconds", the proof carousel resumes 1 s after a hand scroll, theme.js replaces the theme-color meta so iOS repaints the band under the clock, *Comped until* off the founder's account — suite exit 0 / 0 ✗, uicheck 113 ✓, sheetcheck 12 ✓, headless shots; session `2026-09-13-batch-six.md`. Before it: **The pink-orange pass (UX-035, decision `0059`)** is live as `740d852` (PR #22) — every thin ring on the site paints the brand gradient (`--grad`, pink-red left to orange right, the Search button's fill) by a masked `::after` or, on a bordered box, by its border box; `--accent-2` is `#FF5650`, the logo's pink-orange, so the audience's words, the section headings, the edge glow and the Studio's headings follow; the artist and community pages' menu is two pink-orange bars with pink-orange icons in the pop — built in a clean worktree off `origin/main` (`0000bc8`) in Efficient Mode, suite exit 0 / 0 ✗ / 2,290 ✓, uicheck 113 ✓ (now asserting the gradient), sheetcheck 12 ✓, headless shots of four pages; session `2026-09-13-pink-orange-pass.md`. Before it: **Product Dev 4 batch (UX-031–UX-034)** is live as `0000bc8` (PR #21) — the show row is one centred card on the front door, the artist page and now the venue page (RSVP on the venue's *What's on* too — `venue.mjs` rows carry `eventId` + `rsvp`), the thin *Directions* pill from the map sheet under the time and date in place of the pin; the map sheet drags closed from its head with a grab bar; orange rings on both chips under the finder, a hairline on the feed's search field, "Search by name, venue, date, time…"; both Studios' tab bar in a second pass (1px top line, content nudged down in a 56px bar, 13px labels, tighter, the Live icon unclipped); the Gigs tab's *Coming up* rows laid out like the Setlist tab's song cards — built in a clean worktree off `origin/main` (`7859b75`) in Efficient Mode, suite exit 0 / 0 ✗ with `test/rsvp.mjs` 48 ✓, uicheck 113 ✓, sheetcheck 12 ✓, harnesses at 390/320px both themes; session `2026-09-12-product-dev4-batch.md`. Before it: **Product Dev 3 batch (UX-027–UX-030)** is live as `50509c5` (PR #18) — RSVP moved to the right edge of every show row on the front door and the artist page (the Directions pin under the date, "N going" centred under the pill; Tickets a line under the day), a filed night renamable by a tap on the Money tab (decision `0057`, `POST /api/history {action:'rename'}`, `titleByHand`, INVARIANT 0fm), *All songs* a real Use choice that turns the Live checklist step green, the vote page's edge glow made byte-identical to the Studio's (`test/structure.mjs` pins it), the artist page's photos enlargeable in a lightbox, the Community button orange, the hero line / finder ring / ink headings / folded-map icon on the front door, "+ Add a song" centred — built the same way as batch two (clean worktree off `origin/main`, four implementers × three refuting reviewers × a fix pass), suite exit 0 / 0 ✗ / 2,277 ✓ across 44 files with the new `test/histname.mjs`, uicheck 113 ✓, sheetcheck 12 ✓, harnesses at 390 and 320px in both themes; session `2026-09-12-product-dev3-batch.md`. Before it: **Product Dev 2 batch (UX-022–UX-026)** is live as `cd8cbc7` (PR #17) — the Studio tab-bar facelift, "What the room said" moved under merch on Profile, "Select setlist" with All songs as a real choice, the plans renamed Hobbyist / Bar Star / Rock Star (decision `0055`, ids unchanged; Stripe product names are PER-011), the artist page's proof strip as a slow marquee without the rating card, the pinned card's orange ring / orange heading / red countdown ring, anonymous **RSVP** with a public count on every show card (decision `0056`, `/api/rsvp`, `rsvp_<owner>`), addresses off every event card, two spacing fixes — built in a clean worktree off `origin/main`, five implementers each reviewed by three refuting reviewers, suite `44 passed, 0 failed` with the new `test/rsvp.mjs`, uicheck 113 ✓, sheetcheck 12 ✓, headless-Chrome harnesses for the Studio (42 ✓), artist page and front door at 390px in both themes; session `2026-09-12-product-dev2-batch.md`. Before it: **Airbnb batch two (UX-015–UX-021)** is live — merged as `d980ec4` (PR #11, 2026-09-12 night), verified on myset.vip by content 40 s after the merge — and was green (`44 passed, 0 failed`; uicheck 113 ✓; sheetcheck 12 ✓) — four button tiers + one chip recipe in `app.css` and `lock.css`, `docs/design-system.md`, the artist pill and proof carousel, the vote dock and review sheet, the community composer sheet, the venue listing, bottom tabs / Today / first run / Money chart in both Studios, the front door's next-week button — decision `0051`, session `2026-09-12-airbnb-batch-two.md`. It awaits the founder's go to open the PR (decision `0045`). Before it: two Phase 3 items landed on `main` the same evening. The **shared-board
split (P3-001)** is `c3d0a4d`: the audience poll is two calls, `/api/board` (shared, no fan
id, held at the edge for the polling interval) and `/api/me` (personal, one shard, never
cached); `/api/show` stays for open tabs. **Clip bytes on Cloudflare R2 (P3-003)** is the
commit after it: `/api/vid` answers a 302 to a presigned link on the private bucket; every
clip from before stays in Blobs and serves as before; every R2 failure falls back so the
room can still vote (decision `0033`, INVARIANT 0fd). Both were reviewed fresh-context and
their findings fixed. The open line (P3-002) was **measured** the same evening in its own session: a throwaway Durable Object on `workers.dev` held 4,000 sockets, hibernated between votes and woke in ~10 ms (decision `0035`). No production code yet.

**Next work item (pick up here):** Artist Diaries phase 1 (DIA-001, decision `0085`) is live as `a87ff85` (PR #78): the founder writes the first real page on his own account (Studio → Menu → Diary, with a cover) and says how the editor and the crop felt on the phone. Open from 0085: timed lyrics wait for audio; no restore-a-version for a page (the versions are kept); the diary is not in `tools/uicheck.mjs` yet. Then, as before: Read the first real artist's payout schedule in Stripe → Connected accounts → the account → *Payouts* (weekly/Monday for a Hobbyist, daily for a paid plan) — the only part of 0080 the fake cannot prove; then the founder's statement descriptor (`IDYLL MASTERY` on card statements — his call, `MYSET.VIP` suggested); PER-013 stays on 2026-10-24. Then: The founder's own first booking request on his page, and his answer from the Studio on his phone — the first real walk of the whole path. Open from 0074: the booker's receipt letter (kept, once per address per day — drop it on the first bounce complaint); members getting the letter; the two new numbers (20 letters a day, 3 a day per address); a *load older* for spilled conversations; the R2 copy of a deleted account's conversations (0069's open item, now with a stranger's email in it). Open from 0075: the poster card's place on the Gigs tab (under *Add a gig*); a PDF in the window on a real iPhone. Before it: The founder's own password: sign in by email once (run sheet 5 / 12 — his account has no email row yet), then Settings → *Password · Create*; tell an agent how the screen felt on the phone. Open from 0070: Sign in with Apple/Google when there is an iOS app; a breach-list check if a weak-password complaint ever arrives. After the foundations land: watch the first `mirrorcron` ring in the Netlify function log (it should say `done:true` with a `copied` count on its first pass and `done:true` alone after), and look at one real night's `/api/history?log=` after the founder's next gig. Open from 0069: a hard account delete does not yet reach the R2 copy (`backup/<key>` stays) — one `r2Delete` loop over `keysFor` in `deleteArtist`, with a record. Open from 0067: no restore-a-version button in the Studio yet — the data is there (`listVersions`, `verKey`). The other session's `docs/processes/DATA-MODEL.md` (uncommitted) should gain the new families: `evt_`, `ver_` / `vers_`, `postsarch_`, `fbarch_`, `mirror_`, `mirror`. Stray keys in the production store from pre-tenancy experiments (`f0`–`f11` unsuffixed, `show`, `meta`, `state`, `cas/t`, `debug/probe`, 31 `v/1~crowd…`, 50 `vote~1~…`, `hist_index`) — harmless, cost nothing, a person could delete them. Then, as before: The shop is live (`406f3f4`, PR #33) with no merch listed yet on the founder's page — the first thing is his: add an item in Studio → Menu → *Merch store*, look at `/perryidyll/shop` and the card on the community page on his phone, then: watch the first real posted-with-postage sale in Stripe (the *Postage* line, the address, the picture, the application fee on the line alone), and read the Puzzle canvas for sections 41974 and 41967 in a browser. Founder choices left open by 0064: the Picks strip on or off, the Pay button's contrast route (L10), a shop link on `artist.html` (L11), unifying `.sect` headings on the ink-plus-bar recipe (S11). Before it: Batch six is on its branch (merge SHA in the push log once it lands). Two readings for the founder to confirm: "10 shows for free" is still counted per calendar month in the code; the band sign-in lines left the cards but the seats (1/1/5) are still in the code. The status-bar band fix in `theme.js` needs his phone. Follow-ups still open: the venue page shows *Directions* on every row and again under *Things to know*; the Engineering OS c02 sheet/step 370033 wanting the *Studio scripts* line AGENTS.md now carries. From batch two: `admin.mjs` `logPlay` writing `paidVotes` so `topPaid` stops being null (one line + a record), and the heal back-fill of `top`/`topPlayed` on the founder's page, which runs on the next Money-tab load. Then the remaining open items from the Airbnb report (I-02 chips, I-03 map+sheet, I-04 filter sheet, I-08 date cards, I-13/I-14 first-time toasts, I-18 community topic chips) are still the founder's to pick. Otherwise: the board was watched from outside the same evening, From the dashboard session, still open: The dashboard is live — the founder's next requests as he uses it. His own Money tab will say "app money not available" for his nine `stripe-unreachable` and six `off` nights until *Re-check*; the heal back-fills `source` on his first Money-tab load. Then the open items from the session note: the year shard for a 400 KB book; a way to mark a night "not a gig"; `dropped` on the tab. Then, as before: batch six is on its branch (merge SHA in the push log once it lands). Two readings for the founder to confirm: "10 shows for free" is still counted per calendar month in the code; the band sign-in lines left the cards but the seats (1/1/5) are still in the code. The status-bar band fix in `theme.js` needs his phone. Follow-ups still open: the venue page shows *Directions* on every row and again under *Things to know*; the Engineering OS c02 sheet/step 370033 wanting the *Studio scripts* line AGENTS.md now carries. From batch two: `admin.mjs` `logPlay` writing `paidVotes` so `topPaid` stops being null (one line + a record), and the heal back-fill of `top`/`topPlayed` on the founder's page, which runs on the next Money-tab load. Then the remaining open items from the Airbnb report (I-02 chips, I-03 map+sheet, I-04 filter sheet, I-08 date cards, I-13/I-14 first-time toasts, I-18 community topic chips) are still the founder's to pick. Otherwise: the board was watched from outside the same evening,
and the edge taught one rule: **Netlify's durable cache ignores a lifetime under 10
seconds** (3–9 bypassed in every spelling, 10 and 60 hit; measured on two draft deploys
and on production), so the polling dial's middle rung is now 10 s (rooms of 201–3,000)
and pubs keep 3 s with a per-node copy — decision `0034`, INVARIANT 0fi. Still to do
from outside: upload one clip on a community page and `curl -sI` its `/api/vid`
— a **302** with `location:` on `r2.cloudflarestorage.com` and `max-age=3600` — then play it
on a real iPhone; try a large (~75MB) clip once, because the PUT's fit inside the function
budget is unmeasured (session `2026-09-11-clips-to-r2.md` § "The order to deploy in").
Then P3-013 (trim the cast receipts that make a voter's record ~2 KB). The open line
(P3-002) runs **on the user's word — he said not to wait for a 2,000-person booking**; the
numbers to design from are in decision `0035`, and the first design question is the
hostname (cross-origin `workers.dev` now, `line.myset.vip` delegated later).

**Fresh-agent one-liner:** `Read AGENTS.md, then docs/PUSH-LOG.md and IMPLEMENTATION_STATUS.md; Artist Diaries phase 1 (DIA-001, decision 0085) is the newest batch, live as a87ff85; before it the founder's Money-tab list (UX-053, decision 0082) — on its branch until the push log says it merged; before it payouts follow the plan (PAY-001, decision 0080) is the newest batch — Hobbyist every Monday, paid plans daily, the schedule re-checked at every plan write and account read; before it the artist page's doors (UX-047/048/049, decisions 0074/0075) are the newest batch, live as a69154f; before them the data foundations (DAT-001, decisions 0066–0069) — every night has an event log, every hand-edited document a version, every document a nightly copy on R2; start from "Next work item".`

---

## Status vocabulary

`not_started` · `in_progress` · `blocked` · `done` · `deferred` · `cancelled`

---

## Phase gates

| ID | Gate | Status | Evidence | Notes |
| --- | --- | --- | --- | --- |
| GATE-001 | A gig runs end to end with no intervention | done | The Ugly Duckling, 2026-08-30 — 8 voters, 21 votes, one $3 purchase, nothing went wrong | The only real-world data point there is |
| GATE-002 | A second artist can sign up, get paid and run a show without Perry | in_progress | Accounts, roles, billing and Connect all shipped; nobody but Perry has done it | The real test of the $10/month ambition |
| GATE-003 | A room of 2,000 works, not just costs an affordable amount | in_progress | P3-001 live 2026-09-11: `tools/loadsim.py --ceiling` puts the busiest-case read wall at ~2,500 **at today's measured record weight** (it was ~700–1,000 before the split at that weight; the old "~2,500" assumed 152-byte records). The shared copy is measured to hold from 201 phones (10 s, durable); under that it is per edge node | P3-013, then P3-005. **Still do not sell a bigger room** — nothing has been measured at a real big gig |
| GATE-004 | A bug reported by a fan can be traced without reproducing it | done | Hour-keyed durable server errors + fan report context + Studio report reader; 42/42 focused assertions | Reports retain the three server hours before the fan's note |

---

## Phase checklist

### Current requested batch

| ID | Work item | Status | Evidence | Blocker / next action |
| --- | --- | --- | --- | --- |
| EVS-001 | **Every show on the platform** (decision `0095`, INVARIANT 0gi): `netlify/functions/_register.mjs` (`slimNight`, `buildRow`, `mergeSplitNights`, `silentNights`, `rollup` → `act`, `foldRegister` — one writer under the `registersync` lock, incremental by `rowSig` + calendar etag, time-boxed, cursor, month shards `register_<YYYY-MM>` + head `register` + `register_work`, frozen never-go-down counts for requests/RSVPs/ratings, the morning-after `reconcileShow`, tombstones for departed artists), `_nightrule.mjs` (the ONE night rule; `_metrics.mjs` re-exports it, `_warehouse.mjs` uses it), `registercron.mjs` (`*/10`, idle ring = two reads, `regdirty` marks from `_lifecycle.mjs` markLive/unmarkLive and `history.mjs` rename/hide/reconcile), `_passgate.mjs` (the model's door, extracted), `_showsdash.mjs` + `finance/shows.html` (the dashboard at `/moneymodel/shows`, `shows.json`, `shows.csv`, `shows/night.json`, `shows/refresh`; `/shows` 301s), `moneymodel.mjs` (`/moneymodel/*` routed; `live.json`), `finance/model.html` (METER_KEYS, `pickAct`: live → newer paste → seed; the REAL SHOWS stamp names the source), the archive files `country`/`tz`/`plan`/`startedBy`/`endedBy`/`requests`/`rsvps` and keeps `hidden` through a re-archive, `_pay.mjs` tags tips/paid marks with the show, `moneyForShow` cuts only untagged money at the next night, `_mirror.mjs` GLOBALS + `globalKeys()` (0ft), RESERVED gains the routed names + `test/structure.mjs` checks the toml against it, `finance/fixtures/2026-09-25/` (a read-only production snapshot, stripped) | done | `test/everyshow.mjs` 91 ✓ (5 nights = the tracker on 11 Sep; 15/2/13 + $1.037 + 8.6/2.73/2.09/10 on 25 Sep; hidden flagged; no fan; frozen counts; merge takes untagged once; e2e fold/marks/door/bell/feed); `finance/model-test.mjs` all passed (live precedence, meters kept); `tools/actuals-test.py` all passed (25 Sep pinned); `test/sheets.mjs` 196 ✓, `test/metrics.mjs` 20 ✓ on the shared rule; the dashboard opened against `tools/localhost.mjs` | **Live as `a8cd3c9`** (PR #96): the first ring built the register ~9 min after the deploy; 26 Sep 02:30 UTC: 36 rows (16/2/13/5), cron fold 544 ms / 15 reads. Re-take the empty-day meter (0ge) after a day of rings; PER-010 (`FINMODEL_CODE`) matters more now — this page shows every artist's takings per night; the Python still expands calendars by weekday (monthly/`until` would diverge) |
| EVS-002 | **The shows dashboard in the /mediadash look** (amends decision `0095`): `finance/shows.html` rewritten on the /mediadash tokens (black glass, SF system type, one orange, Apple green for money; light and dark); the main table gains a group header row (Votes: all / bought / $ · Tips: count / $ · Merch: items / $), alternate shaded column bands with each group one band, a totals row pinned under the headers that adds up whatever the filters show, every dollar above zero green, a fifteen-row window on every table (`frame()` measures the rows), a sticky first column; the night opens in a side drawer; four charts with both axes titled and dollar tooltips (the by-country chart's category axis printed “$0” — fixed); the nine breakdowns in a two-up carousel (one-up on a phone) turned by arrows or the keyboard, wrapping at the ends, a chip per table | done — **live as `5e978ac`** (PR #97, merged 2026-09-26; production answered the page with the passcode, carrying `carNav`, `tr.tot` and the 15-row `frame()`, the chart from /vendor/) | `sh test/run.sh` exit 0, 3,673 ✓ (`test/everyshow.mjs` 92 ✓ — the page check now asks for no external stylesheet instead of the model's fonts); the page opened in the app's browser against a read-only copy of production's `shows.json` and night pages: 16 counted rows, the window capped at 15, totals 146 phones / $72 tips / $10 bought, carousel 1–2 → 3–4 of 9, drawer, light theme, 375 px with no sideways page scroll | — |
| EVS-003 | **The money model in the /mediadash look, and four asks on the shows page** (second amendment to decision `0095`): `finance/model.html`'s style and markup rewritten on the shows page's tokens (black glass, SF system type, one orange; light and dark, dark first; no web font, so `localised()`'s font swap no longer matches anything on it); a sticky glass bar, a dot-grid hero with the KPI tiles (revenue, costs and profit twice the size), a lit divider, scenario cards, dials in rounded panels; every table: bold ink headings, alternate shaded columns, total/sum rows in a light orange with deep-orange text, the selected host in a blue wash so it never reads as a total, a sticky first column, a fifteen-row window (`frameAll()`, re-run on resize and when the growth box opens); every chart names both axes (`axisTitle`); the four notes in a dark band with the sources. The shows page: headings 12.5–13 px bold ink, the totals row in the same light orange (its text not black), a dot-grid hero and a lit divider over the main table, the breakdowns turned two to a page, stacked full width (`PER = 2`, the track takes the page's height), the rules three across in the dark band. Engine, data and endpoints untouched | done — **live as `16137f3`** (PR #99, merged 2026-09-26, verified by content with the passcode) | `sh test/run.sh` exit 0, 3,673 ✓; `node finance/model-test.mjs` all passed; both pages opened in the app's browser (the model on its seed, the shows page on a read-only copy of production's `shows.json`): model — no console errors but the live feed's expected 404 off myset.vip, sum rows computed rgb(168,64,26) on the orange tint, headings #0A0A0C 800; shows — carousel 1–2 → 3–4 of 9, track 916 → 388 px; light and dark; 375 px with no sideways page scroll on either | — |
| EVS-004 | **What each show cost** (third amendment to decision `0095`): `netlify/functions/_showcosts.mjs` prices every counted night — **server** = traffic credits the night added over an empty day by Netlify's own per-day meters (`finance/actuals.json` meters.nights, matched on `hist_<artist>_<showId>`, merged nights summed), else the average of the measured nights (marked ≈), × the auto-recharge pack's $/credit on the latest `finance/credits.json` reading ($5 / 500 = 1¢); **Stripe** = an estimate at the money model's P0.stripe rates (2.9% + 30¢, +1.5% on 13% of cards) over room money + merch, 30¢ a payment; no figure when Stripe never answered; **total** = the two. `_showsdash.mjs` adds `costs` to each row of shows.json and the block (without the key table); `finance/actuals.json` and `finance/credits.json` join the function's `included_files`. The register and its block stay meter-free (INVARIANT 0fx; the model feed test still holds). The page: a Costs group (Server · Stripe · Total) after Merch, never green, ≈ on estimates, in the totals row; a Costs tile (KPI tiles folded to twelve); the night's drawer lists the three and the room money less costs; a rules card | done — **live as `315f525`** (PR #102, merged 2026-09-26, verified on production's shows.json with the passcode) | `sh test/run.sh` exit 0, 3,684 ✓ (`test/everyshow.mjs` 101 ✓: rates equal the model's P0.stripe, pack price, measured vs average, unknown money → no figure, uncounted → none, 400 deploys move no show's cost); the page in the app's browser on production's rows priced locally: totals ≈ $0.48 server / ≈ $5.93 Stripe / ≈ $6.29, 20 Sep drawer 3.42 credits $0.03, 4 payments on $35 ≈ $2.28 | — |
| EVS-005 | **Stripe's own fee on every night** (fourth amendment to decision `0095`): `_history.mjs` `moneyForShow` asks the session list with `data.payment_intent.latest_charge.balance_transaction` expanded and sums `feeOfSession()` over every payment it counts (tagged and window) into `money.fees {usd, charges, missing}` — every `fee_details` line but `application_fee` (Stripe's fee, tax on it, pass-through), never `bt.fee` whole (it carries MySet's application fee on a direct charge), a non-USD balance transaction brought back at its own `exchange_rate`, no figure without one; if Stripe refuses the expansion the takings are read without it and the fees come back missing. The index row carries `stripeFees` (exact, or null when any payment's fee was missing) and `rowSig` includes it; `slimNight`/`slimFromRow` and the register's row carry it (merged nights summed). `recheckSome` asks a counted, money-known night with no exact fee once (`work.feesAsked`), whatever its age, `FEE_ASKS_PER_RING = 2`, only after its morning-after ask is done or out of window, never marking `rechecked` for it, inside a 12 s time box past the walk's; it writes the FEE ALONE (`refreshShowFees`) and only when Stripe's takings match the night's to the cent — a fresh-context review caught that re-reconciling old nights could have rewritten their money (Stripe down → unknown; a connected account read on the platform → $0); a merged night is asked for each record. `_showcosts.mjs`: Stripe is exact where `stripeFees` is on file (merch orders stay the published-rate estimate, marked); the page shows ≈ only on an estimate | done — **live as `5a25263`** (PR #104; on production 20 Sep's $35 night reads Stripe's own fee $2.67 vs the $2.28 estimate) | `sh test/run.sh` exit 0; new `test/stripefees.mjs` 29 ✓ (incl. the fee-only write refusing on Stripe down and on different takings, the tax line, the time box) (USD / application fee excluded / THB at its rate / no rate / uncaptured / zero; a night filed with $1.19 on two payments through to the index row and the register's row; a missing fee makes the night not exact; exact vs estimate vs merch in the table; the register asks an old night once and never spends the late-tip ask; takings still read when the expansion is refused) | The money model's Stripe dials (P0.stripe, 13% of cards from abroad) read 17% low against the first exact night — revisit once the backfill is done |
| PERF-001 | **The fan pages share one script** (decision `0087`, INVARIANT 0gc) — **live as `68efdb4`** (PR #85, merged 2026-09-25 03:51 UTC; live on production ~30 s later): `public/fan.js` — `$`/`esc`/`fanId`, `toast`, `shareLink`, the date words, `money`/`liveIn`/`firstNameOf`, `dirHref`, `rsvpMap`/`rsvpRemember`, `cssReady`/`hideIntro`/`lift`, the sheet (`freezePage`/`thawPage`, `openSheet`/`closeSheet`/`attachDrag`, Escape, `customAmt`), `drift`/`undrift`/`ghost`, `menuDoors` — loaded by index, artist, artists, vote, community, venue, about, shop and diary right before their own script, hash-stamped (`tools/stamp.mjs`, nine `PAIRS`) and served immutable (`netlify.toml`); the page copies deleted (716 lines); the sheet is the shop's a11y version on the artist page's frozen-page counter, drag zones on the element (`data-handle`/`data-scroller`), `sheetopen`/`sheetclose` events for a page's own needs; `test/_src.mjs` reads the pair, `test/structure.mjs` refuses a redeclared name, a missing tag, a sheet without the file; `tools/_puppeteer.mjs` brought `uicheck`/`sheetcheck`/`clipcheck` back (they had imported a moved path since 2026-09-20) | done | **live as `68efdb4`** (PR #85, merged 2026-09-25 03:51 UTC; live on production ~30 s later); `curl -sI https://myset.vip/fan.js` → `public,max-age=31536000,immutable`, `content-encoding: br`, the stamp in every page matches the file (`580721d7`). Before the merge: `sh test/run.sh` 52 files 3,502/0 on the final tree (3,478/0 before the review's fixes); `node tools/sheetcheck.mjs` vote 12/12 + shop 27/27; `node tools/uicheck.mjs` 242 ✓/2 ✗ on both origin/main's pages and these (the same two stale profit figures); `tools/mock.mjs` in the app's browser at 375 px on every fan page — no page errors, every sheet froze/thawed/gave focus back; session `docs/sessions/2026-09-25-the-fan-pages-share-one-script.md` | Open: the Studios keep their own `$`/`esc`/`toast`/sheet (0053); the About page is the ninth loader — measure its first open on a phone |
| PERF-002 | **A fan's first open is the visit that matters** (decision `0088`, INVARIANT 0gd) — **live as `68efdb4`** (PR #85, merged 2026-09-25 03:51 UTC; live on production ~30 s later): measured every page, asset and public read on production (HTML is an edge lottery, per-node copies — 4 hits in 12; a durable hit ~0.45 s; `artists`/`mapconfig`/`places` cold at 1.2–2.0 s; the artist page pulling ~400 KB of originals behind its thumbnails; `pull.js` blocking three heads; the vote page bare until the board). Shipped: one `<img>` per slot on `artist.html`; `pull.js` to the bottom on vote/index/about and `theme.js defer` in both Studios; the vote page's `#intro`, `<style id="tokens">` (app.css's :root blocks, byte-identical by test), preloaded app.css and a ≤400 ms first-paint wait, *Couldn't reach the room* when the board never comes; `fan.mjs` DOORS += `artists`, `mapconfig` (edge-shared 60 s / 300 s, places 300 s), `index.html`/`artists.html` through the door with head-starts; `tools/mock.mjs`/`uicheck.mjs` follow. Not done, each a founder decision: SW stale-while-revalidate navigations, app.css inlined everywhere, the community read split | done | **live as `68efdb4`** (PR #85, merged 2026-09-25 03:51 UTC; live on production ~30 s later). AFTER, measured on production 2026-09-25: `/perryidyll` in the app's browser fetches 0 `/api/img?` originals (was 4 — 85/108/123/85 KB), four `<img>` slots; `/api/fan?what=artists&maps=1` 1.49 s on the first call after the deploy then `"Netlify Durable"; hit` at 0.46 s (and an Edge hit at 0.17 s); `what=mapconfig` 0.42 s durable hits; `what=events&places=1` 0.42–0.45 s durable hits (before: 1.98 / 1.15 / 1.77 s cold, `fwd=bypass`); the directory's two reads 322 / 314 ms and the front door's 311 ms in the browser; `/perryidyll/vote` served with `<style id="tokens">`, `#intro`, the app.css preload and zero `<script src>` in the head; `/fan.js` `cache-control: public,max-age=31536000,immutable`, stamp `580721d7` matches the file. Before the merge: `sh test/run.sh` 52 files 3,502/0; `node tools/uicheck.mjs` 242 ✓/2 ✗ (parity with origin/main); `node tools/sheetcheck.mjs` 39/0; the mock in the app's browser at 375 px (vote intro/preload/failure path, artist thumbs, front door + directory through the door); measurements in `docs/sessions/2026-09-25-the-first-open-speed-pass.md` | Open: the three founder decisions above (SW stale-while-revalidate navigations; app.css inlined everywhere; the community read split), and a real phone on bar wifi for the honest test |
| PERF-003 | **A page seen tonight is shown from the phone's copy and refreshed behind it** (decision `0091`, INVARIANT 0ax rewritten) — `public/sw.js` rule 2: a page this phone fetched in the last six hours is shown at once from its copy (keyed by path, stamped `x-myset-stored`) and re-fetched behind with `waitUntil`; older or unseen pages wait for the network, the copy only when there is no signal; a redirect, a non-200, a `no-store` or `private` page (the money model) is never stored; a reload (`no-cache`/`reload`) asks the network first; every cache call guarded; cache `myset-runtime-v4`. Rule 3: a stamped file (`?v=…`) is never re-asked (load-bearing: an old page must keep its old script); the refresh of an unstamped file is held open. Push, activate, the escape hatch untouched. `test/sw.mjs` runs the real worker in a fake browser and is in `test/run.sh`; 0ce, `AGENTS.md`, `netlify.toml`, `pull.js`, six pages' comment, `FINGERPRINTS.md`, 0038, the overview and the handoff re-pointed | done | **live as `ebdce9e`** (PR #94, merged 2026-09-25; verified by content on production — see the session note): `node test/sw.mjs` 33/0, eight mutations each fail; `node tools/overview.mjs --tests` 3,535/0; the app's browser on `tools/mock.mjs` (its pages now `no-cache`, not `no-store`, so the worker may keep them — production's pages are `public, max-age=60`, only `/moneymodel` is `private, no-store`, checked by read-only GETs) with the worker registered by hand — artist page 101,281 bytes then 0 transferred on the second open (different query), a `location.reload()` 101,281 again (a reload goes to the network), then 0 again; vote page 117,484 then 0, board rendered; no `/api` key in the cache; no console errors; `myset-unregister` drops the worker; session `docs/sessions/2026-09-25-a-page-seen-tonight.md` | Open: a real phone on bar wifi; a deploy during a gig now lands on the open after next (≤ 6 h); option D (tell the page a newer copy arrived) is the founder's; the other two 0088 levers are PERF-004/PERF-005 below |
| PERF-004 | **app.css rides inside every fan page, byte for byte** (decision `0094`, INVARIANT 0gh) — the whole stylesheet between `<style id="app-css">` and `</style>` where the `<link>` stood on all nine fan pages, written by `node tools/stamp.mjs` (`appBlock`/`appRe`; the tool refuses an app.css containing `</style`); the vote page's 0088 tokens block and every preload/noscript pair gone; `cssReady()` true at once so the 400 ms waits end on their first frame; `test/structure.mjs` refuses a stale copy, two blocks, or a re-added `<link>`; the id is `app-css` because `#app` is every page's content container (the first cut used `app` and the render wrote into the stylesheet — caught in the browser) | done | **live as `ebdce9e`** (PR #94, merged 2026-09-25; verified by content on production — see the session note; AFTER: every fan route one `app-css` block, no `/app.css` link, the inline copy's hash equal to the served file's; `/perryidyll` 36.2 KB br with the styles inside): `node test/structure.mjs` 18 new ✓; `node tools/overview.mjs --tests` 3,571/0; the app's browser on the mock at 375 px: community/artist/vote/shop pages make no request for `/app.css`, the inline sheet has 98 rules, buttons rounded, body painted from the tokens, dark and light; `tools/uicheck.mjs` 242 ✓ / 2 ✗ (parity with origin/main); `tools/sheetcheck.mjs` 39/0 after its grab-area assertion was moved from the bounding box to the layout height (the sheet is mid-spring when it runs now that the styles are present at first paint) | Open: a real phone on bar wifi; 0051's token and recipe copies in shop/diary are redundant now — retire them in a later cleanup; each page +~4 KB compressed |
| PERF-005 | **The community page reads a shared copy and wears its own marks** (decision `0093`, INVARIANT 0gg) — `community.mjs` GET with no device named is the SHARED read: `canPost` true, every mark off, no token consulted, `jsonCached(…, 30)`; `…&fan=<id>&me=1` is the PERSONAL call (`mine`, `editable`, `liked`, `canPost`; registry + posts + likes, never cached); the old whole reply stays for kept pages; every reply that carries the list says when it was made (`at`). `community.html` starts both reads from the head (the shared one for a signed-in artist too), paints from the shared one, `wear()`s the marks from the kept copy then from the personal call, keeps a newer list it already holds over an older shared copy, and marks `D` on a like; the shop page's read is the shared one for free; `tools/mock.mjs` answers `me=1` | done | **live as `ebdce9e`** (PR #94, merged 2026-09-25; verified by content on production — see the session note; AFTER: the shared read `"Netlify Edge"; hit` 0.44–0.47 s and a durable hit 0.42 s, was 0.55–0.73 s `fwd=bypass`; the personal call 0.49 s `no-store`): `test/community.mjs` 285/0 with the new block (edge header, no mark in the shared read, the artist's token changes nothing, the personal call's four answers, 404, no-store, ≤4 reads, 0 writes; the old reply still personal); `test/fandoor.mjs` 31/0; the app's browser on the mock: the community page fires `what=community&a=demo` and `…&fan=…&me=1` from the head, 5 posts painted, the composer docked; the shop asks the shared read alone | Open: a real phone on bar wifi; the ♡ → ♥ beat on a phone with no kept copy not reproduced; a purge on write (option C) if a room ever needs a post seen within seconds |
| SHT-001 | The Google Sheet's marketing read (decision `0072`): `_warehouse.mjs` — two new snapshot tabs **Signals** (source, referral, city from gigs, a derived segment, active, real nights all-time/30 d/per week, calendar gigs per week, published gigs used %, rooms, votes per phone, packs and conversion, tips, money per night/phone, days to first/since last show, stars, library, genres, seats, test nights) and **Features** (a column per feature, a score of 24); *Shows* rows tagged real/test with the published gig, votes per phone, paid votes; *Venues* with artists/nights/phones/votes there, posts, merch, Connect, seats, password, last sign-in; `_sheets.mjs` `existingKeys` / `rowKey` — log tabs never append a row they already hold; `GUIDE` exported; the plan returned on a dry run | done | `test/sheets.mjs` 175 ✓; suite exit 0; the first copy of the founder's real data rendered from the dry-run plan to a styled .xlsx and handed to him for a one-step import into the empty *MySet data* sheet in his Drive (session note `2026-09-14-sheet-marketing-tabs.md`) | The service account + three variables (run sheet 29) make it nightly — **switched on by the founder 2026-09-15**: Sheets API enabled, `myset-sheets` service account, three Production-only `GSHEET_*` variables (the key secret), first sync 313 rows / 11 of 11 tabs; the 10 s scheduled function will want the mirror's cursor pattern before there are dozens of artists |
| UX-050 | Tip sheet: the *Or another amount* field is the fourth tile — lit orange with the soft fill when tapped, the $5/$10/$20 tiles dim, the Continue button reads the typed amount (`· $7`); same on the community page and the paid-replay custom field | done | `98e2320` (PR #68); `customAmt()` in `vote.html` / `community.html`, `.inp.on` in `app.css`; deploy preview read at 375 px: field `inp on`, tiles unlit, button *Continue to payment · $7* (a transition frozen by a hidden tab hid the border at first — with the transition off, the accent border reads) | Look at it on a phone |
| MON-001 | **A night's money is everything tagged to it** (decision `0081`, INVARIANT 0ga): the founder's two $1 tips sent the day after the 14 Sep night showed in the Money tab's *Taken* tile ($22) and not in profit ($30). `_history.mjs` `moneyWindowEnd` (a night's window runs to the next night's start, or now) + `refreshShowMoney` (the detail and the index row take one figure); `history.mjs` heals the filed row from the read that draws the tile; *Re-check* uses the same window | done | `test/latetips.mjs` 19 ✓ — 6 of them fail on `origin/main`'s code; suite exit 0 | Open the Money tab once: the tile and profit agree |
| UX-051 | **The first gig is the onboarding** (decision `0081`): the first run is four steps ending on *Print my sign* (`/sign.html` — name, *Pick the next song*, the code, the address, print-ready, *Send it to my phone* by the share sheet; `signPrinted` → `meta.signAt`); *Your first gig* pinned on the Live tab until a night is filed (`meta.nights` on the stage payload, stamped by `endShow`); three example rows on an empty first board, artist-only; the tip burst (`tipWatch` / `tipBurst`, chime + buzz + confetti); tonight's money sticky under the header on every plan; one morning-after letter (`_lifecycle.mjs` queues it on the first filed night, `_auto.mjs` `sweepNotes` sends it ten hours later, once) | done | `test/firstgig.mjs` 34 ✓; uicheck 244 ✓; mock shots at 390 px (`?first=1`, `?first=1&live=1`), the sign as PDF | The founder's own first look on a phone |
| UX-052 | **"Diary" is reserved for the song-story feature.** The artist page's list of upcoming gigs (`events.mjs` `?a=<slug>`, the RSVP horizon, the calendar's dot source) was called *the diary* in comments, tests and docs; the founder is keeping *Artist Diaries* for a per-song storytelling feature (strategy doc 2026-09-15), so the gig list is now **the gig list** everywhere that is still read — code comments, `test/rsvp.mjs`, `tools/mock.mjs`, INVARIANTS, the handoff, the fans' night f22, the catalogue and this ledger. Dated records (decisions 0042/0052/0056/0075, session notes, the landing audit) keep the old word as written | done | `git grep -i diary` over `netlify public test tools INVARIANTS.md IMPLEMENTATION_STATUS.md HANDOFF-MySet.md docs/processes docs/landing/APP-CATALOGUE.md` finds nothing; no identifier or key changed (`ev_<owner>`, `HORIZON_DAYS`, `/api/events` untouched); `sh test/run.sh` green | When Diaries is built it owns the word: name its keys `diary_…`, nothing else should |
| UX-053 | **The founder's Money-tab list** (decision `0082`): burst on votes bought (`stage.mjs` `paid` {count,total,last}; brief over a chart); *$x from in-app tips* (`_history.mjs` `tipped` in `ROW_TOPS`, `Biz.join` `tipsApp`, `Biz.sum` `tipsApp`/`tipsAppKnown`); currency (`bizPrefs {currency}`, `Biz.currency`, `/report` follows); gig form *Pay and the business side* open; `.bzmin` `inputmode=text`; ✕ clear (`.bizro` margin-top 20px); save/delete → `toTop()` before `render()`; by-show chart labels thinned past 8 bars and clipped by the row (`scrollWidth` 441 → 393); *Past shows*; `FOLDN` 5/+20 on All payments and MySet's books, `MORE` 5/+20 on Past shows; bug card last; *Delete show* (`hideShow`, `history.mjs` filters `hidden`) | on its branch | `studio/money-tab-batch` off `0b4db1c`; suite exit 0; new assertions in `test/bizmath.mjs`, `test/biz.mjs`, `test/histname.mjs`; Chrome 375px and iOS Simulator + Playwright WebKit checks in decision `0082` § How it was verified | The founder: open the PR's deploy preview on his phone, pinch the Money tab (it should refuse to zoom out), open the editor, save, delete a test show; merge is the deploy |
| UX-054 | **Swipe to reveal, hold for more** (decision `0083`): every Studio list row with action buttons gets an iOS-style swipe-left tray and a press-and-hold sheet, both built from the row's own buttons (`studio.js` foot module, `studio.html` `.swtray`/`.swact`/`.sheet.swipe`); a full swipe never fires a button; a one-time tip toast | on its branch | same branch as UX-053; suite exit 0; Chrome 375px synthetic touch + iOS Simulator real touch — decision `0083` § How it was verified | The founder: swipe a song, hold a gig on the preview; port to the Venue Studio if wanted |
| UX-055 | **Two boxes per kind of time, hours and minutes** (decision `0084`): `Biz.parseHms(h,m)`, `data-u` on the box, `inputmode=numeric`, the unit after the box not the label, 60+ minutes roll into hours on blur; *Set-up / break-down* in both `TIME_KINDS`; *Edit last show's numbers*. | done | `sh test/run.sh` exit 0; `node tools/uicheck.mjs` exit 0, 244 ✓ (two stale 0082 expectations repaired); Chrome 375px against the mock. Live as `9f46b8b` (PR #77). |
| UX-056 | **Tips shared on their own** (decision `0086`): `tipsCut` on the record (server + `Biz.norm`); `Biz.calc(gig, app, prefs, fee, tipsApp)` returns `tipsAll`/`tipsMine`, *My cut* = pay share + tips share; the show sheet's Cash tips / In-app tips (read-only) / My cut of tips; the profit block, row chips and the sheet's readout follow *Total / My cut*; the Live tab's Tonight figure and tile are tips + votes bought. | done | `node test/bizmath.mjs` 199 ✓; `sh test/run.sh` exit 0; Chrome 375px against the mock (money tab, the sheet, `?live=1`). uicheck not run — its puppeteer install is gone from this Mac. Live as `84f0b6b` (PR #80) + `2f6a095` (PR #82 — profit = revenue − costs before the splits, the founder caught Total ≈ My cut); live Money tab, This year: TOTAL PROFIT $8,686.00 / MY CUT $3,585.33 over 65 shows, 64 logged, 64 timed. `tools/uicheck.mjs` 564/614 expect the pre-#82 profit figures — recompute when it next runs. |
| UX-057 | **Loaders never freeze; lyrics read as verses** (decision `0090`, INVARIANT 0gf) — **live as `ee39bb1`** (PR #90, merged 2026-09-25 06:22 UTC; verified by content on production ~06:24: `/leave.js` serves `msLeaveFill1`, `/perryidyll` the `<i><b></b></i>` bars, `/perryidyll/vote` `introfill1` + `lyr-st`, `/studio.html` `bbfill`, `studio.js?v=85eaccb7` `verses()`). The founder's iPhone recording showed the page-change splash as three still dots for ~0.9 s. Every page-change, boot and busy splash now moves by transform only: a window `i` that clips, a pill `b` that slides by translateY, a gradient `b::before` that stretches by scaleY, and negative delays. The splashes are `leave.js`, the six fan-page `#intro`s (including the vote page's from 0088), and both Studios' `#busy`/`#leave`/`#boot`. Lyrics: the Studio sheet uses the MySet face (`--f`) at 20 px instead of the chord chart's monospace, and the vote page's sheet is 19 px. Both split into `.lyr-st` verses, with every other verse on a grey band (.12 light / .24 dark). The Studio sheet no longer drags closed from inside the words (0f1). | done | **Reproduced and fixed in iOS 27 Simulator Safari.** The test was a local page whose next page answers 3 s late, recorded with `simctl io recordVideo`. The old `leave.js` and the Studio `#leave` froze as dots for the whole wait; the new ones moved in every sampled frame, from the first. **Pixel comparison, old against new:** Playwright WebKit (webkit-2359) and headless Chrome at 390 px, all twelve splashes paused at 0/¼/½/¾/1. Heights, widths and round ends are identical, and the mean colour is within 1/255. **Checks:** `sh test/run.sh` 3,286/0; `test/copy.mjs` 73/0, including the new 0gf guard, which was mutation-tested (it fails on the old splash); `tools/sheetcheck.mjs` 39/39; `tools/uicheck.mjs` 242 ✓, with the 2 ✗ being the known stale profit figures (lines ~564/614, since 0086). Lyrics were checked at 375 px in the mock in light and dark. **Not checked** on a physical iPhone. |
| UX-058 | **A Studio tab opens at its top** — **live as `beed75b`** (PR #98, merged 2026-09-26 06:21 UTC; verified by content on production 06:22: `/studio.html` → `studio.js?v=df44350a` carrying `if(t!==TAB)window.scrollTo(0,0)`, the same in `venue-studio.js?v=4e87826b`). the founder, 2026-09-26: the first tap on Gigs took a moment and then showed the bottom of the page. `render()` keeps `window.scrollY` so the 4 s poll never moves anyone; a tab switch rode the same path, so the old tab's offset landed on a Gigs page still short with loading, clamped to its bottom, and stayed there once the gigs filled in. `setTab` in `studio.js` and `venue-studio.js` now calls `window.scrollTo(0,0)` when the tab changes (a re-render of the same tab keeps its scroll). Per-tab scroll memory was not chosen: a restored offset on a still-loading tab clamps the same way. | done | **Reproduced before the fix** on `tools/mock.mjs` with the tab's reads held 1.5 s: Chrome (the app's browser, 375×812) and Playwright WebKit (webkit-2359, iPhone 13) both landed Gigs at the bottom of its 736–884 px loading page (scrollY 72) and stayed there at 1,630 px. **After:** scrollY 0 while loading and 0 when loaded, from the bottom of Setlist and the middle of Money; a same-tab re-render kept 300 → 300; the Venue Studio's four tabs open at 0 from any offset. `test/copy.mjs` guards both `setTab`s (mutation-tested: fails on the old line); `sh test/run.sh` 3,434/0. **Not checked** on a physical iPhone. |
| MKT-001 | **The Instagram dashboard at myset.vip/mediadash** (decision `0092`): `netlify/functions/mediadash.mjs` keeps and serves one JSON the content engine pushes after every pull (four times a day) plus a small JPEG per live post, in three computable blob keys; `public/mediadash.html` is the engine's `publish/dashboard.html`, byte-identical; `/mediadash` rewrite above the catch-all; boosts logged on the page go to `mediadash/boosts` behind the founder's sign-in or recovery code and flow back to the engine on its next pull | done | **live as `4f95289`** (PR #92, merged 2026-09-25 ~06:50 UTC). The squash inherited `[skip ci]` from a docs commit on the branch, so Netlify built nothing until a build of `main` was triggered by hand (`netlify api createSiteBuild`, 06:54); verified by content on production ~06:55: `/mediadash` → `<title>MySet Signal</title>`, `/api/mediadash` → 14 posts · 29 days, `?thumb=` → 200 image/jpeg 7.6 KB, a boost without a code → 401. The engine's push (38 KB + 14 thumbs) landed on the deploy preview and production serves the same store; the first push straight to production followed the merge. Suite green on the branch before the PR | The page is as fresh as the Mac (the scheduled pull runs while the Claude app is open); reads are public, writes are two doors (push key `MEDIADASH_KEY` in Netlify's env, the founder's code) |
| PAY-001 | **Payouts follow the plan** (decision `0080`): `_connect.mjs` `payoutScheduleFor` / `syncPayoutSchedule` — Hobbyist `weekly` on `monday`, paid plans `daily`, set at account creation and re-checked from `syncFromStripe`, `_billing.syncSubscription`, `redeemPromo` and `rewardReferrer`; `connect_<owner>.payout` remembers what Stripe was last told; `connectStatus` carries `payout` + `payoutLine`; the plan cards say *Paid out weekly* / *Paid out daily*, the Get-paid card names the current one. Same PR: a Stripe refusal on `accounts.create` / `accountLinks.create` shows the artist `REFUSED` (one MySet sentence) and files Stripe's reason with `logErr('connect.create' / 'connect.link')` — on 2026-09-15 the artist saw *"Please review the responsibilities of managing losses … platform-profile"* on step 4 of the first run | in_progress | `test/connect.mjs` 69 ✓ (9 new under *WHEN THE MONEY LANDS FOLLOWS THE PLAN*, 6 under the refusal); `sh test/run.sh` 49 files, no ✗; `test/stripe-fake.mjs` gained `accounts.update` and `state.refuse`; merge SHA in the push log once it lands | Not checked: a real account's schedule on Stripe (the fake stores the settings) — read it on the first artist's account. A lapsed plan is caught at the next Money-tab look, not on the day |
| SHT-002 | The sheet hands over before it is full (decision `0073`): `_sheets.mjs` `sheetCells` / `createSheet` / `shareSheet` / `useSheet` (+ `drive.file` scope); `_warehouse.mjs` `settleSheet` runs every sync — one email at 60%, a successor at 80% named with the date, shared with the same people, recorded in the `gsheet` doc, written into the same run; `sheetStatus` carries id, url, cells, pct, the chain; the Studio's Sheet card shows the room and an *Open* link | done | `test/sheets.mjs` 193 ✓ (ROOM); suite exit 0 | `GSHEET_SHARE` (optional) names who the successors go to; default is the owner addresses on the founder's account |
| MET-001 | Current Show Stats (decision `0071`): `netlify/functions/_metrics.mjs` (`occurrences`, `placeNight`, `artistPart`, `buildSnapshot` — the first week's night rule as code), `tools/metrics.mjs` (read-only CLI by name, `--from` a backup folder, `--html` fills `finance/metrics.html`), the page template with all-time + 24h/7/30/60/90-day/custom ranges done in the browser; `_sheets.mjs` `styleTabs()` (bold white on pink-orange headers, bold tinted row titles, frozen, auto-sized) called on every sync | done | `test/metrics.mjs` 20 ✓; `test/sheets.mjs` 168 ✓; suite exit 0 / 3,085 ✓; the artifact *Current Show Stats* (the old *Gig Week One* URL) republished from the live documents; a scheduled task republishes it daily | **Retired 2026-09-25 (decision `0095`)**: `/moneymodel/shows` supersedes the page as the founder's surface; the snapshot builder stays for the Sheet and re-exports the one night rule from `_nightrule.mjs`; stop the daily artifact republish. The Google Sheet still needs its spreadsheet, service account and three env vars (run sheet 29) |
| ACC-001 | Email + password, the standard door (decision `0070`): `netlify/functions/_cred.mjs` (`setPassword` / `checkPassword` / `hasPassword` / `clearPassword`, `weakPassword`, the per-address lock), `auth.mjs` + `venueauth.mjs` (`passwordSignIn` public — one sentence, one cost for every failure; `passwordSet` with `current` or `code`, signs the address's other devices out; `passwordClear`; `pw` per email on `list`), `_account.mjs` / `_venueaccount.mjs` (`credKey` in the delete list, never in the export), `studio.js` / `venue-studio.js` (the gate rewritten to the founder's spec: `.signbox`, `join` / `forgot` / `code` / `name` / `recover` modes, `openStudioCode()` → `#pop`, `passwordSignIn()`, the Settings *Password* row, `openPasswordSheet()` / `savePassword()` / `pwForgotCode()`, a one-time offer after a code sign-in), the CSS in both `studio.html` and `venue-studio.html` | done | `test/password.mjs` 50 ✓; suite exit 0 / 3,047 ✓; uicheck 239 ✓; sheetcheck 39 ✓; `b13-signin-{light,dark}.png`, `b13-studiocode.png`, `b13-join.png`, `b13-forgot.png`, `b13-venue-signin.png` at 390 px | The founder's own first email sign-in (run sheet 5 / 12) is the real-inbox proof; then set his own password |
| ACC-002 | A Studio-code session sets a password (decision `0073`): `passwordSet` with `email` + `code` for an owner/manager address on this account; Settings row and sheet in both Studios (*Email me a code* → code → password) | done | `test/password.mjs` 59 ✓; suite exit 0 | The founder's own case: in with the Studio code, two addresses on the account |
| UX-047 | No profile shows another act's picture (decision `0078`, INVARIANT 0fy): the stock `band.jpg` is no longer any profile's default cover — `_profile.mjs` DEFAULTS `photo: ''`; the artist page paints a brand-gradient cover box and an initial tile when there is no photo, Community and shop headers the same, `og:image` is the MySet icon; the Studio's *Save profile* button centred (`.big.mid`) | done | `test/sheets.mjs` NOBODY ELSE'S BAND; mock screenshots of the empty page and the button; suite exit 0 | The founder's own page stores `/img/band.jpg` by path and is unchanged |
| UX-048 | Setup step 3 (*Prices*) on a plan without pricing: **Next** and one orange line (*You can change these later, in Settings.*) — no *See the plans* button (decision `0079`); the Settings lock pill reads *Bar Star feature · Upgrade* | done | `tools/uicheck.mjs` 244 ✓; mock screenshot of step 3 on free and the locked section | `frPlans` removed |
| UX-049 | The song cards' green pill counts a tipper's votes too and reads *Paid votes: N* (decision `0079`): `paidVoteCounts(fans, tippers)`, `tippersTonight` in `_lib.mjs`; `stage.mjs` passes tonight's tippers | done | `test/decline.mjs` 40 ✓ (WHO PUT MONEY IN) | The filed nights' `paidVotes` still count bought credits only |
| UX-050 | Settings → *What the room sees* (decision `0079`, INVARIANT 0fz): two switches, votes + voters and tips, Bar Star and up (plan flag `crowdNumbers`, `crowdNumbersAllowed`, `crowdSet`); `buildBoard` `numbers`; the vote page's *Tonight* line under the voting strip | done | `test/decline.mjs` (WHAT THE ROOM SEES); `test/limits.mjs` 112 ✓; suite exit 0 | `board.mjs`/`show.mjs` read `meta` only when the tips switch is on |
| DAT-001 | The data foundations (decisions `0066`–`0069`): the night's event log (`_evlog.mjs`, `_append.mjs`; vote rows `[cost, paid, when]`; harvest on `dropSongVotes` / `refundSongVotes` / `wipeBoard`; `admin.mjs` files `vote`+`play`, `refund`, `drop`, `reset`; `archiveShow` → `closeLog`; `/api/history?log=`), versions of the hand-edited documents (`_versions.mjs` `casKeep`; `mutateProfile` / `mutateLists` / `mutateEvents`; the library on `libChanged`), the capped feed and feedback list spilling into `postsarch_` / `fbarch_` (`spillPosts`, `spillFeedback`; `keysFor` / `keysForVenue` name the archives and their images), the nightly R2 copy (`_mirror.mjs`, `mirrorcron.mjs` `*/20 * * * *`; etag-skip; cursor), `tools/backup.py --restore` / `--wipe` (refuses `myset`; `MYSET_SITE_DIR` for a worktree), INVARIANTS 0fq–0ft | done | `test/foundations.mjs` 60 ✓; suite exit 0 / 2,932 ✓; `test/cost.mjs` unchanged ceilings; the rehearsal log in `docs/sessions/2026-09-14-data-foundations.md`; `tools/backup.py` committed for the first time (it lived untracked in the shared checkout since 0046) | The first ring (17:40Z) timed out pulling the 70 MB clips through the function — fixed in PR #37 (`vid_` skipped, per-key deadline, `keyCursor`); the 18:00Z ring after that fix ran in 1 s and finished the pass — **0 copied / 69 failed: R2 refuses every PUT (the token cannot write — P3-003)**; PR #38 makes the ring say which key and why and retry hourly; the R2 copy on a hard delete; a restore-a-version button when somebody needs one |
| UX-001 | Filtered thirty-day event map on Find artists | done | Decision `0025`; production `d1a6531`; restricted live PNG verified; 1,882/1,882 | — |
| UX-002 | Signed eligibility, Pro fee, light default and themed loading screens | done | Decisions `0024`, `0026`, `0027`; production `d1a6531`; 1,882/1,882 | — |
| UX-003 | Verified-only artist discovery, first-Settings notice and plans-testimonial cleanup | done | Decision `0028`; production `d1a6531`; served directory contains one qualifying profile; 1,882/1,882 | — |
| UX-004 | Interactive event map: pan, zoom, tappable details, Google Maps links and current-location dot | done | Decision `0036`; production `8c41e13`; Chrome verified the live map, five venue pins, event list and location retry; full suite 2,095/2,095 | Physical blue-dot placement still needs a real device location fix; denial/retry fallback is live |
| UX-005 | Free plan: ten shows a calendar month (was four) | done | Decision `0037`; `test/limits.mjs` 84/84, `test/tenancy.mjs` 80/80; overview table regenerated; the past-due banner's "undefined shows a month" fixed alongside | Working tree only — awaits the user's push |
| UX-006 | Community composer: folded to the five stars until one is tapped; "Where did you see them?" gains "Somewhere else — I'll type it" (free text, ≤60, stored as the post's label) | done | `community.html` composer + `community.mjs` `body.where`; `test/community.mjs` 116/116 | Working tree only. Not seen in a browser — checked by tests and syntax |
| UX-007 | Faster page switching: `leave.js` splash on every tap, first API call started from `<head>`, static files cached 10 min + stale-while-revalidate, Chrome prefetch on touch, smaller default cover | done | Decision `0038`; `test/copy.mjs` 37/37; `node --check leave.js` | Working tree only. Verify on a phone after the push: tap artist → vote → community and watch for any white frame |
| UX-008 | Setlist control cleanup, live-only refund actions, ended-show payload masking, centered verification notice, and starter-pack retirement | done | Production `d9b2f2f`; full suite green; rendered checks confirm the modal is centered and Setlist has no vote/refund controls; live content verified | — |
| UX-009 | Airbnb-dive batch 1: front door shows gigs near the phone (geolocation seeds the city), feed search field restyled, artist countdown says "(view setlist)", vote page counts down to the next gig between shows (wrap-up card kept three hours, Up next only while live), Merch folded into the Studio's Profile tab (six tabs fit 375px), sign-in = email + code box (Studio code behind a link), theme choice survives the back button | done | Working tree only. Decision `0039`; session `2026-09-12-airbnb-batch-front-door-vote-studio-signin.md`; `sh test/run.sh` 2,110/2,110; scratch puppeteer run at 375px (three vote states, near-you seed, 0px tab overflow, section order Videos → Merch → Save); `tools/uicheck.mjs` 104 ✓ + 1 ✗ in the other session's map | Not committed. Not seen on the live site. Rules "a vote never comes back" and "the room is always free" demoted to current behaviour in AGENTS / VISION / INVARIANTS / overview at the user's word |
| UX-010 | Product Dev 2 efficiency follow-up: Support labels; compact Setlist controls; reflowed song cards; wider gradual scroll glow; red second-by-second audience countdown and seconds on profile; saved-coordinate map pins; home-page map modal | done | Production `5cfa27d` + `51bf8a8` + `a6d7566`; decisions `0040`, `0041`; session `2026-09-12-product-dev-2-efficiency-followup.md`; full suite green; live profile ticker advances; both production maps show five exact saved-coordinate pins with A–E letters; home modal opens/closes without navigation | — |
| UX-011 | Speed pass one: each fan page paints the last copy this phone saw before the network answers (or a grey placeholder frame instead of the logo screen); `/api/profile` (15s), `/api/events` (30s gig list, 60s picker + city feed) and `/api/venue` (30s) are kept at the edge like the board; `/api/events?n=` sends only the nights the page draws (24 artist, 2 vote, was 60 = 44KB); the Studio's own profile read adds `?t=` so the artist always sees their save | done | Decision `0042`; `sh test/run.sh` 44 files, 2116 assertions, 0 failed; `node --check` on every touched file | Working tree only, not pushed. Pass two (side-by-side storage reads, and the one-warm-door merge only if the first tap is still slow) waits on the founder's phone check |
| UX-012 | Speed pass two: HTML routes and static files kept at the edge (`Netlify-CDN-Cache-Control`, durable; HTML 60s + 10 min stale-while-revalidate, css/js 1h, img a day); every `json()` reply says `netlify-cdn-cache-control: no-store`; the Stripe SDK loads on demand in `_history.mjs`/`_requests.mjs` instead of on every cold start of profile/community/board/me; profile's six reads and venue's vouches batched | done | Decision `0048`; `sh test/run.sh` 44 files, 2128 assertions, 0 failed; production curl after the push (session note) | Founder's phone check: first open of a fan page, target 2–3s. Then decide the one-warm-door merge (0048 option B) |
| UX-013 | The one warm door: `/api/fan?what=profile|events|board|me|community|venue` serves the six public reads from one function; autocron pings `?what=warm` every fourth minute (~11k calls/month); artist, vote, community and venue pages ask the door; old addresses kept. Photos: `/api/img` already immutable at the edge per `?v=` — left as is | done | Decision `0049`; `test/fandoor.mjs` 22/22; `sh test/run.sh` 2157 assertions, 0 failed | Founder's phone check on a quiet site: first open, target 2–3s |
| UX-014 | Studio speed: `<head>` starts `/api/stage` and planGet before the page body arrives; autocron also pings `/api/stage` and `/api/admin` awake (3 pings / 4 min, ~32k calls/month); community venue reads batched (step 6 leftover). Hotfix in the same evening: Studio profile read had returned a Response since 16:10 (`5b4a531`), fixed `69ef030`, guarded in `test/copy.mjs` | done | Decision `0050`; `sh test/run.sh` 2191 assertions, 0 failed | Founder's phone check of the Studio; if still over 3s: lazy Stripe in `_billing`/`_connect` (0050 option B) |
| UX-015 | Airbnb batch two, foundation (I-27, I-16 recipe, I-29): four button tiers (`.btn-pri` commit · `.btn-ink` next step · `.btn-grey` secondary · `.btn-text` tertiary), one `.chip` recipe (hairline + `--sh-1`, `.on` = accent ring never a fill), the `.fab` pill — in `app.css` and again in `lock.css` for the Studios; `docs/design-system.md` (tokens quoted from `app.css:5–71` by line, tiers, chips, `.fab`, tab bar, sheets 0f0–0f2, locks, motion, copy voice, what `design-handoff/` got wrong); `design-handoff/README.md` banner (git-ignored) | done | Decision `0051`; `public/app.css:122–150`, `219–232`; `public/lock.css:64–96`; `test/limits.mjs:127–142` still finds `.lock>.lockin` in `lock.css` only; Venue Studio `.btn-text` measured `rgb(255,55,95)` light / `rgb(255,69,110)` dark after the `--accent` fallback (`scratchpad/btntext-check.mjs`); this agent's gate on the final tree: `sh test/run.sh` → `44 passed, 0 failed` (40 suites, 2,077 assertions, 0 ✗); `node tools/uicheck.mjs` 113 ✓ / 0 ✗; `node tools/sheetcheck.mjs` 12 ✓ / 0 ✗; `node tools/overview.mjs` → decision records 51; session `2026-09-12-airbnb-batch-two.md` | Live: `d980ec4` (PR #11); verified on myset.vip by content. `scratchpad/` paths in this batch's rows are the session scratchpad, not the repo. The Studios' own `.chip.on` still fills with the gradient (two page edits) |
| UX-016 | Airbnb batch two, artist page (I-05, I-06/07, I-09 + feedback): pinned `.fab` block centred as a column (Live now · N voting / Next: Thu 6:30pm · Venue / No shows listed) over the one action; proof carousel of server-vouched facts turning every 3.5 s, paused by a finger, off under reduced motion; share + toast; `/api/profile` gains `requests`, `rating{avg,count,nights}`, `topVoted`/`topPlayed`/`topPaid`, `comments`, `setlist`, `songs`, `posts`; index rows stamp the three tops and the heal back-fills once and no longer strips `title` | done | Decision `0043`; `public/artist.html:45`, `132–193`, `418–442`, `548–580`, `672–679`; `netlify/functions/profile.mjs:1–39`, `99–111`; `_history.mjs:16–53`, `289–298`, `336–338`, `393–408`; `test/community.mjs:254–311` (+25, 127 → 152 with the feedback round); `test/autoshow.mjs:304–334` (107/107 on the final tree; the history fix agent saw 98/3 with the heal gate reverted, 101/0 restored); `tools/uicheck.mjs` PROFILE PAGE 9/9 ✓ (`TAP TO VOTE THE SETLIST`, `emberGlow`, no `.livepill`, `Live in 1m 05s → 1m 03s`); `scratchpad/fix/fabcheck.mjs` against origin/main's `app.css` (pill `position:fixed`, `scrollWidth 375`, board requests bare `/api/board`); screenshots `scratchpad/shot-{live,next,none}-{light,dark}.png` read | Live: `d980ec4` (PR #11); verified on myset.vip by content. `topPaid` is null on every night until `admin.mjs` writes `paidVotes` into `logPlay` (one line, not in this batch). The page is live half-styled since `5b4a531` — it now carries its own `.fab`/`.btn-ink` copy |
| UX-017 | Airbnb batch two, vote page (I-09, I-11, I-12, I-15, I-16 + feedback a/b/c): Share shares `/<slug>/vote`; dock state line (`<div>`: Voting open / paused — votes stay / Last call · N / Doors open soon / Show's over — say thanks inside the three-hour window / Between shows after); "Review and continue" card, title wraps; Choose → Pay → Done only when a pack is bought for a song, Done reached by resuming the sheet after the return, gated on an affordable wallet; FLIP re-rank 240 ms skipped under a finger, a sheet, reduced motion; genre/sort chips on the shared recipe; "Not yet" `btn-text` with a page-local copy | done | `public/vote.html:43–48`, `201–217`, `290–299`, `360–385`, `677–730`, `925–975`, `1143–1180`, `1327–1387`, `1457–1580`; pins kept: `test/darkroom.mjs:146` verbatim, `test/copy.mjs:35`, `test/votesstay.mjs`, `test/finality.mjs` (`grep -c "come straight back\|votes come back\|Take .\{0,12\} back"` → 1); `tools/uicheck.mjs` `live: both buttons — Last call · 10 | More votes | …`, `and it is support, on its own — Between shows | Support Test | Show your appreciation`, `the header labels the free-vote allowance — 3/3 votes`; `tools/sheetcheck.mjs` 12/12 (`56px`, `translateX(-50%) translateY(40px)`, `500 vs 500`); `scratchpad/vc3.mjs` 33 ok × 2 themes (full round trip through a fake Stripe return, `cur=Done`), `scratchpad/resumegate.mjs` 26 ✓; screenshots `scratchpad/shots3/c3-done.png`, `ended-10min-crop.png`, `ended-5h-dark-crop.png`, `buy-pair.png`, `sheet-{light,dark}.png` read | Live: `d980ec4` (PR #11); verified on myset.vip by content. Live since `5b4a531` with "Not yet" through `.btn` only until the page copy ships. Real iOS share sheet and the real Stripe redirect not checked |
| UX-018 | Airbnb batch two, community page (I-17, I-09, I-16 + fixes): "Say something" dock; composer is one element moved into a sheet and parked back so a draft survives; vote.html's sheet mechanics (grab zone ≥56px, `body.sheeting`, drag); proof rail (All · ★ N · N posts · N clips · a chip per night) filtering the feed; `?show=` pre-selects the night; tip bar honest when card tips are off (`.off`, no glow, "Card tips aren't on yet — say hi at the show"); page-local `.chip` copy | done | `public/community.html:45–52`, `127–142`, `159`, `267–271`, `316–404`, `450–468`, `483–520`, `554–557`, `999`; `test/darkroom.mjs:150–154` literal ``if(!VENUE) h+=`<div class="tipbar"`` byte-identical; `tools/uicheck.mjs` COMMUNITY TIP 6/6 ✓ (`tipbar` is `#app.children[1]`, `emberGlow`, 2 orange `.checkoutcopy`); `scratchpad/comm.mjs` (`#app` children `owner, tipbar, sumrail, sect, post×6, fineprint`, night filter `2 of 6`, sheet `matrix(…,-187.5,0)`, `grab:56`, drag-close keeps the draft) light + dark; `tools/clipcheck.mjs` 15/16 — the ✗ reproduces at HEAD; screenshots `scratchpad/comm-{light,dark}-{page,sheet}.png` read | Live: `d980ec4` (PR #11); verified on myset.vip by content. Live since `5b4a531` with chips as bare text until the page copy ships. `sheetcheck.mjs` drives vote.html only; this page's drag was verified by the scratch harness |
| UX-019 | Airbnb batch two, venue page (I-25, I-26, I-09 + feedback): "What's here" six amenity icon rows (22 line icons keyed on `_venues.mjs`) + "Show all N" revealing in place; "Things to know" hours · offers · address row with "Directions ↗" (the top pill stays: directions once as a button); share; pitch card "What <venue> will see" with identity from the token (`/api/auth me`, never `localStorage`) and the profile via `/api/fan?what=profile`; cached-copy paint null-guarded, a null reply says "Couldn't load", only `unknown venue` says "No venue here"; `body.sheeting` (0f2); page-local `.btn-grey` + `.chip` copies | done | `public/venue.html:108–125`, `200`, `226–246`, `331–360`, `384–409`, `497–558`, `604–683`, `708–725`; `test/copy.mjs` `✓ Verified` / `Unverified listing` / `#intro` pins green; `scratchpad/vfix/check.mjs` ALL PASSED light + dark (22 `.ai`, 6 visible, unfold keeps the same `<img>` and `.rise` `finished`; `body.sheeting` `top:-640px`; server log `auth me token="tok" ; profile a="demo"` with a stale `myset.aslug` never requested); `scratchpad/venue-harness-f1.mjs` 7/7 (not-found cases); screenshots `scratchpad/vfix/venue-{folded,unfolded}-light.png`, `venue-ttk-dark.png`, `venue-pitch-dark.png` read | Live: `d980ec4` (PR #11); verified on myset.vip by content. No `venue.html` fixture in `tools/uicheck.mjs`. Live since `5b4a531` with the reply pill as bare text until the page copy ships. Pre-existing, left: light-theme `.vbadge.no` paints green |
| UX-020 | Airbnb batch two, both Studios (I-19, I-20, I-21, I-23, I-26 + two feedback rounds): fixed bottom tab bar from `lock.css` (Artist: Live · Setlist · Gigs · Money · Menu; Venue: Page · What's on · Numbers · Merch · Menu), Menu sheets, `fitTabs`/`--headh` gone, toasts lifted; Today checklist + Tonight card with "Name this night"; first run in five steps keyed by account, cleared on sign-out, every step skippable, honest step 3/4/5 fallbacks; earnings bar chart, revenue donut, "Quick tips" sheet (Last call and tipper tips reworded to what the product does); pitch reply chip on the ring; Money-tab 375px overflow fixed; closed verify-intro no longer swallows taps | done | `public/studio.html` (CSS `84–163`, `220–256`; `1275–1330`; `2104–2231`; `2950–3110`; `3871–3970`; `3077–3088` — drifting while the other session edits the file); `public/venue-studio.html:54`, `88–91`, `337`, `1414–1456`; `test/structure.mjs:27–37`, `54–57` (`fitTabs` 0, `--headh` 0, `tabBar` 1 per Studio); `test/copy.mjs:119` `.tabbar button[data-tab-live]{color:#FF375F}`; `tools/uicheck.mjs` STUDIO all ✓ (`390/390` no overflow, `320/320` gig actions, verify notice centred `422/422`, `Now Playing shine cannot swallow real taps`); `scratchpad/barcheck.mjs` 46 ✓ / 0 ✗ (bar `rgb(0,0,0)` dark / `rgb(255,255,255)` light, h52, z55 < `.bg` 60, ring `inset 0 0 0 1.5px` accent, Live `rgb(255,55,95)`); `scratchpad/frcheck.mjs` 21/21 per theme; `scratchpad/money-shots.mjs` 24 ✓ at 375 and 320; screenshots `scratchpad/bar/*.png`, `scratchpad/fr-step1-{light,dark}.png`, `scratchpad/money/*.png` read | **Urgent**: production `studio.html` (HEAD `ce50252`, since `5b4a531` 16:29) calls `drawFirstRun()` at the tail of every `render()` and does not define it — `git show HEAD:public/studio.html \| grep -c 'function drawFirstRun'` → 0. Repaired live in `d980ec4` (PR #11): `function drawFirstRun` → 1 on myset.vip/studio. Ring colour is `--accent-2` (orange, the "View on map" outline) — the founder asked for orange and pink already marks Live (`lock.css:133`). Venue Studio Numbers tab throws on `{ok:false}` (pre-existing, ~`venue-studio.html:1250`) |
| UX-021 | Airbnb batch two, front door / about / directory (I-28, I-16 + feedback): six clay icons on `/about` in place of emoji; directory toggles and the header links on `.chip` (`.signin` keeps accent ink + the orange ring); address off the gig card (search still matches it); "View next week's events" → "the week after" with week heads, "Nothing listed for next week yet.", a retry note, 28-day stop, inclusive-window arithmetic matched to the server | done | `public/about.html:212`, `503–605`; `public/artists.html:22`, `57–59`; `public/index.html:70–78`, `171–186`, `563–692`; `scratchpad/weekcheck.mjs` 45 passed, 0 failed (fake server honouring `days=`; `days=14/21/28` requested in turn, `NEXT WEEK · 19 Sep – 25 Sep`, empty / gate / fail cases, `scrollWidth<=innerWidth` both themes); `tools/uicheck.mjs` DEFAULT THEME 1/1, PUBLIC THEME 8/8, ARTIST DIRECTORY 11/11 (`three header controls fit a 320px phone — 390/390`); `test/copy.mjs` home/directory strings green; screenshots `scratchpad/week/week-{before,after}-light.png`, `week-after-dark.png`, `head-{light,dark}.png` read | Live: `d980ec4` (PR #11); verified on myset.vip by content. The button is gated on the server echoing `window`, and `events.mjs` now does: the city feed honours `days=` 7–28 and echoes `window` + `horizon` (decision 0052; `test/featured.mjs` § THE WINDOW, 75 ✓) — same PR. Pre-existing: `about.html` scrolls 379px at 375 (nav "Get your page" pill, same at HEAD) |
| UX-022 | Product Dev 2: Studio — tab bar facelift (graphite-into-black slab, hairline, diagonal sheen, raised orange-ringed selected pill, light-theme twin; shared by both Studios), Menu → Profile subtext "Manage your profile page + merch", "What the room said" moved from Money to Profile beneath merch, Setlist tab song list 12px under the genre chips, Live checklist "Select setlist" with All songs a real **Use** choice (`myset.allsongs`), plan names in every Studio string | done | `public/lock.css:92–162`, `public/studio.html` (CSS) + `public/studio.js` (script, since decision 0053 — restamped with `tools/stamp.mjs`); harness `check-studio.mjs` 42 ✓ in dark and light incl. Gigs / Money / Profile / Live / Setlist painting without a pageerror; `test/studiocode.mjs`, `test/feedback.mjs` green | Gigs/Money/Profile were confirmed painting in the harness with fixture data; the signed-in Studio on myset.vip is the founder's own check (Claude-in-Chrome was unreachable this session) |
| UX-023 | Product Dev 2: artist page — proof strip loses the rating card, gains Most voted / Most played / Also voted for / Most paid for between the comments, and turns as a slow (~28px/s) seamless marquee that a finger still scrolls; pinned card wears the orange ring, orange state heading and a true-red ring on the countdown; RSVP under the date of every Upcoming-shows row; addresses gone from the rows and the next-show card | done | `public/artist.html:154–166`, `181–196`, `429–480`, the row template; harness `check-artist.mjs` 44 ✓ + `check-artist-fix.mjs` 11 ✓ (wrap, pause/resume, tap, revert on refusal, collapsed rows); uicheck 113 ✓ | `docs/design-system.md` § 6 and § 8 updated the same session |
| UX-024 | Product Dev 2: front door — RSVP under the time and date of every city-feed row (artist gigs by slug, venue events by `v:`), state kept across the search box's re-renders, one delegated listener, no `stopPropagation` (the city picker's click-outside stays alive); 12px above and below the two chips, the install card included | done | `public/index.html` `.rsvp`, `bindRsvp`, `.feedsearch`, `.a2hs`; harness `check-front.mjs` 33 ✓ (gaps 12/12 in both themes, button above the row link, pressed state survives typing, failed POST reverts) | Rows never printed the address — confirmed, nothing to remove there |
| UX-025 | Product Dev 2: RSVP server — `POST /api/rsvp?a=\|v=` toggles a hashed fan in `rsvp_<owner>` (decision `0056`, INVARIANT 0fl): the occurrence must exist, not have ended, and be within `HORIZON_DAYS` (120, shared with the gig list); 3-day prune, 5,000-fan cap; every `/api/events` row carries `rsvp`; erased with the account (`keysFor` / `keysForVenue`) | done | `netlify/functions/_rsvp.mjs`, `rsvp.mjs`, `events.mjs`, `_account.mjs`, `_venueaccount.mjs`; `test/rsvp.mjs` (in `test/run.sh`, all ✓); `docs/processes/the-gig/01` step f21 | Puzzle: changelog entries for 0055 and 0056 and step f21 in section 41964 are the tandem follow-up |
| UX-026 | Product Dev 2: plans renamed **Hobbyist / Bar Star / Rock Star** — labels in `_plan.mjs`, every refusal in `admin.mjs`/`auth.mjs`, Stripe product names for first creation in `_billing.mjs`, the Studio's TIER_COPY, lock captions, Menu line, past-due copy; ids `free`/`plus`/`pro` untouched; venue plans untouched | done | Decision `0055`; `test/copy.mjs` 42 ✓, `test/limits.mjs`, `test/tenancy.mjs`, `test/verification.mjs`, `test/community.mjs` follow the labels; overview §2.1 regenerated (`node tools/overview.mjs --check` → current); ACCOUNTS.md, `docs/processes/money/03` | PER-011: the two products already created in Stripe keep their old names until renamed in the dashboard |
| UX-027 | Product Dev 3: RSVP at the right edge of every show row — front door (`.rsvpcol` between the headline and the chevron, above the row link; the 38px pin under the time and date) and artist page (`.rsvpcol` after the headline; the pin under the day; a Tickets link a line under the day, not a fourth column); "<n> going" centred under the pill on both; the hero line "Find a show, open the setlist at the venue, and **vote on your favorite songs**." (orange bold); the finder's 1.5px orange ring with 700-weight `--ink` headings; "View on map" a 17px folded-map line icon centred on its label | done | `public/index.html` (CSS + `row()`), `public/artist.html` (CSS + `row()`); `test/copy.mjs` (the map-button assertion); harness `check-front-door.mjs` (column x 277–332 between `.m` and `.chev`, count centred 0.01px, pin 8px under the date, tap RSVPs without navigating, chips 51.75/51.75, icon centred 0.00px), `check-artist-rsvp-lightbox.mjs` 116 ✓, `check-artist-tix.mjs` 32 ✓ (no spill at 390/320); uicheck 113 ✓ | `docs/design-system.md` § 5 (the RSVP pill), § 12 (the icon); f21 sheet + Puzzle step 370394 reworded. Not added: RSVP on the venue page's own events |
| UX-028 | Product Dev 3: artist page — every photo up top (cover, portrait, the three small ones) opens a lightbox on tap (full-size source, `grid-template-rows:100%` so a tall photo fits, `data-nopull`, focus kept without scrolling, Escape); the Community button's ring and words `--accent-2` | done | `public/artist.html` `.lb`, `lbOpen`/`lbClose`, `zoom()`; harness `check-artist-fixes.mjs` 19 ✓ (844×390 cover fits, 400×1600 photo fits, drag on the sheet does not reload, close keeps `scrollY`) | The stock placeholder is never offered as "Enlarge photo". Real iPhone not checked |
| UX-029 | Product Dev 3: vote page — `@keyframes edgeGlow` is the Studio's box-shadow recipe, byte for byte (was `filter: drop-shadow`), `.list.votelist` `position:relative` so its glow paints over the card after it as the Studio's does; `test/structure.mjs` refuses any difference between the two blocks | done | `public/vote.html:227`, `242–256`; `test/structure.mjs` (the last check); harness `check-vote-glow.mjs` (both windows' computed `box-shadow` at the 3200ms peak carry 18/44/72px, `filter` none), `vote-glow-compare-*.png` beside a Studio stand-in | `docs/design-system.md` § 0 and § 10. iOS Safari on the scroller itself not measured |
| UX-030 | Product Dev 3: Studio — a filed night's name is tappable on the Money tab (rows and the opened night): `prompt` → `POST /api/history {action:'rename'}` → `renameShow` writes `title` + `titleByHand` on the detail and `title` on the row, `archiveShow` keeps both through a richer re-archive, 100 chars, owner-only by key; the search reads titles; *All songs* has a Use button in the setlists sheet unless it is the chosen one, and a done checklist step paints its title `--good`; "+ Add a song" and "⇪ Import songs" centred | done | Decision `0057`, INVARIANT 0fm; `netlify/functions/_history.mjs`, `history.mjs`; `public/studio.js` (`histName`, `renameNight`, `openLists`, the dispatcher + keydown), `public/studio.html` (`.todo.done .t`, `.setlist-tools .big`; restamped `?v=4d1ab49f`); `test/histname.mjs` 27 ✓ in `test/run.sh`; harness `check-studio-dev3-fix.mjs` 56 ✓ at 390/320 both themes | Puzzle: changelog 1642, step 370395 (h12) in section 41977; sheet `docs/processes/money/07` |
| UX-031 | Product Dev 4: the show row is one centred card on three pages — columns on the row's middle line, each centring its words; the thin *Directions* pill (the map sheet's `.hmapgo`, 11px/750, words only) under the time and date in place of the pin circle; RSVP column at the edge with the count centred; the artist page's next-show card the same (time block left, words centred, RSVP right, the wide Directions beneath); the map sheet's own cards untouched | done | `public/index.html` (`.gig`, `.dirs`, `dirBtn`), `public/artist.html` (`.gigrow`, `.dirs.thin`, `dirBtn`), `public/venue.html` (`.r`, `dirPill`); harness `check-dev4-public.mjs` 28 ✓ (columns' centres within 0.0px of the row's on all three pages, pill under the date, count centred 0.01px, `.dirs.ico`/pin svg gone) | `docs/design-system.md` § 5 rewritten as "the show row is one card on three pages". Also from the founder's look at the local preview: "6 seconds" on the install card, the featured-count paragraph off the Setlist tab, and the cover's `onerror` now drops `srcset` before falling back (the pre-existing loop; `check-dev4-tonight.mjs` 10 ✓) |
| UX-032 | Product Dev 4: RSVP on the venue page's *What's on* and *Next up* rows — `venue.mjs` rows carry `eventId` and `rsvp` (read in the same hop as the owner's events, for the venue's own events and the artists' gigs at it); the page keys them as the front door does (`v:<venueSlug>` / artist slug), same `myset.fan` / `myset.rsvp` memory, optimistic paint, above the row link | done | `netlify/functions/venue.mjs`, `public/venue.html` (`rsvpCol`, `rsvpTap`); `test/rsvp.mjs` 48 ✓ (the venue page's own event carries its id and the count; an artist's gig at the venue carries its id and a zero); harness: tap → pressed, "1 going", remembered, no navigation, the button above the row link | f21 sheet + Puzzle step 370394 reworded |
| UX-033 | Product Dev 4: front door — both chips under the finder wear the 1.5px orange ring, the feed's search field a hairline, placeholder "Search by name, venue, date, time…"; the map sheet drags closed from its head (grab bar 44×5, `touch-action:none`, follows the finger, closes past 80px, springs back short of it, `data-nopull` on the modal) | done | `public/index.html` (`.artistsearch`, `.feedsearch .inp`, `.hmaphead`, the pointer handlers); harness: chips' computed shadows carry the ring and the field does not, a 140px drag reads `translateY(120px)` mid-way and closes, a 40px drag springs back | `docs/design-system.md` § 10 (the sheet), § 12 (the chips) |
| PL-001 | Plan rules moved with the founder's cards (decision `0060`): `audience` 50 / 300 / 2,000; `library` 100 / 2,000 / 2,000 with `libraryCap()` read on every add; `moderate` gates `postHide` and `postDelete` is no longer an action; `reports` gates the filed nights (`history.mjs` answers a free plan `locked:'plus'` + `nights`, a night's detail 402); INVARIANT 0dy rewritten; overview §2.1 regenerated | done | `netlify/functions/_plan.mjs`, `admin.mjs`, `history.mjs`; `test/limits.mjs` (+13: 101-song import keeps 100, the 101st is 402 naming the plan), `test/clips.mjs`, `test/community.mjs`, `test/histname.mjs`, `test/roomsize.mjs` | The card says "10 shows for free"; the code still counts ten per calendar month — for the founder to confirm |
| UX-036 | Batch six, the pages: `--accent-ink` = `#FF5650` both themes (every accent word), `--accent-soft` pink-orange, `--grad-soft` on every *Directions* pill (front door, map sheet, artist, venue, artists pages); `.tonightbox` one gradient ring around the whole night; "View on MAP"; tip/pack copy and heading; the proof carousel's 1 s resume that restarts on every scroll while stopped; `theme.js` replaces the theme-color meta (the page's own root background) and nudges the scroll; the plan cards in the founder's words; the Money tab's locked *Data reports* card; Hide greyed on free, no Delete; *Comped until* never on the founder's account | done | `public/app.css`, `theme.js`, `index.html`, `artist.html`, `venue.html`, `artists.html`, `vote.html`, `community.html`, `studio.html`, `venue-studio.html`, `studio.js` (restamped); `tools/uicheck.mjs`, `test/copy.mjs`, `test/decline.mjs` | The status-bar band fix is a best reading of iOS, unverified until the founder's phone |
| PL-002 | Second pass on the plan rules (decision `0061`): free `featured` unlimited (every song in the library is live to the room; `toggleSong` / the 0be refusal / `shapeLimits` null all kept), plus `library` 200; `_profile.mjs` normalises `bandcamp` and `gofundme` links with `hostOk` (wildcard `*.bandcamp.com`); INVARIANTS 0be/0ca annotated; overview §2.1 regenerated | done | `netlify/functions/_plan.mjs`, `_profile.mjs`; `test/limits.mjs` (Bar Star 200, Rock Star the most), `test/split.mjs`, `test/darkroom.mjs` (link order) | Rock Star's 2,000-song library is not named on its card |
| UX-041 | A second *Save profile* button at the top of the Profile tab; and the show record follows the profile name on save (`artist` + `artistFirst`) — found the evening 0062 shipped: the founder's show record carries its own `artist`, so `getShow` never read the registry and the board's `artistFirst` stayed empty after his save | done | PR #29; suite exit 0, `test/sheets.mjs` three new assertions (show record + registry follow the profile), uicheck 113 ✓, headless `studio11.mjs` (`b11-studio-name.png`) | The founder must Save profile once more for the voting page to carry *Perry* |
| UX-042 | The current song has to be ended before another can start: `startSong()` gates every ▶ in the Studio, opening a small centred *End current song?* window (red *Yes, end it* sends the same play; pink-orange-bordered *Keep playing* closes it); *End current song* is a light-red fill (`rgba(255,59,48,.14)`) with red text (decision `0063`) | done | Suite exit 0 / 2,318 ✓ (`test/darkroom.mjs` three new assertions), uicheck 117 ✓ (four new: colours, the window opens on ▶, Keep playing closes it), headless `studio12.mjs` (`b12-endnow.png`, `b12-ask.png`) | Client rule only — the server still files the playing song on `play` |
| UX-040 | The artist page's floating bar: while live the button is a true-red fill reading *ENTER NOW TO VOTE* (`.act.now`); the countdown's *view setlist* is pink-orange (`.vs`) with the parentheses in the pill's own ink | done | PR #28; suite 44/0, uicheck 113 ✓ (asserts the new label), headless `shots10.mjs` (live: bg rgb(255,59,48); next: `.vs` rgb(255,86,80)) | — |
| UX-043 / PL-003 | The business dashboard for Bar Star and Rock Star: `biz_<aid>` (a record per night keyed `eventId@date` or showId, a default per run), `bizGet`/`bizSave`/`bizPrefs` owner-only and 402 on free, caps `band`/`costs` 0/5/10 enforced against growth, `public/biz.js` + `public/studio-money.js` loaded on demand for a paid owner, the Money tab redesigned (hero, tiles, charts, *Your evening*, Shows, Merch, Band, Costs), the editor sheet, *The business side* on the gig form, `/report`; the filed night's `key` + `source`; a hand start near a gig stamped and a far one cleared; the unconnected-owner ledger fix (artist + venue) — decision 0065, INVARIANT 0fn | done | **Live as `81a48f3` (PR #31, 2026-09-13)**; suite 2,577 ✓ / 0 ✗ (45 files; `test/biz.mjs` 140, `test/bizmath.mjs` 165, `test/cost.mjs` 6 / 7 reads); uicheck 171 ✓ / 0 ✗; sheetcheck 12 ✓; report check 35 ✓; live `/biz.js` and `/studio-money.js` byte-identical to the tree; five-lens review, 8 majors confirmed and fixed with red-then-green tests; session `2026-09-13-business-dashboard.md` | **Not pushed** — the founder's go to open the PR; nothing seen on the live site |
| UX-047 | The Book button and the inbox (the founder, 2026-09-14; decision `0074`, INVARIANT 0fw): **Book** under the artist page's numbers opens a sheet (Booking · Collab · Press · Other, name, email, phone / venue / date, the message, a honeypot); `POST /api/messages {action:'send'}` decides every refusal inside the index CAS (`inbox_<aid>`: blocked / honeypot → the same `{ok,id,k}` and nothing stored; a repeat → the existing conversation; 3 a day per phone and per address → 429; 60 per network, 200 per artist, 3 links → Spam) then writes `msg_<aid>_<tid>` with a 32-hex token; the booker reads and replies by `POST get / reply` (the token never in a URL) at `/<slug>#m=<tid>.<k>`, shown with Copy on the Sent screen and remembered on the phone; the Studio's **Messages** (Menu, a bubble; a dot on the Menu tab): Requests · General · Business · Casual · Spam, All · Unread · Read, the conversation with a reply (Requests → General), Move to…, Mark unread, Report (Spam + flagged + the founding account told) and Block (email hash + salted device hash; the owner's), every question through `ask()`; the badge read quietly after boot, on return, on the Menu, after actions — never on the poll, never on a timer; push + `sendMail` best-effort, letters budgeted (20 a day to the artist, one receipt per address per day, never a stranger's words), `mail:emailReady()` on the replies so the copy is honest; `msgList` carries the caps; `keysFor`/export/delete cover inbox, archive (`inboxarch_`, 300-row spill) and every conversation; CAPABILITY `community` for reading and answering, OWNER_ONLY for block/report | done | **Live as `a69154f`** (PR #51, 2026-09-14; the branch rebased onto `e774481`, where another session's 0073 and 0fv had landed — hence 0074/0075 and 0fw). Preview #51 looked at 375 px on the founder's own page (the four numbers, Book · Merch · Community, the calendar with 60 dotted nights over four months, the Book sheet); live by content 50 s after the merge (`/perryidyll` carries `bookBtn`, `/studio.js` `msgPeek` and `tourCard`, `/api/messages` answers 405 to a GET, the profile payload carries `tour` and `msgMax`). Gate on the rebased tree: `sh test/run.sh` → exit 0, **3,214 ✓ / 0 ✗** (`test/messages.mjs` 91 ✓); uicheck 244 ✓; sheetcheck 39 ✓; overview current; the headless walks in the session note `2026-09-14-book-merch-messages-calendar-tour.md`. Docs: 0074, INVARIANT 0fw, overview §2.1 (a new table), `docs/processes/artist-lifecycle/06-booking-messages.md` + the fans' night f23, Puzzle section 42087 (steps 370856–370865, `Testing`) + changelog 1665 | Puzzle 42087's ten steps and 370866/370867 flipped to `Live`; changelog 1665/1666 carry the SHA. The founder's: send himself a booking request from his page and answer it from the Studio on his phone. Not checked: a real letter (Resend; PER-004), a real push (VAPID), a real phone's Book sheet. Open: the receipt letter (kept, capped), members getting the letter, the two new numbers |
| UX-048 | The artist page's hero and calendar (the founder, 2026-09-14; decision `0075`): *Songs* gone; Joined · Shows · Fans · Votes cast on one line sized to their words; `compact()` reads the whole number under 10,000 then `10k` / `10.1k` … `1m`; Book · Merch (only with a shop) · Community beneath, all `class="ps cta"`; **View calendar** under the rows opens the Studio's month in a floating window (`#win`, the page frozen behind it — 0f2), dots by the night's own date string, months from the earlier of the phone's and the first listed night's to the last with a show, one lazy read of the 120-day gig list; a tapped night's card with Directions / Tickets / RSVP and *‹ Calendar* | done | Live as `a69154f` with UX-047. uicheck 244 ✓ (links above the fold at 568 px; *Watch & listen* last); headless at 375: the hero, the calendar with disabled arrows, a night's card with *On now* / RSVP / 12 going, `body.sheeting` with `top:-537px`; the preview on the founder's page showed his residency's 60 nights dotted across four months. Docs: 0075, the fans' night f22, Puzzle 370866 (`Live`), changelog 1666 | Shipped with UX-047. Not checked: a real phone; the founder's page (his gig list) |
| UX-049 | The tour poster (the founder, 2026-09-14; decision `0075`): Studio → Gigs → **Tour dates poster** — a PNG/JPEG shrunk on the phone or a PDF under 3 MB (`decodeTourFile`: signature-checked, the `tour` slot as its own family, never in `SLOTS`), *Where fans get tickets* (https), Replace / Remove (asked); `tourSet` / `tourClear` (CAPABILITY `profile`), `planGet.tour` carries the byte caps; `profile.tour {url,type,link}` on the public payload (`normTour`: same-origin `/api/img?…&s=tour` only); `/api/img` serves a PDF as `application/pdf` inline, immutable; the page's **View tour dates** under the *Upcoming shows* headline (drawn for a poster even with no nights) opens the poster in the window with *Download* top-right (the file itself, a new tab) and *Grab your tickets ↗* when a link was set; `keysFor` names the slot; the export carries `tour`. Fixed in passing: `loadProf` read the founding page's profile for every other artist (no `?a=`) — the Profile tab drew the founder's fields; it now carries the artist's slug and *View your page ↗* points at their page | done | Live as `a69154f` with UX-047. `test/messages.mjs` (the poster half: PNG in, link alone, `javascript:` dropped, PDF replaces with a link, served inline as PDF, the public payload, oversize / fake / SVG refused, `photoUpload` never takes the slot, crew refused, clear, `keysFor`). Headless: the Gigs card with and without a poster, the page's button and window | Shipped with UX-047. The founder's: upload his own poster. Not checked: a PDF inside the window on a real iPhone; whether iOS saves or opens the Download; a real 3 MB upload against the function body cap |
| UX-046 | A count per size (the founder, 2026-09-14, under decision `0064`): `variants[].stock` (blank = infinite while in stock), fields under the size chips in both Studios that follow the sizes as typed, the item's own count hidden while there are sizes; `pickVariant` refuses a size at zero and *Only N left in M* past the count; `takeStock` takes the order's size first; `merchSoldOut` when every size is gone; the shop strikes a size at zero, says *Only N left in L* once picked and caps the stepper | done | Live as `c4df3f0` (PR #42, rebased onto `be8d60c`; preview checked by content; live `/studio.js` carries `mcVarQtyRows`, the shop `fewLeft(m,SIZE)`). Suite 3,010 ✓ / 0 ✗; uicheck 239 ✓; sheetcheck 39 ✓; overview current; headless walk on the mock (editor fields live, saved per size; the sheet's per-size strike, count line and cap). Docs: 0064 §second look, INVARIANT 0fo, the merch-orders sheet, Puzzle 369901/369903/369906, session `2026-09-14-the-shop-second-look.md` | The founder's: put a number under a size on a real item |
| UX-045 | The shop's second look (the founder, 2026-09-14; under decision `0064`): up to five pictures per item in the item's own slots (`imgs`, `img` = the first; `_merchpix.mjs`, `MERCH_SLOT` `_1..4`), the editor opening on *Photos* for new and saved items (a new item's staged and uploaded after the first Save — no second step), the gallery under the price in the sheet with dots and the lightbox; a `stock` count brought down per paid order (`takeStock` in `redeemSession`), refused past what is left (*Only N left*, 409) and at zero (sold out without flipping `out`), *Only N left* on the shop at ≤ 5, the stepper capped; ↑ ↓ re-ordering (`merchMove`), the first item on top of a larger community trio; the brand ring on *Browse the shop*; gradient step numerals; no pause control on the strips; pink-orange *Paid to {name} through Stripe*; "shipping/shipped" for "postage/posted" everywhere a person reads, Stripe's rate named *Shipping*; `keysFor` covers the four extra slots and `wishes_` | done | Live as `ab0a05e` (PR #39, squash of `57cc7b6`+`85f6c67`, off `8444c6e`); preview #39 looked at 375 px with the founder's two real items (gallery, *Ask at the show* — his card payments are off); live by content: `/perryidyll/shop` carries `galleryBlock`, `/studio.js` carries `merchMove`, the community page the ring rule. `sh test/run.sh` → exit 0, 2,996 ✓ / 0 ✗ (unit +12, community +28, copy +9); uicheck 239 ✓; sheetcheck 39 ✓; overview current. Headless walk on `node tools/mock.mjs`: gallery swipe + lightbox; Add an item with a staged picture through the crop and back; ↑ ↓; the Venue Studio editor. Docs: 0064 §second look, INVARIANT 0fo, overview §2.1 (+3 rows), the merch-orders sheet, Puzzle 369901/369903/369904/369906 + changelog 1648; session `2026-09-14-the-shop-second-look.md` | The founder's: add more pictures and a count to a real item; switch card payments on when ready |
| UX-044 | The shop: merch on its own page at `/<slug>/shop` and `/v/<slug>/shop` (`public/shop.html`, reads `/api/fan?what=community` with no `fan=`); the community page's rail retired for a ringed finder-sized `.shopcard` under the name and above the tip button, only with ≥ 1 item (decision 0064 supersedes 2026-09-07's order; INVARIANT 0f8 amended); static two-column grid, the room's starred posts and *More from {First}* drifting via the artist page's `drift()` (28 px/s, ⏸ remembered, static under reduced motion), Picks after the grid behind `?picks=1` with six or more items; product sheet with size chips, quantity, sticky Pay (never disabled), one history entry per item; sizes `variants[{label,out}]`, `out` separate from `on`, `post` as a Stripe `shipping_options` fixed rate with the fee on the line (`metadata.base`), `product_data.images`; `pickupCode` (4 chars, no 0/O/1/I, 5th on a clash, inside the CAS) on the order, returned by `/api/confirm`; `from:'shop'` server-built return (5b names three pages; new 0fo; 0cm, 0cn rewritten); `cleanOwnerId` keeps `v_`; Studio *Merch store* Menu row (items, sizes, prices, orders with the code first), caps from `merchList`, 480 px WebP photos; Venue Studio mirrors it. The founder's five same-day tweaks after inspecting `node tools/mock.mjs`: **Make a request** (`_wishes.mjs`, `wishes_<owner>`, `action:'wish'`, `wishList`/`wishDone`, *Requests from the shop* in both Studios; INVARIANT 0fp), the *Community ↗* crumb, earlier orders at the foot without a date, *shipped to your door — options vary per item*, *What fans are saying* | done | Worktree `merch/shop-page` off `34215b8`. Gate on the final bytes, rebased onto `15beca1`: `sh test/run.sh` → exit 0, 45 suites, 2,867 ✓ / 0 ✗ (`test/community.mjs` +18 for the request path, `test/copy.mjs` +6 pins); `node tools/uicheck.mjs` → 228 ✓; `node tools/sheetcheck.mjs` → 39 ✓; `node tools/overview.mjs --tests` → `stamped: 2867 assertions, 0 failing`, `--check` → current; `shop.html` 81,965 B raw / 26,739 B gzip. Headless Chrome on the mock at 375: card → shop → sheet → Pay → receipt `K7PQ`; *Make a request* → *Sent to Demo* → the Studio's *Requests from the shop · 3 new* → Done → *2 new*; the Venue Studio's section; both themes; zero console errors. Docs: decision `0064`, INVARIANTS 5b/0f8/0cm/0cn/0fo, overview §2.1 (11 pages, the shop table), `docs/design-system.md` §0/§2/§3, `docs/processes/money/04-merch-orders.md` + `the-gig/04` b05, Puzzle sections 41974 (steps 370633 and 370744 *Make a request*, steps `Testing`) / 41967 + changelog entry 1648 (re-bodied for the same-day look), session `2026-09-13-the-shop-sizes-postage-and-the-pickup-code.md` | Live as `406f3f4` (PR #33, squash of `e85cae8`+`d4d47ec`, rebased onto `15beca1`; preview #33 looked at 375 px: the empty shop with *Make a request*, the community page with no card and no Buy — the founder's page lists no merch yet). Puzzle 41974 steps flipped to `Live`. Then flip the Puzzle steps in 41974 from `Testing` to `Live`. **Decision-number collision:** the `crm/artist-dashboard` worktree also has a `0064` (the business dashboard) — whichever merges second renumbers to 0064. Not run against real Stripe (`shipping_options` + address + images on a Connect direct charge); not on a phone; wallets in Instagram's webview unmeasured |
| UX-039 | The name in two parts — *First name or band name* + optional *Last name* on the Profile tab; every "Support <First>" on the vote and community pages, and the artist page's *Watch more from <First>*, reads the band name (`firstOf()`, `artistFirst` on board/stage, the registry follows the profile — decision 0062). With more than three videos the ticked one (*Top video on your page*, `mediaHero`) is the hero and the rest drift in a strip; a tap lifts a card into the hero slot and autoplays | done | PR #27; suite 2,312 ✓ / 0 ✗, uicheck 113 ✓, headless `shots9.mjs` (hero, strip, tap-to-lift) and `studio9.mjs` (fields prefilled, hero tick); `docs/decisions/0062` | The registry write on profile save is verified by the suite, not against production; the Studio's *Listen & follow* section label unchanged |
| UX-038 | The artist page's link strip: heading *Listen, follow, & support*; the pills drift left to right, the mirror of the proof strip (`drift(id,dir)` runs both strips, per-strip handles in `DRIFT{}`, ghost copies `aria-hidden`) | done | PR #25, squash `466a907`, verified on myset.vip by content 30 s after the merge; suite 44/0, uicheck 113 ✓, headless drift check (+42 px proof / −42 px links over 1.5 s) | The Studio's Profile section label still says *Listen & follow* — not asked for |
| UX-037 | Batch seven, the pages: the plan cards in the founder's words again (all-in-1 artist page, in-app merch store, the community page line, "and more" for the core features, 100 / 200 songs "showcase all of them", the business dashboard on Rock Star tagged `(coming soon)` per decision 0005, `.tier .soon` muted italic); Bandcamp + GoFundMe fields on the Profile tab and pills on the artist page (`ICON`/`LABEL`); `theme.js` `toggle()` reloads an installed page (`navigator.standalone`) 60 ms after saving the theme | done | `public/studio.js` (restamped), `studio.html`, `artist.html`, `theme.js`; headless shots `b7-studio-plans.png`, `b7-studio-profile.png`, `b7-artist-links-light.png` | The standalone reload is unverified until the founder's phone; "sell of your merch" was written as "sell your merch" |
| UX-035 | The pink-orange pass: every thin ring paints `--grad` (masked `::after` where the ring was an inset shadow; padding-box/border-box double background on `.queue`, `.list.votelist`, `.askbox`, `.search input`, `.gig.feat`, `.scroll-shell`, `.tier`); `--accent-2` → `#FF5650` in `app.css` and both Studios; `edgeGlow` and the tab pill's tint/glow at `rgba(255,86,80,…)`; `.sect` headings on the artist and venue pages `--accent-2`; the page menu two bars, stroke and pop icons `--accent-2` | done | `public/app.css`, `lock.css`, `index.html`, `artist.html`, `vote.html`, `venue.html`, `community.html`, `artists.html`, `studio.html`, `venue-studio.html`; `tools/uicheck.mjs` + `test/decline.mjs` assert the gradient; decision `0059` | `docs/design-system.md` § 2 carries both ring recipes |
| UX-034 | Product Dev 4: both Studios' tab bar, second pass — 1px top line (`rgba(255,255,255,.2)` dark / `rgba(0,0,0,.14)` light), 56px bar with 3px top padding so the content sits lower, 13px labels, gap 4px, pill inset 4px, `overflow:visible` on the icons (the Live arcs reach the viewBox edge); the Gigs tab's *Coming up* rows are `.gigcard`s — date + words on one line, Feature / Edit / ✕ on a full-width row beneath, like the Setlist tab's song cards | done | `public/lock.css` (`.tabbar`), `public/studio.html` (`.gigcard`, `.gighead`), `public/studio.js` (the row template; restamped `?v=d6907d62`); harness `check-dev4-studio.mjs` (bar 56 / 1px / 13px / 3px / `overflow:visible`, five labels fit at 320 and 390, the words column 258px at 390 and 188 at 320 where it was ~120, buttons beneath) | `docs/design-system.md` § 7. Body padding (72px + inset) still clears the 56px bar |
| DOC-001 | Puzzle mapping: plan, conventions, workspace skeleton (Phase 0) | done | `docs/processes/PUZZLE-MAPPING-PLAN.md`, `CONVENTIONS.md`; Puzzle workspace 13099 tabs/teams/tools created 2026-09-12 | Founder's standing rule (2026-09-12): the Puzzle maps are updated in tandem with the overview, same session |
| DOC-002 | Puzzle: The gig tab (Phase 1) | done | seven sheets `docs/processes/the-gig/`; sections 41964–41970, 106 steps; decisions as changelog entries | Awaiting the founder's browser check of the canvas (agents cannot sign in) |
| DOC-003 | Puzzle: Money tab (Phase 2) | done | seven sheets `docs/processes/money/`; sections 41971–41977, 77 steps, beside the pre-existing Stripe section 41595 | Same |
| DOC-005 | Puzzle: drift fix for `425fb0a` (renumbering, email+code sign-in, Merch→Profile, wrap-up/countdown, finality demoted) | done | nine steps, three section notes, seven changelog entries; sheets edited; session `2026-09-12-puzzle-mapping-phase-3-accounts-and-venues.md` | — |
| DOC-006 | Puzzle: Accounts & venues (Phase 3) | done | nine sheets under `artist-lifecycle/`, `venue-lifecycle/`, `onboarding/`; sections 41978–41986, 84 steps; 31 of 39 decision records in the changelog | Onboarding section is `Draft` where P4 items are missing. `VERIFYING-A-VENUE.md` vouch count corrected 2026-09-12 to cite `MIN_VOUCHES` |
| DOC-007 | Puzzle: Engineering OS (Phase 4) | done | six sheets `docs/processes/engineering-os/`; sections 41987–41992, 51 steps, RACI on every step; changelog 1615 (Part 7 system), 1616 (the Puzzle rule), 1617 (decision 0040); read back through `list_sections` 2026-09-12 | Starting → making → testing → committing → keeping true → ending. Found: *Efficient Mode* is defined only inline in `AGENTS.md`. Awaiting the founder's browser check |
| DOC-008 | Puzzle: Reliability & security (Phase 5) | done | six sheets `docs/processes/reliability-and-security/`; sections 41993–41998, 50 steps, RACI on every step; 18 steps `Draft` — every ledger open-risk row now has a step (P3-008 → *Check payments actually work*, PER-003 → *Turn on 2FA*, restore → *Backup and restore* b03–b07, alerting → *Page somebody*, PER-008 → *Add the Connected-accounts webhook secret*); decisions 0029/0030/0013/0008 linked; changelog 1618 (the 2026-09-05 security pass, which had no decision record); read back through `list_sections` 2026-09-12 (9/8/9/7/7/10) | Found: *Backup and restore* is entirely unbuilt — no store export exists, only per-account `exportArtist`; whether the 2026-09-08 replacement `STRIPE_SECRET_KEY` is a **restricted** key is not recorded anywhere (j06, Draft). Later the same day: y02 Live (ruleset), b03/b06 Live (`backup.py`, decision 0046), d04/d05 rewritten for the PR flow, s09 added; changelog 1620–1622. Next: Community & media (tab 39038) |
| DOC-009 | Puzzle: Community & media (Phase 6) | done | five sheets `docs/processes/community-and-media/`; sections 41999–42003, 45 steps, RACI on every step; three `Draft` (a real-phone clip measurement, a platform moderation queue, aligned chords); changelog 1624–1627 (decisions 0011, 0020, 0022, 0033) linked; read back through `list_sections` 2026-09-12 (9/12/8/8/8) | Found: "where a night happened" (§3.8) was already on the Money tab (h04), so it is a `go_to`, not a section. Next: Marketing & growth (tab 39041) |
| DOC-010 | Puzzle: Marketing & growth (Phase 7) | done | seven sheets `docs/processes/marketing-and-growth/`; sections 42004–42010, 81 steps, RACI on every step (links 326540–326662); **76 of 81 are `Draft`** because the strategy has not started running — `Live` only for the product mechanisms the founder uses as-is (paste/CSV import, the printable codes, the artist's night) and the two evidence rules that already bind every document; read back through `list_sections` 2026-09-12 (10/12/11/13/11/12/12) | Found: the strategy's "founding-artist badge" collides with the code's "founding artist" (the platform owner's account, `_plan.mjs`); none of §12.4's attribution (free-text "how did you find MySet?", referral codes) exists in the product; the workspace has no content-agent role, so the panel steps sit on *Coding agent*. Next: Admin & finance (tab 39042) |
| DOC-011 | Puzzle: Admin & finance (Phase 8) | done | four sheets `docs/processes/admin-and-finance/`; sections 42011–42014, 42 steps (23 `Live`, 19 `Draft`), RACI on every step (links 326663–326719); read back through `list_sections` 2026-09-12 (10/10/9/13). Deliberately thin: the code side of every money and secrets item already lives on the Money and Reliability tabs, so this tab is the *account-holder's* view — aliases and go_tos, plus the founder's rhythm (session start, before a gig, weekly, monthly) | Found: `moneymodel.mjs` falls back to a default passcode that sits in the now-public repo — `FINMODEL_CODE` should be set (PER-010); the registrar is a fifth account outside the 2FA list (PER-003); refunds, disputes and tax have no procedure and none has been needed. Next: the six decision records without a changelog entry, then the AGENTS.md session-end line |
| DOC-012 | Puzzle: remaining changelog entries and the session-end line (Phases 9–10) | done | changelog 1628–1633 (decisions 0015, 0025 superseded, 0027, 0036, 0042, 0043) and 1634–1635 (the other session's 0048, 0049 — live via PRs #4 and #6), each linked to the steps it governs; **all 49 decision records now have an entry**; f03/l03/l04 notes updated for the one warm door (0049) in sheet and Puzzle; `AGENTS.md` § Before ending a session and this checklist carry the "Does Puzzle need updating?" line; `CONVENTIONS.md` note closed | The ten-phase plan is complete. Standing: the founder's browser pass over every tab (an agent cannot sign in); the tandem rule from here on |
| DOC-013 | Puzzle: the data model (plan phase 8) | done | `docs/processes/DATA-MODEL.md`; 48 entities (4843–4890) with 156 attributes (46027–46182) on the Tools canvas — 44 Netlify Blobs key families, the R2 clip object, three Stripe objects; the first attribute of every entity linked to the steps that read or write it (163 links, 299616–299778); shapes read from the owning modules (`_lib.mjs`, `_account.mjs`, `_history.mjs`, `_events.mjs`, `_auth.mjs`, `_billing.mjs`, `_connect.mjs`, …) | Found: overview §5.2 omits eight families the code enumerates (`sess_`, `log_`, `rec_`, `pkeys_`, `ledidx_`, `feats_`, `bugs_`, `err_<hour>`) — the overview is in the other session's tree; and the registry already records a signup source label and a referral slug (`src`, `refSlug`, `referredBy`), which corrects Marketing x04 |
| DOC-014 | Puzzle: whole-map audit | done | 51 sheets ↔ 51 sections: every sheet's row count equals its section's step count (533 + the 16 pre-existing Connect steps = 549); every step read back on The gig, Money, Artist lifecycle and Onboarding has a section, a type, a status, an executor and at least one role (the other six tabs were linked in this session with counts matched at load); 134 conditionals read back with their arrows — eight that were rules stated as questions retyped `task` in sheet and Puzzle; all 49 decisions have a changelog entry | By convention a refusal ends a path rather than being a step, so ~40 conditionals show one drawn branch; standing-rule steps are unconnected on purpose. Both are choices the founder can reverse; recorded in the session note |
| DIA-001 | **Artist Diaries — phase 1** (decision `0085`, INVARIANT 0gb): a dedicated `/<slug>/diary` page; each page a written story optionally tied to one song from the library (its lyrics open beside the story through `/api/lyrics`), or to no song at all; every plan gets it, sized by plan — free 3, Bar Star 10, Rock Star 40 (`PLANS.*.diary`, `diaryCap`); no embeds, no native audio in v1 | done | Built 2026-09-18 on `docs/artist-diaries-phase1-scope` off `9f46b8b`: `_diary.mjs` (the document `diary_<aid>`, the cap inside the CAS, `diaryList/Save/Remove/Move` on `/api/admin`), `diary.mjs` on the warm door (`what=diary`, edge-shared 15 s), `public/diary.html`, the Studio's Diary screen under Menu with a phone-side draft, the Diary door on the artist page when `profile.diary > 0`, the Diary card under the Merch card on the community page (same card, same fan — `community.mjs` ships `diary` + `diaryPeek`), a cover photo per page (`DIARY_SLOT`, `diaryPhoto`/`diaryPhotoClear`, cropped wide in the Studio, heading the story on the page and filling the card's fan), the route above the catch-all, `diary`/`diaries` reserved, export + `keysFor` (covers too) + versions; `sh test/run.sh` green with `test/diaries.mjs` (89); walked on `tools/localhost.mjs` at phone width (a page written through the editor with a cover through the real crop sheet, the card, the page, the lyrics, the cover cleared) | **Live as `a87ff85` (PR #78), 2026-09-18** — the preview walked at phone width (the founder's empty diary, no door, no card, the community and profile reads at 0), production verified by content (studio stamp `8e4c3019`, `/perryidyll/diary` served). Next: the founder writes the first real page on his own account and says how the editor and the cover crop felt on the phone |

### Phase 3 — scale preparation

| ID | Work item | Status | Evidence | Blocker / next action |
| --- | --- | --- | --- | --- |
| P3-001 | **Shared-board split** — one cached board, tiny per-fan endpoint | done | Live 2026-09-11, `c3d0a4d` + the rung correction: `_board.mjs`, `board.mjs`, `me.mjs`, `show.mjs` (composes from the same builders), `vote.mjs` (`at`), `_lib.mjs` (`getShow(aid,{withName:false})`, `pollFloorFor` middle rung 10 s), `public/vote.html` (`load`, `mergeBoard`, `applyCast`, `SHOWN`/`CAST_AT`), `test/split.mjs` 80/80, `test/cost.mjs` board ≤15 / personal ≤3, `tools/loadsim.py --ceiling`, INVARIANTS 0fh–0fk, decision `0034`, fresh-context review with seven fixes taken; real-browser check of both halves failing in turn; the durable minimum and the per-node 3 s copy measured on drafts and production | Read the personal call's billed duration off the function log after the first busy room (estimated ~50 ms, never measured) |
| P3-002 | Open line to the room (Cloudflare Durable Objects) | in_progress | 2026-09-11: **measured, not modelled** — throwaway `cloudflare/probe/` deployed to `workers.dev` (own `package.json`; the repo root still has two dependencies). One object held 4,000 sockets and every broadcast reached the last phone in ≤ 552 ms; hibernates within 15–20 s even with 1,000 sockets attached; wake ≈ 10 ms empty, 40–70 ms with 1,000 attached; 3,000 opened at once, 0 failures; decision-`0030` bucket verified over the socket. DO analytics: 24,473 billed requests for 12,202 connections (**two per connection**, open + close), 13.65 s billable duration all afternoon, 35,839 free outbound messages, $0 on Workers Free; the "errors" column counts disconnects, not faults. Session `2026-09-11-open-line-probe.md`, decision `0035` | **Trigger withdrawn by the user 2026-09-11** (reverses `0012`'s trigger). Next: design the production Worker on top of `/api/board` (`c3d0a4d`). DNS is at Netlify, not Cloudflare, so the line is cross-origin on `workers.dev` and `netlify.toml` `connect-src` gains one `wss://` host. Still unmeasured: the 1,000/s ceiling (needs more than one machine), iOS background Safari (9d12). The probe Worker is still deployed and public; `npm run delete` removes it |
| P3-003 | **Clip bytes onto Cloudflare R2** | done | `netlify/functions/_r2.mjs` (SigV4 by hand), `_video.mjs`, `vid.mjs` (302 to a presigned link); `test/r2-fake.mjs`; `test/clips.mjs` 163/163, full suite 2,004/2,004 (stamped); independent fresh-context review, findings fixed; decision `0033`; INVARIANT 0fd; session `2026-09-11-clips-to-r2.md` — **BUT the bucket has never taken a write**: `err_2026-09-11T18` (in the laptop backup) holds `r2.put 403` from the first upload after the deploy; on 2026-09-13 both live clips answer `200 video/mp4` from Blobs, not a `302` to R2; the mirror's first real pass (18:00Z) was 0 copied / 69 failed; with the same credentials a HEAD answers 404 (signature accepted) while PUT and DELETE answer 403 | **The token writes since 2026-09-15 ~04:30Z** — the founder gave it *Object Read & Write* (same key pair; the four variables untouched, last set 2026-09-11). Measured: the 04:20Z ring still had 129 refusals; the 04:40Z ring copied 7 with no new refusal (`copied:7, failed:129` — the 129 are the pass's earlier count, carried). The pass finishes ~05:00Z; because it carries failures the retry pass starts an hour later and is the first that can read `failed: 0`. **Done 2026-09-15:** the founder uploaded a fresh clip that afternoon and it played back on his phone — the first clip whose bytes live on R2. Left as they were: the two clips from before 09-15 still serve from Blobs (the fallback, by design) |
| P3-004 | **Error tracking that outlives the night** | done | Durable capped hourly error documents, guarded handlers, fan report endpoint and Studio reader; 42/42 focused assertions | Retained in MySet's existing blob store; no third-party telemetry account required |
| P3-005 | Fan-shard write ceiling actually measured | not_started | Derived from a measured 40ms *read*, never from a write | **Now the next wall after reads.** Hammer one shard before selling a room over 2,000 |
| P3-013 | **A voter's record weighs ~2 KB after eight casts — trim the cast receipts** | not_started | Measured 2026-09-11 on the real write path: 101 bytes present-only, 412 after one cast, 864 after three, 2,010 after eight; `casts[]` (up to 20 receipts with outcomes) is most of it. At that weight the busiest-case read wall is ~2,500; at the report's 152-byte record it is past 10,000 | Keep the idempotency (INVARIANT 15h) and shrink the receipt: id + a compact outcome, fewer kept, or receipts aged out after a few minutes. Re-run `tools/loadsim.py --ceiling` after |
| P3-014 | Cache the slug→id registry read in `publicArtist` for a minute | not_started | A slug artist's personal poll is 3 reads (the registry, the show, one shard); the founding page is 2. Flags already cache this way (`_flags.mjs`, 60 s) | A separate decision: a deleted account would stay reachable for up to a minute. Low value until there are many slug artists in big rooms |
| P3-015 | The front door's "near you" seed reads `/api/artists` (every listed artist's profile + calendar) once per first visit | not_started | Added 2026-09-12 with UX-009; one artist today | Put a coordinate per city into the city index (`?places=1`, already fetched) and pick the nearest city from that instead — one read, no directory scan |
| P3-008 | **A payments health check that runs off the hot path** | not_started | — | The 2026-09-08 outage was invisible until a fan tapped. A key can be valid-looking and dead |
| P3-012 | **Rate limit on casting** — token bucket on the fan record | done | 2026-09-11: `takeCastToken` in `_lib.mjs`, checked inside the vote mutation; 429 in plain words; `test/errlog.mjs` | 20 in a row, then 30 a minute (the user lowered the burst from 30 on 2026-09-11). Decision `0030`, INVARIANT 0fa |
| P3-011 | Same person, two browsers, not recognised as one | cancelled | — | Not fixable honestly — the fan id lives in the browser and browsers do not share it. IP matching is worse (the whole bar shares one). INVARIANT 0ae already stamps a per-show network hash so the artist can see one network making many phones. See master overview §1.12 |
| P3-009 | `/api/pay` throws when a caller sends no `attempt` | not_started | Reproduced live 2026-09-08: 502 `Stripe: Unknown arguments` | `opts` is `{}` for a platform-owner charge with no `attempt`, and stripe-node rejects an empty options object. **No fan is affected** — `vote.html` and `community.html` always send `attempt`. One-line fix: pass `opts` only when non-empty |
| P3-010 | Featured-show bidding | deferred | Fixed-price Featured shows are live; user chose to file bidding for later on 2026-09-10 | Revisit only after real demand shows the fixed-price, first-come inventory is regularly full |
| P3-006 | `AGENTS.md` + this ledger on every project, not just MySet | in_progress | MySet done 2026-09-08 | iOhm landing, Idyll Mastery, Idyll Enterprises, Clients |

### Perry's own list (not code — these need his hands)

| ID | Work item | Status | Blocker / next action |
| --- | --- | --- | --- |
| PER-001 | Add `charge.updated` to the Stripe webhook | done | Done 2026-09-12 in the dashboard, with `account.updated` (which the docs had listed as present; it was not). MySet destination `we_1UACXQKFtJidxE16IAYdxfYN`, seven events, verified by reading the destination back |
| PER-002 | Stop the double deploy; move Netlify to Pro | not_started | ~570 credits a month are the same change shipped twice |
| PER-003 | **2FA on Google, GitHub, Netlify, Stripe, Cloudflare and Porkbun** | not_started | (Branch protection — the other half of HARDENING §2 — done 2026-09-12 by an agent, decision 0045.) Twenty minutes, and the highest-value item in `SECURITY.md` — the ranked #1 threat is these accounts being phished, not the code |
| PER-004 | Set `AUTH_FROM` to a verified MySet sender | done | Done. `AUTH_FROM` = `MySet <hello@myset.vip>` in Netlify's production context (set 2026-09-10; the earlier "absent" reading was the CLI's `env:list` hiccup, not the variable). The founder confirmed 2026-09-15: a real sign-in code arrived from `hello@myset.vip`. Puzzle step 370273 `Live`. The same night the three `VAPID_*` variables were generated and set by an agent (production; the private key never printed) — they reach the functions on the next deploy |
| PER-005 | Press **"Name these from my calendar"** in Money → Past shows | not_started | Five filed nights are still named after one venue |
| PER-006 | Try the passkey on stage | not_started | Is Face ID actually faster mid-set? |
| PER-007 | Replace `STRIPE_SECRET_KEY` in Netlify — and make it a **restricted** key | done | 2026-09-08: replaced (full key); verified live: `POST /api/pay` returned 200 and a `cs_live_` url. **2026-09-15: restricted.** `myset-production` (`rk_live_`) created in the dashboard — an agent set the 13 permissions from the code's 25 calls (Checkout Sessions, Payment Intents, Charges & Refunds W/W incl. Connect; Balance R/R; Customers, Products, Subscriptions, Coupons, Customer Portal, Accounts, Account Links W; Invoices, Application Fees R), the founder pressed Create and pasted it into Netlify; rebuilt (`createSiteBuild` → `479dd91` live 05:26 UTC); the founder's real $1 tip succeeded on it (`pi_3UFp45KFtJidxE161ZPSzf21`, descriptor `IDYLL.MYSET`); error log empty for the hour; the two old full keys (`…HAsL`, `…y4mF`) deleted by the founder, the rolled `…8P29` expires by itself | Not checked: the Connect half of the key (an artist's *Start with Stripe*; a fan buying on an artist's page) — a real artist proves it; a missing permission lands in the error log with its name |
| PER-008 | A second Stripe event destination for **Connected accounts** | done | Destination created by the user 2026-09-12 (scope *Connected accounts*, same URL, events `account.updated`, `charge.updated`, `checkout.session.completed`, `checkout.session.async_payment_succeeded`); `STRIPE_CONNECT_WEBHOOK_SECRET` pasted into Netlify by the user; `webhook.mjs` tries each configured secret in turn — decision `0058`, `test/twosecrets.mjs` 9/9 against the real signature code | First real delivery not yet seen — read Event deliveries on the new destination after the first artist onboards |
| PER-009 | Rehearse the "lost everything" procedure on your own account | not_started | ACCOUNTS.md §6.4 — the live registry has no `byEmail` row for the founder's account, so attaching one is the rehearsal. Read half rehearsed 2026-09-12 |
| PER-010 | Set `FINMODEL_CODE` in Netlify | not_started | `moneymodel.mjs` (now `_passgate.mjs`) falls back to a default four-digit code that is in the source, and the repository is public (decision 0047) — so the courtesy lock is currently no lock. **Since decision `0095` the same door guards `/moneymodel/shows`, which shows every artist's takings per night** — the lock matters more. Two minutes: Netlify → environment variables → add `FINMODEL_CODE` (up to 12 characters) → redeploy. Admin & finance d09 |
| PER-011 | Rename the two artist products in Stripe → Products | cancelled | Read in the live dashboard 2026-09-12: the catalogue holds four Idyll Mastery products and **no MySet product at all** (live or test) — nobody has ever checked out a plan, so `ensurePrice` will create them as "MySet Bar Star" / "MySet Rock Star" on first use. Nothing to rename | — |
| PER-013 | Revisit Connect payout fees once artists connect | deferred | Stripe bills MySet $2 per connected account in any month it receives a payout, plus 0.25% + 25¢ per payout. $0 today. Since decision `0080` a Hobbyist is paid weekly, so the per-payout part is already trimmed on the free plan; the $2 is the same on any schedule. Todoist 2026-10-24 | Say "keep it", "weekly for everyone" or "pass the fee on" when the fee is real |
| PER-015 | Statement descriptor and Connect display name | done | Statement descriptor `IDYLL MASTERY` → **`IDYLL.MYSET`** (the founder, 2026-09-15; Stripe refused `MYSET.VIP` — must resemble the legal name or the business URL); on the $1 tip's charge. Connect → Onboarding interface → Business name → *Idyll Enterprises*. Not a Stripe problem: the founder's Revolut notification still said *Idyll Mastery* — the bank's cached merchant name, refreshes on its own | Check the next tip's bank notification in a couple of days |
| PER-014 | Acknowledge the Connect **platform profile** (negative-balance liability, ongoing seller compliance) | done | Both lines read *Completed September 14, 2026* on dashboard.stripe.com/settings/connect/platform-profile (read back 2026-09-15 after the founder clicked them). Until then `stripe.accounts.create` was refused for every artist with *"Please review the responsibilities of managing losses…"* — found on the first live press of *Start with Stripe*, step 4 of the first run; the founder's press went through straight after | Nothing — a one-time gate. Also set that day: Connect → Onboarding interface → Business name `Idyll Enterprises` (was *Idyll Mastery*) |
| PER-012 | Save the Customer Portal configuration in live mode | done | Saved 2026-09-12 from the dashboard (with the user's go-ahead): invoices on, card updates on, cancel **at end of period**, plan-switching off — the Studio owns plan changes. Before this Stripe refused every `billingPortal.sessions.create`, so "Manage billing" errored; there is no fallback path in the app | — |

---

## Metrics snapshot

| Metric | Target | Current | Last measured | Notes |
| --- | --- | --- | --- | --- |
| Cost of a 3-hour gig, 20 people | under 5¢ | **2.7¢** | 2026-09-05 | `tools/loadsim.py` |
| Cost of a 3-hour gig, 10,000 people | — | **$4.29** | 2026-09-05 | Was $36.40 before the signature split |
| Server cost as a share of revenue | under 10% | **5.6%** | 2026-09-05 | Does not move with scale — both sides scale together |
| Blob reads on the audience poll | board ≤ 15 per interval, personal ≤ 3 per phone | 14 / 2 | 2026-09-11 | `test/cost.mjs` fails the build above it. The board's reads happen once per interval for the whole room; the personal call reads one shard |
| Cost of a 3-hour gig, 20 people (after the split) | under 5¢ | **3.0–4.0¢** | 2026-09-11 | Up from 2.7¢: two requests a tick, and a cache hit is still a billed request. A range because under 10 s the copy is per edge node (3.0¢ if one node, 4.0¢ if none shared). Every bigger room costs less than before: 1,000 phones $0.56 (was $0.98), 10,000 $3.76 (was $4.29) |
| Honest room ceiling | 2,000 sold | **~2,500** busiest case at today's record weight (was ~700–1,000 before the split at that weight) | 2026-09-11 | `tools/loadsim.py --ceiling`. The earlier "~2,500" assumed 152-byte fan records; a voter's record is ~2 KB today (P3-013). At the lighter record the wall is past 10,000. Reads, not money, are the wall; the write wall (P3-005) is next |
| Test assertions | all passing | see §2.1 of the overview | 2026-09-08 | Stamped by `tools/overview.mjs --tests` |
| Real gigs run on MySet | — | **1** | 2026-08-30 | Treat every projection as a projection |
| Netlify egress per clip view, once on `main` | 0 for clips on R2 | — | 2026-09-11 | A 302 per view; a HEAD per clip per hour at the edge if the CDN caches the 302 (not verified), else per view; pre-R2 clips still bill until they leave (`0033`) |

---

## Verification log

| Date | Check | Result |
| --- | --- | --- |
| 2026-09-18 | The Money tab's pinch-out zoom: live Studio loaded under Playwright WebKit (iPhone 14 Pro profile) with the founder's Studio-code session read from the iOS Simulator's Safari storage; `document.documentElement.scrollWidth` | **441** on a 393px viewport; the three elements past the edge were the by-show chart's last labels ("Sep 15", "Sep 16", "Sep 17"); after the fix a fifteen-bar mock chart lays out at 393 (`test/../scratchpad/pw/wk.mjs`, not committed). Reproduced first on the iOS Simulator (iPhone 17 Pro, Safari) against myset.vip: the page sat at ~0.91 zoom with a strip down the right; the mock at the same size refused to zoom out (Safari opened the tab switcher instead) |
| 2026-09-18 | `sh test/run.sh` on `studio/money-tab-batch` | exit 0; every file's tail reads `0 failed` |
| 2026-09-15 | SSD mirror, after `mirror-to-ssd.sh` was extended: `Followthrough` 344 files = Mac; `Project Handoffs` 432 = Mac; `agent-config` 92 = Mac; `MySet` 601 = origin/main `7f13d84` (545 tracked) + 57 untracked files from the checkout; four Claude memory folders under `_claude-memory/`; the 9 colliding local edits in `MySet-uncommitted-local-edits.patch` (40 KB) | Stale `.git` (1,482 files) and `node_modules` removed from the SSD's MySet. The founder had not plugged the SSD in for a while; the newest copy before tonight was 3 days old |
| 2026-09-15 | Stripe dashboard: the founder reports the *Action required* banner is gone (the task done by him; content not recorded) | Run sheet item 30 closed on his word |
| 2026-09-15 | The restricted key, live: rebuild `479dd91` ready 05:26 UTC; `python3 tools/prod.py` healthy; `err_2026-09-15T04/05` absent (no errors); the founder's $1 tip `pi_3UFp45KFtJidxE161ZPSzf21` succeeded, statement descriptor `IDYLL.MYSET`, metadata `artist: perry-idyll · kind: tip`, paid in THB by Adaptive Pricing, net $0.67 | PER-007 closed |
| 2026-09-15 | PAY-001 in the worktree `pay/connect-refusal-message` off `ad880a5`, rebased on `abe9821`: `node --import ./test/register.mjs test/connect.mjs` → 69 ✓ / 0 ✗; `sh test/run.sh` → 49 files, no ✗ | The account is created with `{interval:'weekly', weekly_anchor:'monday'}` for a Hobbyist; upgrade → exactly one `accounts.update` to `daily`; second call a no-op; a lapsed `planUntil` + Money-tab refresh → back to Monday; a refused `accounts.create` returns `REFUSED`, no `stripe.com` in the text, no half-made account, the real message in `recentErrs` against the artist id |
| 2026-09-15 | Stripe dashboard, read back in the in-app browser after the founder's clicks | Platform profile: both acknowledgements *Completed September 14, 2026*; the founder reports *Start with Stripe* now hands him to Stripe's onboarding. Connect → Onboarding interface preview reads *Idyll Enterprises partners with Stripe…*. Statement descriptor still `IDYLL MASTERY` — the founder's call, not changed |
| 2026-09-14 | Email + password (ACC-001) in the worktree `auth/password-door` off `44f3a21`: `sh test/run.sh` → exit 0, 48 files, 3,047 ✓ / 0 ✗ (`test/password.mjs` 50 ✓: one sentence for every failure, weak ones refused with the reason, scrypt + salt on disk and no trace of the text, a session that works in the Studio, per-address lockout that leaves the code door alone, change with the current one or a fresh code and the other device of the address signed out, a Studio-code session told to use its email, clear, the delete list, the export, and the screen's words and colours in both Studios); `MYSET_PUBLIC=<wt>/public node tools/uicheck.mjs` 239 ✓ / 0 ✗; `sheetcheck` 39 ✓; headless Chrome at 390 px both themes — the heading, the box ring, *Sign in* and *Create account* compute to `rgb(255, 86, 80)`, the foot link to the muted grey and `underline`, the window opens on the foot link | green |
| 2026-09-14 | The data foundations (DAT-001) in the worktree `data/foundations` off `406f3f4`: `sh test/run.sh` → exit 0, 46 files, 2,932 ✓ / 0 ✗ (`test/foundations.mjs` 60 ✓: the chunked log spills at 20 and reads back 60 in order, a vote row carries its cast time, alpha played → 3 votes + play with two pseudonyms, refund/drop/reset each once, end files the standing vote + the tip + `end` and a second end files nothing, `?log=` 200 / 404, versions: none for a missing document, one per burst, write-once, the library on `addSong` not on `play`, 205 posts → 5 archived with photos findable, 203 notes → 3, the mirror copies then skips by etag, never a shard or a secret, cursor at 0 ms budget); `test/cost.mjs` a vote 5 reads / 2 writes, a post 8 reads — unchanged; `node tools/overview.mjs` current; `python3 tools/backup.py --restore … --store myset` → refused, exit 1; the real rehearsal into `rehearsal-20260914` on the production site: **209 of 209 keys restored and read back equal, 287.0 MB, 580 s**, then wiped (`deleted 209 of 209`, `blobs:list` empty) | green |
| 2026-09-13 | The dashboard's second pass (PR #35): `sh test/run.sh`, `MYSET_PUBLIC=<wt>/public node tools/uicheck.mjs`, the report's headless check (the *Rate settings* window: a switch redraws and sends one `bizPrefs`, the fee switch, *My cut* with nothing typed changing the words and not the figure, the card hidden in print), the localhost in both themes | exit 0, 0 ✗, 2,588 ✓ across 45 files; uicheck 173 ✓ / 0 ✗ / 0 page errors; report 44 ✓; `test/histname.mjs` proves Re-check fills `paidVotes` / `paidRequests` on a night filed without them. Not checked: the founder's real nights (his rows predate the counts until he taps *Re-check*) |
| 2026-09-13 | The shop batch (UX-044), integrator's final gate in the worktree (`merch/shop-page`): `sh test/run.sh`, `node tools/uicheck.mjs`, `node tools/sheetcheck.mjs`, `node tools/overview.mjs --tests` and `--check`; headless Chrome against a mock server on the real routes (`/demo/shop`, `/demo/community`, `/v/demo/shop`, the `?paid=` and `?cancelled=1` return trips) | `44 passed, 0 failed`, exit 0, 2,434 ✓ / 0 ✗ (`structure OK`, stamps `e77d3765` / `b1bbca48`); uicheck 148 ✓ / 0 ✗ (shop card at `children[1]`, tipbar at `children[2]`, `children[1]` again with `merch:[]`; the shop at 390 and 320, seven card states, reduced motion); sheetcheck 30 ✓ / 0 ✗ (12 lyrics + 18 shop); `stamped: 2434 assertions, 0 failing`, `overview is current`; `shop.html` 70,538 B raw / 23,399 B gzip; receipt `Paid — thank you | 2 × Tour tee (M) · $50 | K7PQ`. **Gate predates the *More from {First}* strip in the sheet — re-run on the final bytes.** Nothing on a phone; no real Stripe |
| 2026-09-14 | Every `confirm()` in both Studio scripts routed through the Studio's own `ask()` window (18 sites; `ask` now returns a promise): `sh test/run.sh`, `MYSET_PUBLIC=<wt>/public node tools/uicheck.mjs`, the localhost at 375 px | exit 0, 2,934 ✓ / 0 ✗ (48 files; `test/darkroom.mjs` asserts no `confirm(` is left); uicheck 244 ✓ / 0 page errors (*Clear setlist* → *Remove every song?*, the dim closes it); the Money editor's *Remove the numbers?* drawn above the sheet, *Keep them* left the record. Not checked: each site tapped by hand |
| 2026-09-13 | The business dashboard (UX-043 / PL-003), the tree that shipped, rebased on `f004926`: `sh test/run.sh`, `MYSET_PUBLIC=<wt>/public node tools/uicheck.mjs` (Money-tab pass: a Bar Star owner boots into the tab, logs a night, the calendar and the door), `tools/sheetcheck.mjs`, the report's headless check (mocked `bizGet`, print media, A4 PDF), `node tools/overview.mjs --check` | exit 0, 0 ✗, 2,577 ✓ across 45 files; uicheck 171 ✓ / 0 ✗ / 0 page errors; sheetcheck 12 ✓; report 35 ✓; overview current; both themes at 375 px on `tools/localhost.mjs` (the real functions on the in-memory store — Log tonight → Save wrote a real `bizSave`, the cut and fee toggles moved the rates as the maths says); after the merge `curl` of myset.vip `/biz.js` and `/studio-money.js` compared equal to the tree and `/studio` names `studio.js?v=ade6a231`. Not checked: a real phone's touch, reduced motion under emulation, the founder's real nights on the live tab |
| 2026-09-13 | Batch six (UX-036, PL-001), final tree in the worktree: `sh test/run.sh`, `tools/uicheck.mjs`, `tools/sheetcheck.mjs` (pointed at the worktree's `public/`), headless Chrome on the front door (Tonight ring, MAP, pills), artist page, vote page tip sheet, Studio Money tab and plan sheet on a synthetic free stage | exit 0, 0 ✗, 2,302 ✓ across 44 files (`limits.mjs` 93 ✓); uicheck 113 ✓ / 0 ✗; sheetcheck 12 ✓; the plan sheet reads exactly the founder's lines; the locked card says "18 nights filed and waiting". Not checked: a real iPhone (the status-bar band); the Studio's moderation row from a signed-in stage |
| 2026-09-13 | The pink-orange pass (UX-035), final tree in the worktree: `sh test/run.sh`, `tools/uicheck.mjs`, `tools/sheetcheck.mjs` (pointed at the worktree's `public/`), headless Chrome at 390×844 on the front door, artist page, vote page, Studio Live tab (fixtures from read-only GETs) | exit 0, 0 ✗, 2,290 ✓; uicheck 113 ✓ / 0 ✗ (the vote page's three rings and the Studio's Up next window read `linear-gradient(135deg, rgb(255, 55, 95)…`); sheetcheck 12 ✓; every ring runs pink-red → orange in the shots, the menu is two bars. Not checked: a real iPhone; the Studio's Setlist/Gigs tabs from a signed-in stage; the deploy preview |
| 2026-09-12 | Product Dev 4 (UX-031–UX-034), final tree in the worktree: `sh test/run.sh`, `tools/uicheck.mjs`, `tools/sheetcheck.mjs` (both pointed at the worktree's `public/`) | exit 0, 0 ✗, 2,290 ✓ across 44 files (`rsvp.mjs` 48 ✓); uicheck 113 ✓; sheetcheck 12 ✓; uicheck 113 ✓ / 0 ✗; sheetcheck 12 ✓ / 0 ✗. Nothing on a physical phone |
| 2026-09-12 | Product Dev 3 (UX-027–UX-030), final tree in the worktree: `sh test/run.sh`, `tools/uicheck.mjs`, `tools/sheetcheck.mjs` (both pointed at the worktree's `public/`) | exit 0, 0 ✗, 2,277 ✓ across 44 files (`histname.mjs` 27 ✓); uicheck 113 ✓ / 0 ✗; sheetcheck 12 ✓ / 0 ✗; decision records 57. Nothing on a physical phone |
| 2026-09-12 | Airbnb batch two (UX-015–UX-021), final tree: `sh test/run.sh`, `node tools/uicheck.mjs`, `node tools/sheetcheck.mjs`, `node tools/overview.mjs` | `44 passed, 0 failed` (40 suites, 2,077 assertions, 0 ✗); uicheck 113 ✓ / 0 ✗; sheetcheck 12 ✓ / 0 ✗ (`56px`, `translateX(-50%) translateY(40px)`, `500 vs 500`); decision records 51. Nothing on a physical phone; the fan pages are live half-styled since `5b4a531` |
| 2026-09-12 | Studio head-start + pings (UX-014): full suite | 2191 assertions, 0 failed |
| 2026-09-12 | The one warm door (UX-013): `test/fandoor.mjs` + full suite | 22/22; 2157 assertions, 0 failed. Production cold/warm timing of `/api/fan?what=warm` after the deploy in the session note |
| 2026-09-12 | Speed pass two (UX-012): `sh test/run.sh` after edge headers for HTML/static, no-store on every function reply, lazy Stripe, batched reads | 44 files, 2128 assertions, 0 failed. Production headers checked with curl after the push |
| 2026-09-12 | Whole-map audit (`list_sections` step counts vs sheet rows; `list_steps` per tab with roles and status; `list_steps type=conditional` with connections; `list_entities`) | 51/51 sections match their sheets (533 steps); 0 steps without a role or status on the four tabs read in full; 134 conditionals, 21 with no outgoing arrow (rules or terminals — 8 retyped), 1 unconnected step by design (a standing note); 48 entities, 156 attributes, 163 attribute→step links |
| 2026-09-12 | Changelog coverage (`list_changelog_entries`, all entries) | 49 decision-titled entries for 49 records in `docs/decisions/` (0001–0049), plus the three non-decision entries (Part 7 system, the tandem rule, the security pass) and the pre-existing Connect entry; nothing missing |
| 2026-09-12 | Admin & finance tab read-back (`list_sections` 39042 with step names, statuses, connections) | four sections 42011–42014: 10/10/9/13 = 42 steps; 23 `Live`, 19 `Draft`; six cross-section arrows created in the same call; tab was empty before the load |
| 2026-09-12 | Marketing & growth tab read-back (`list_sections` 39041 with step names, types, statuses, connections) | seven sections 42004–42010: 10/12/11/13/11/12/12 = 81 steps; 5 `Live`, 76 `Draft`; the three cross-section arrows (r13→a08, n06→r01, x07→m08) created by id (414419–414421); tab was empty before the load |
| 2026-09-12 | Community & media tab read-back (`list_sections` 39038) | five sections 41999–42003: 9/12/8/8/8 = 45 steps; cross-section arrow w08→g06 created by id |
| 2026-09-12 | `python3 tools/backup.py` against the live store (read-only, 8 parallel `blobs:get`) | 206 of 206 keys, ~287 MB (four pre-R2 clips are ~270 MB of it), verify: registry 1 artist / 1 slug / 0 email rows, `authsecret` present, checksums match, 3 stray non-JSON keys (`cas/t`, `cas2`, `debug/probe` — probe leftovers, harmless), *copy is whole*; `--if-stale` then answered *nothing to do* |
| 2026-09-12 | `gh api repos/perryidyll/myset/rules/branches/main` after creating the ruleset | `deletion`, `non_fast_forward`, `pull_request` (0 approvals, squash+merge) in force on `main`; **a refused push not yet attempted** |
| 2026-09-12 | `gh repo view --json visibility` | **public** — HARDENING.md §2 says private; recorded as an open risk, not changed |
| 2026-09-12 | Speed pass one (UX-011): `sh test/run.sh` after edge-caching profile/events/venue, `n=` on events, last-seen paint + placeholder frames on artist/venue/community | 44 files, 2116 assertions, 0 failed. Not checked in a browser or on a phone; the edge headers are not yet on production |
| 2026-09-12 | Production `a6d7566`, live content/API checks, production Chrome on `/` and `/artists?map=1`, and `python3 tools/prod.py` | Home View on map opens and closes without navigation; both maps render five markers at exact saved coordinate pairs; all 25 upcoming occurrences retain A–E letters; production health remained clean |
| 2026-09-12 | Reliability & security tab read-back (`list_sections` 39040, step counts and connections) | six sections 41993–41998: 9/8/9/7/7/10 = 50 steps; the two cross-section arrows (i06→u01, h10→y01) present |
| 2026-09-12 | Product Dev 2 efficiency follow-up: focused checks, `sh test/run.sh`, `node tools/uicheck.mjs`, visual phone screenshots, and draft `6aa508262ef6305007973b78` served-content/API checks | Every automated and rendered check passed; draft serves Support copy, compact controls, reflowed cards, larger glow and red seconds; live saved Ugly Duckling link resolves to the exact Taladkao Road address. Production unchanged |
| 2026-09-12 | Production `5cfa27d` + `51bf8a8`, full regression suite, rendered UI, live API/content checks and production Chrome | Profile timer advances second-by-second; The Ugly Duckling resolves to the canonical address and renders at `9.72658, 100.00396`; wrong-region and unverifiable map results produce no pin |
| 2026-09-12 | Stripe dashboard, MySet event destination read back after saving | Seven events listed, `charge.updated` and `account.updated` among them; *Events from: Your account*, no control to change it |
| 2026-09-12 | `netlify blobs:get myset artists` (read-only) | Registry has `v`, `rev`, `byId`, `bySlug`, `byEmail`; one artist, `byEmail` empty |
| 2026-09-12 | `VERIFYING-A-VENUE.md` vouch count | Said ten; `_verify.mjs` `MIN_VOUCHES` changed in `190e2b4` (2026-09-02). Page now cites the constant |
| 2026-09-12 | Puzzle read-back of the Engineering OS tab (39039): `list_sections` with step counts and connections | 8/9/9/7/10/8 steps as written; every labelled branch present; three cross-section arrows added and confirmed by id |
| 2026-09-12 | Puzzle read-backs: `list_steps` on *Casting a vote* (41965), *Stripe events arriving* (41972), *Getting verified* (41984) against their sheets | 15/15, 11/11, 9/9 steps; every branch labelled; statuses as written. Canvas layout not checked (login wall) |
| 2026-09-11 | R2 clip store, worktree `claude/r2-clips`: `node tools/overview.mjs --tests`; `test/clips.mjs`; the signer against Amazon's published SigV4 example; a fresh-context review that re-derived SigV4 from undici's wire bytes | Full suite 2,004/2,004; clips 163/163 (77 new); header signature `f0e8bdb8…`, canonical hash `7344ae5b…`, presigned `aeeed9bb…`, PUT payload hash `44ce7dd6…` all match; wire re-derivation matched for PUT/HEAD/DELETE. Not run against the real bucket |
| 2026-09-11 | Production `c3d0a4d`, then two draft deploys (`6aa3e51586…`, `6aa3e61262…`) with a header switch, then production again | New page and both endpoints live and correct within 40 s; the durable cache bypasses every lifetime under 10 s in every spelling and hits at 10 and 60; a 3 s copy is an edge hit on the same connection (`ttl=2`) and a miss from the next node — on the draft and on production |
| 2026-09-11 | `sh test/run.sh` on a clean checkout of `c3d0a4d` before the push | 2,017 assertions, 0 failures, every section green (`test/split.mjs` 80/80; `test/cost.mjs` board 14 reads, personal 2, legacy 14) |
| 2026-09-12 | Ten-shows, composer fold, page-switch work: `test/syntax.mjs`, `test/structure.mjs`, `test/copy.mjs`, `test/limits.mjs`, `test/community.mjs`, `test/tenancy.mjs` | 37 + 84 + 116 + 80 passed, 0 failed; syntax and structure OK. Full suite not run; no browser check |
| 2026-09-11 | Parity of the old `show.mjs` (`git show HEAD:…`) against the new one, same in-memory store, 15 scenarios | 0 differ, ignoring key order and the additive `at`/`freeCredits` (the reviewer's script, re-run by hand) |
| 2026-09-11 | Real browser (390px) against the real handlers over HTTP: vote through the sheet; `/api/me` returning 500; a fresh phone casting with `/api/me` down; `/api/board` returning 500 | Tally and "Your vote" stable across polls; board still updates with others' votes while the personal call is down; a cast with the personal call down is shown as mine with the server's `remaining`; the page keeps its board while the board call is down; no page errors |
| 2026-09-11 | Live, read-only: `/api/img` on myset.vip three times (same `durable` directive, same `/api/*` rewrite as the new board) | 2nd and 3rd: `cache-status: "Netlify Durable"; hit`, `age` present — the mechanism the board relies on works in production. The 3 s TTL itself is unverified until the deploy |
| 2026-09-11 | `tools/loadsim.py --ceiling` | Reproduces the room-ceiling report's column to the decimal at 152 B/fan; at today's 2 KB record the busiest-case wall is ~700–1,000 before the split and ~2,500 after |
| 2026-09-11 | `node tools/uicheck.mjs` | 104 rendered checks pass; the one failure (the artists-page static-map pin) is the other session's in-progress interactive-map work, not this change |
| 2026-09-11 | Production commit `e74292b`, deploy and live map/copy checks | Home actions and 2% Pro card are live; map config is enabled and a production-referrer request returns a real PNG; full suite 1,926/1,926 |
| 2026-09-11 | Draft `6aa3c42c072c9b9fc5bea38a`, focused copy/render checks and full suite | View-on-map is left of artist search at 320px; deep-link and consistent 2% Pro copy are served; 1,882 assertions, zero failures; production unchanged |
| 2026-09-11 | Production commit `d1a6531` and live HTTP/config checks | Combined discovery/miscellaneous batch deployed successfully at `myset.vip`; map remains correctly hidden while provider config reports disabled |
| 2026-09-11 | Draft `6aa3bcce9fd9709b3a45eb9c`, verified-only directory tests, rendered first-Settings notice and full gate | Served Studio has 2% Pro copy, verified-only explanation and no placeholder testimonials; directory endpoint returns only the qualifying profile; notice colors/persistence and mobile fit pass; 1,882 assertions, zero failures; production unchanged |
| 2026-09-11 | Draft `6aa3b8e8eedd9a5d7e4e5391`, focused directory/fee/theme checks, rendered UI, `sh test/run.sh`, and overview stamp | Signed requires both label fields; Pro is 2%; first visit and loading screens are light while saved dark remains dark; map remains safely hidden without its key; 1,876 assertions, zero failures; production unchanged |
| 2026-09-11 | Draft `6aa3ab8413b61425b33b9fcb`, focused map/copy checks, `node tools/uicheck.mjs`, `sh test/run.sh`, and overview stamp | Map popup, exact pin, existing directions link, filters, failure fallback and 320px fit pass; 1,872 assertions, zero failures; served config is honestly disabled without a key; production unchanged |
| 2026-09-11 | Production commit `02169fa` | Artist directory card/filter batch is live; served HTML and real computed directory data verified |
| 2026-09-11 | Draft `6aa3a5cdaf109164f02e0a0d`, focused directory/community/copy checks, `node tools/uicheck.mjs`, and `sh test/run.sh` | New directory tags, filters, calculated ratings/show counts, separate name/one-liner spacing and 320px fit all pass; full suite has zero failures; production unchanged |
| 2026-09-10 | Production commit `33d7429`, Netlify deploy `6aa2bf1185cb7d000874ed54` | Deploy ready; live Studio serves gig-level Feature and orange promotion bullets, and live directory/theme assets match the approved batch |
| 2026-09-10 | `sh test/run.sh` + `node tools/overview.mjs --tests` | 1,863 assertions, 0 failures, including verified-sender readiness, provider rejection, both signup doors and Featured-show settlement |
| 2026-09-10 | `node tools/uicheck.mjs` | Gig-level Feature/Edit/cancel order, 320px fit, scoped three-bullet orange promotion sheet and all existing rendered checks pass |
| 2026-09-10 | Netlify draft `6aa2af9b3a81cc0488b757c9` | Updated Studio, auth function, global theme and directory assets served; no preview write path exercised; production unchanged |
| 2026-09-10 | `sh test/run.sh` + `node tools/overview.mjs --tests` | 1,852 assertions, 0 failures, including Featured shows settlement and public artist-directory privacy/filter behavior |
| 2026-09-10 | `node tools/uicheck.mjs` | Global Studio/public theme palettes, red Live label source, directory filters and 320px layout all green |
| 2026-09-10 | Netlify draft `6aa2a605960074ede64f0f3a` | Home, `/artists`, both Studios, voting page, theme asset and no-cache directory API all served successfully; production unchanged |
| 2026-09-09 | `node tools/overview.mjs --tests` + rendered UI/touch checks | 1,808 assertions, 0 failures; paid replay votes, conditional request authorization/capture, three-vote birthdays, mobile layout and sheet behavior all green |
| 2026-09-09 | `sh test/run.sh` | Every suite section passed with the new show controls, positive-vote guard and fee ladder |
| 2026-09-09 | `node tools/uicheck.mjs` | Phone-width unified replay list, five-vote minimum, tip copy, live controls, inactive Live state and no overflow all passed |
| 2026-09-09 | Netlify draft deploy `6aa0db9c42570c274fbd5c1e` | Requested audience and Studio content served from the preview; read-only `/api/show` succeeded; no preview write path exercised |
| 2026-09-09 | `sh test/run.sh` + `node tools/overview.mjs --tests` | Full suite green; overview stamped 1,762 assertions, 0 failures |
| 2026-09-09 | `node tools/uicheck.mjs` | Phone-width orange borders/copy, played-song state, repeat voting, paid-vote pills and decline action all rendered without horizontal overflow |
| 2026-09-09 | `node --import ./test/register.mjs test/decline.mjs` | Exact free/paid refunds, unrelated-song isolation, hidden-song lockout and retry idempotency all passed |
| 2026-09-09 | Netlify draft deploy `6aa04e5930bf024ac5cfb479` | One live profile CTA; retired voting-sheet sentence and duplicate Settings controls absent; existing room still returns 3/3, 3/$5 and 15/$20 |
| 2026-09-09 | `node tools/uicheck.mjs` | Phone-width vote, live profile and Settings layouts all passed, including exact section order and no duplicate CTA |
| 2026-09-09 | `sh test/run.sh` + `node tools/overview.mjs --tests` | 1,737 assertions, 0 failures |
| 2026-09-09 | Netlify draft deploy `6aa041448e6b1e3af49326be` | Existing stored room now returns 3/3 free votes, 3/$5 and 15/$20; Settings still allows paid customization |
| 2026-09-09 | `sh test/run.sh` | 1,735 assertions, 0 failures, including raw legacy-document migration and paid customization |
| 2026-09-08 | Netlify draft deploy `6aa03c5c4e14a64f2722db68` | Internal preview live; served content contains the new counter, empty-wallet purchase action and pack defaults |
| 2026-09-08 | `sh test/run.sh` | 1,721 assertions, 0 failures |
| 2026-09-08 | `node tools/uicheck.mjs` | Phone-width `3/3 votes` counter and empty-wallet “Buy more votes” sheet rendered; all checks passed |
| 2026-09-08 | `node tools/overview.mjs --check` | Current; voting defaults generated from source |
| 2026-09-08 | Live `POST /api/pay` after the key swap | 200, `cs_live_` session created — payments restored |
| 2026-09-08 | Git hooks fire on a throwaway branch | pre-commit refreshed and staged; post-commit wrote `PENDING.md` |
| 2026-09-07 | 40 QR lengths × {with mark, without}, decoded | 80/80 |
| 2026-09-07 | Production content check after deploy | new copy live, old copy absent |

---

## Deviations from plan

| Date | Planned | Actual | Reason | Follow-up |
| --- | --- | --- | --- | --- |
| 2026-09-05 | A hard cap on room size | A soft cap: the room slows and shortens, nobody is refused | The research said a mid-song lockout costs the artist relationship, which is the whole business | Decision `0006` |
| 2026-09-06 | Shrink clips on the phone | Upload them untouched, with a trimmer | Three rounds of silent-audio bugs, all caused by the size constraint | Decision `0011` |
| 2026-09-07 | Votes return between songs | A vote never comes back | Perry's rule, stated as final | Decision `0001` |
| 2026-09-09 | No setlist vote ever returns | An artist may explicitly decline an unplayed song and return its votes | The queue needs a fair correction when a song cannot be played | Decision `0016` |
| 2026-09-10 | Refresh the external SSD mirror | Mirror script stopped without writing because the SSD is not mounted | Preserve the local handoffs and rerun when the drive is connected | `~/Docs/Project Handoffs/mirror-to-ssd.sh` |
| 2026-09-11 | R2 after the shared-board split, on a ~100GB/month trigger | R2 built now, in a worktree, while the split is mid-edit in the main checkout | The user withdrew the trigger, opened the Cloudflare account and put the keys in Netlify; the main checkout held another session's uncommitted files, so a worktree off `main` kept the two apart | Merge `claude/r2-clips` when told; the two touch no common code, only `IMPLEMENTATION_STATUS.md` |
| 2026-09-11 | A custom domain in front of the bucket | A 302 from `/api/vid` to a presigned GET on the bucket's S3 endpoint | `myset.vip`'s DNS is on Netlify (NS1), not Cloudflare; `r2.dev` is rate-limited | Decision `0033`; revisit if the zone ever moves |

---

## Decision log

**The full log is `docs/decisions/`** — one file per decision, with every option that was
on the table, what it costs, what it makes harder and what would reverse it. Do not
duplicate it here. Index: `docs/decisions/README.md`.

| Date | Decision | Record |
| --- | --- | --- |
| 2026-09-15 | Payouts follow the plan: Hobbyist weekly on Monday (the weekend's money starts the week), paid plans daily; set at creation, re-checked at every plan write and account read | `0080` |
| 2026-09-14 | The sheet carries the marketing read (Signals) and the feature adoption (Features) per artist, tags every night real or test, and never writes a row it already holds; the first copy reached the founder's Drive as an .xlsx | `0072` |
| 2026-09-14 | The sheet measures itself every sync, warns at 60% and hands over to a new spreadsheet it makes and shares at 80%; a Studio-code session sets a password by naming one of the account's own addresses and proving it with a code | `0073` |
| 2026-09-14 | Current Show Stats is one snapshot of the app's own records, sliced by date in the browser; a night counts when it lines up with a published gig; daily as an artifact until it is myset.vip/metrics | `0071` |
| 2026-09-14 | The email address is the username and a password sits under it — per person, scrypt-hashed, swapped for a session; the six-digit code stays the front door for a new account and the whole of "forgot" | `0070` |
| 2026-09-14 | Every vote, play and dollar of a night is filed in an append-only event log as it happens — the archive keeps sums, the log keeps the night | `0066` |
| 2026-09-14 | The profile, the setlists, the calendar and the library keep a version before every overwrite | `0067` |
| 2026-09-14 | A capped list must have a complete sibling; the feed and the feedback list spill into archives; an id is never reused | `0068` |
| 2026-09-14 | Every document is copied nightly to R2 under the same key; the restore is rehearsed and refuses production by name | `0069` |
| 2026-09-13 | The paid plans get a business dashboard — one document per artist keyed by the calendar night, profit with MySet's fee named, my cut, the whole evening as the hours; the unconnected-owner ledger hole closed (0fn) | `0065` |
| 2026-09-13 | Hiding a post, the filed nights and a library past 100 songs are Bar Star; the free room is 50 | `0060` |
| 2026-09-13 | Every ring is the brand gradient and the audience colour is the logo's pink-orange | `0059` |
| 2026-09-12 | Every page composes from four button tiers, one chip recipe and a written design system (`docs/design-system.md`); a page that must ship ahead of its stylesheet carries a byte-identical copy of the recipe it uses | `0051` |
| 2026-09-12 | The public artist page carries the room's rating, comments and top songs as server-vouched proof | `0043` |
| 2026-09-12 | The repository stays public — the founder's call after it was found to be so | `0047` |
| 2026-09-12 | Artists are paid out daily, on purpose — the founder chose the reward loop over Stripe's payout fees | `0044` |
| 2026-09-12 | `main` is protected: nothing lands on it except through a pull request (ruleset 23031933, zero approvals, no bypass) | `0045` |
| 2026-09-12 | The datastore is copied weekly and before every gig, verified, kept ninety days — `tools/backup.py` | `0046` |
| 2026-09-11 | Find artists lists only effectively verified artist profiles | `0028` |
| 2026-09-11 | Light is the first-visit default and loading screens follow the active theme | `0027` |
| 2026-09-11 | The $20 artist plan takes a 2% transaction fee | `0026` |
| 2026-09-11 | Filtered event maps use a static image and existing exact directions links | `0025` |
| 2026-09-11 | Artist directory discovery facts derive from their existing source records; style alone is new profile data | `0024` |
| 2026-09-09 | An artist-declined unplayed song returns its votes | `0016` |
| 2026-09-08 | Three free votes; default packs are 3 for $5 and 15 for $20 | `0014` |
| 2026-09-07 | A vote never comes back | `0001` |
| 2026-09-07 | The countdown is a nudge, not a lock | `0010` |
| 2026-09-11 | Errors and bug reports live in the blob store, not a vendor | `0029` |
| 2026-09-11 | Casting is rate-limited by a token bucket on the fan record | `0030` |
| 2026-09-13 | Merch has its own page at `/<slug>/shop`, entered from a ringed card above the tip button; postage is a shipping rate off the cut, the pickup code is a lookup key, sold out is not off, and the owner id is kept whole | `0064` |
| 2026-09-11 | The audience poll is split into a shared, edge-cached board and a tiny personal call | `0034` |
| 2026-09-11 | A clip's bytes live on Cloudflare R2 and the phone is sent there by a signed link | `0033` |
| 2026-09-07 | The open line is not next | `0012` |
| 2026-09-06 | Clips go up as they are | `0011` |
| 2026-09-05 | A full room is never refused | `0006` |

---

## Open risks

| Risk | Impact | Mitigation | Status |
| --- | --- | --- | --- |
| ~~The R2 API token cannot write~~ (found 2026-09-13 by the mirror's first pass; the clips had been falling back to Blobs since 2026-09-11 with `r2.put 403` in the error log nobody read) | Was: no second home until fixed; every clip on Blobs | **Closed 2026-09-15.** The founder gave the token *Object Read & Write* ~04:30Z; the 04:40Z mirror ring copied 7; that afternoon a fresh clip uploaded, landed on R2 and played back on his phone (P3-003 `done`). The two clips from before stay on Blobs by design | closed |
| The repository is public on GitHub | Every document in it is readable by anyone | Decided 2026-09-12: **it stays public** (decision 0047). The writing rule is the mitigation — nothing goes in that the founder would not publish; no secret value ever has (INVARIANT 11) | Resolved 2026-09-12 — by decision |
| Google Maps key is absent or misrestricted | Event map cannot render | Button is hidden while absent; full show list/directions survive map failure; key is restricted to MySet production and the current QA preview, and to Static Maps, Maps JavaScript and Geocoding only | Resolved 2026-09-12 |
| **Perry's own accounts are phished** | Total — Google, GitHub, Netlify, Stripe, Cloudflare, Porkbun. No line of MySet's code is involved | 2FA on all six. PER-003 — the founder says "later" (2026-09-15) | **Active, unmitigated** |
| **The shared checkout `~/Docs/MySet` has diverged from `main`** | Other sessions' uncommitted work (19 modified tracked files, 57 untracked, 9 colliding with what shipped 12–15 Sep) sits on a 12 Sep base; a session that builds on it, or a `git add -A` there, ships stale files or conflicts. `node_modules` is a tracked self-referential symlink on `main` since `ffa6cfc` | Ship through worktrees off origin/main (the standing rule); the SSD mirror no longer reads the checkout as truth. Someone needs to merge or commit that work deliberately, and drop `node_modules` from the index | **Active** — found 2026-09-15 by the mirror |
| **Pre-attribution active votes cannot be split exactly by song** | A paid-vote pill or decline refund on a vote cast before this batch may not know its original source | New votes are exact; legacy decline uses a fan-favouring paid-first fallback | **Known transition risk** |
| Card payments down on a bad Stripe key | Nobody can buy votes or tip | Key replaced 2026-09-08; live checkout verified. The gap that let it go unnoticed is still open — P3-008 | Resolved 2026-09-08 |
| **Nothing detects a dead Stripe key** | `paymentsEnabled` checks the key EXISTS, never that it WORKS, so the room is shown a buy button that fails on tap | P3-008 | **Active, unmitigated** |
| **A bug cannot be diagnosed after the night** | A fan reports something, the logs are already gone | Durable hourly errors + fan reports with three-hour context | Resolved 2026-09-11 |
| Nobody has tested a restore | Data loss would be discovered during recovery | **A verified copy now exists** — `tools/backup.py`, first run 2026-09-12, rhythm in decision 0046 (session start if stale, before every gig). Restore into a throwaway site is still unrehearsed (Reliability & security b04/b07) | Copy: resolved 2026-09-12 · Restore: active |
| An artist loses inbox, codes and passkey at once | Locked out for good; the app offers no door by design | Founder procedure written, ACCOUNTS.md §6.4; PER-009 rehearses it | Written, unrehearsed |
| Connected-account Stripe events never arrive | Fee split, money-button flip and the payment safety net are silent for any artist but the founder | PER-008 | Mitigated 2026-09-12 (decision 0058) — first real delivery unseen |
| A payout schedule lags the plan | A lapsed paid artist is paid daily until they next open the Money tab; a new plan-writing code path that forgets `syncPayoutSchedule` leaves the old schedule | Decision 0080 § What this makes harder | Accepted — a day late, never wrong money |
| Nobody is alerted when something breaks | A silent failure runs until somebody notices | Errors are now kept, but nobody is paged — that is the half Sentry would add. `Draft` step *Page somebody* (370083) | Active |
| A room over ~2,500 breaks on reads | A big booked show fails live | P3-001 built (not deployed): the busiest-case wall at today's record weight is ~2,500 — and was ~700–1,000 before it. P3-013 (lighter records) moves it past 10,000 in the simulator | Active, bounded by the soft caps; **the number is a simulation** |
| The edge does not hold the board in production | Every phone pays the full render again — the pre-split bill, not an outage | Measured 2026-09-11: the durable cache needs ≥10 s, so the middle rung became 10 s; pubs (≤200) keep a per-node 3 s copy, which costs cents either way | Resolved 2026-09-11 |
| The personal call bills more than the ~50 ms estimated | The split saves fewer credits than the simulator says | Read the function log after the first gig on the new endpoints | Open |
| One clip watched a lot costs more than thirty gigs | Bandwidth is the only line item that can run away | The 75MB cap and the trim screen; clip bytes on R2 (no egress charge) once `claude/r2-clips` is on `main` — pre-R2 clips still bill Netlify egress until they leave | Mitigated in worktree 2026-09-11, pending deploy |
| **A 75MB clip may not reach R2 inside the function's budget** | The upload falls back to Blobs (or fails with "try again"); the room is unaffected | PUT gives up at 8s; the first real large upload after deploy measures it; S3 multipart from the piece path is the fallback design | **Unmeasured** |
| **Clip playback through the 302 on a real iPhone** | A clip on R2 does not play on the phones in a bar; voting unaffected | Verified in the suite that the Range survives to the far side of the link; not on a device | **Unmeasured** |
| **The shop's first paint waits on a cold function** | `/api/fan?what=community` is the shop's one read; on a cold start the grid arrives late and LCP with it (the `lastSeen` copy paints first only on a second visit) | Head-started from `<head>`, the one warm door (0049), the splash lifts when `app.css` lands or after 400 ms; measure DevTools LCP at Fast 4G cold and warm on the preview (session note § Readings) | **Unmeasured** |
| **The Pay button's contrast (L10)** | White on the gradient's pink end is 3:1 only as large text; the shop's `.go` is 19/700 to clear WCAG's 18.66/700 threshold, and no number is printed on the gradient alone | Two routes, both brand calls: ink text on the gradient, or the orange end darkened; a 0059-class token afternoon, not a page-local hack (decision 0064 § What this makes harder) | Open — the founder's call |
| **Apple Pay / Google Pay inside Instagram's webview** | A fan who arrives from a bio link may see no wallet and type a card, or give up | The shop shows a one-time hint (*Copy link*, and the words for the ··· menu → *Open in browser*); Buy still works by card. Whether the wallets appear is unmeasured; no auto-redirect (every escape scheme is undocumented) | **Unmeasured** |
| **The first real posted-with-postage sale** | `shipping_options` + `shipping_address_collection` + `product_data.images` on a Connect direct charge has only run against the Stripe fake; if Stripe refuses the combination the buyer sees a 502 and nothing is charged | Watch the first posted order in Stripe: the *Postage* line, the address, the picture, and that the application fee is the plan's cut of the line alone; the fee-split correction (`metadata.base`) against a real balance transaction | **Unmeasured** |

---

## Handoff checklist

Before ending a session:

1. Refresh **Last reviewed**, **Current focus**, **Next work item** above
2. Set checklist rows with **evidence** — paths, command output, commit SHAs
3. Write a record in `docs/decisions/` for anything that could have gone another way
4. Write the session file in `docs/sessions/`
5. Ask **"Does Puzzle need updating?"** — sheet under `docs/processes/` + Puzzle section in the same session; a changelog entry for every new decision record
6. Update `HANDOFF-MySet.md` and `PORTFOLIO-MASTER-BRIEF.md`
7. Run `~/Docs/Project\ Handoffs/mirror-to-ssd.sh`
8. Push
