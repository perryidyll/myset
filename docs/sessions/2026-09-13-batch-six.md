# 2026-09-13 — batch six: the plan cards in the founder's words, and the rules that moved with them

**Asked:** after the pink-orange pass went live, one more list: the proof carousel to
resume one second after a hand scroll; one pink-orange border around the whole
*Tonight* section on the front door; "goes straight to <First>" → "<First> will
receive through Stripe Connect" on the tip and vote-pack sheets, and the tip heading
"Give <First> some love"; why a band at the top of the page keeps the other theme's
colour after the toggle, and a fix; "View on MAP"; every *Directions* pill and every
accent word pink-orange, with the gradient's hue shift wherever a fill allows it;
and the three plan cards rewritten line by line, four of the lines with "(please
adjust this rule in the code)".

**Built** in a clean worktree off `origin/main` (`740d852`), Efficient Mode:

- **The rules** (decision 0060, INVARIANT 0dy rewritten): `_plan.mjs` `audience`
  50 / 300 / 2,000; `library` 100 / 2,000 / 2,000 with `libraryCap()` read by every
  add in `admin.mjs` (`FULL(cap)` names the plan that lifts it); `moderate` now gates
  `postHide`, `postDelete` is no longer an action; `reports` gates the filed nights
  — `history.mjs` answers a free plan `{locked:'plus', nights, shows:[]}` and a
  night's detail 402; POSTs (rename, heal, place, reconcile) stay open because they
  keep the record right. The Studio's `drawFirstRun` reads `nights` when the rows
  are withheld; the Money tab draws a locked *Data reports* card with the count;
  the community row greys *Hide · Bar Star* on free and has no Delete.
- **The cards** (`TIER_COPY`): the founder's lines verbatim, every line "heading –
  detail" with an en dash; the paid cards open with *Everything in Hobbyist* /
  *Everything in Bar Star*; the sign-in lines are gone from all three; "Price your
  own vote packs" folded into "Set your own rules … price of buying more … and more".
  *Comped until* only when the plan came from a code or a referral (`!PLAN.owner`).
- **The colour**: `--accent-ink` is `#FF5650` in both themes (app.css, both
  Studios), `--accent-soft` a pink-orange tint, `--grad-soft` the soft ground with
  the hue shift — on `.dirs`, `.hmapgo`, `.mapgo`, `.r .d .dirs`, the artist page's
  `.dirs`. "For artists", "For venues", "View on MAP", "Search for artists" follow
  the token.
- **Tonight**: `index.html` wraps the day labelled *Tonight* in `.tonightbox`, one
  brand-gradient ring painted to its border box around the featured rows and the
  list together.
- **The carousel**: resume after 1,000 ms, and the timer restarts on every scroll
  event while stopped (so iOS momentum finishes first); `touching` keeps a finger
  on the strip from arming it.
- **The band under the clock** (`theme.js`): iOS reads a standalone app's
  `theme-color` at load and does not reliably notice `content` changed in place;
  `sync()` now REPLACES the meta with a fresh element carrying the page's own root
  background colour, and `toggle()` nudges the scroll a pixel and back. Best
  reading of iOS from the screenshot; needs the founder's phone to confirm.
- Copy: the pack sheet "<First> will receive through Stripe Connect"; the tip sheets on
  the vote and community pages "Sent via Stripe Connect" under "Give <First> some love";
  the install card "Runs faster, is free, takes ~6 seconds" (the founder's last two tweaks).
- Tests and harnesses updated where they pinned the old rule or copy:
  `test/limits.mjs` (+13), `clips.mjs`, `community.mjs`, `histname.mjs`,
  `roomsize.mjs` (the throttle asserted at the dial's rung, not the cap's),
  `copy.mjs`, `decline.mjs`, `tools/uicheck.mjs`.

**Verified:** `sh test/run.sh` exit 0, 2,302 ✓, 0 ✗; uicheck 113 ✓; sheetcheck 12 ✓;
headless Chrome at 390×844: the front door with a fixture feed (the Tonight ring, the
pills, MAP), the artist page, the vote page's tip sheet, the Studio's Money tab and
plan sheet on a synthetic free stage.

**Not checked:** a real iPhone (the status-bar band is the one thing only a phone can
show); the moderation row from a signed-in stage; the deploy preview.

**Two readings flagged for the founder:** "10 shows for free" — the code still counts
ten per calendar month; and the band sign-in lines left the cards while the seats
(1 / 1 / 5) stay in the code and in Settings.

**Decision:** 0060. **Puzzle:** changelog 1645; steps 369887 (Open the plan sheet)
and 369934 (Read the past show) reworded; sheets `money/03` p01 and `money/07` h03.
