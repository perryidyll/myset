# 2026-09-14 — The shop, the founder's second look (decision 0064, UX-045)

**Asked:** after a day on the live shop and the Studio: the pictures in the first step of adding an item, at least five to swipe through, shown at the top of the item under the title with the sizes beneath; a quantity that comes down as fans buy, next to the per-size sold-out chips; the ⏸ off the comments carousel; the 1-2-3 step numerals in the brand gradient; "shipping/shipped" for "postage/posted" everywhere; *Paid to {name} through Stripe* pink-orange (the rest of the line unchanged); a pink-orange ring on *Browse the shop*; the community card's previews larger with the first item on top; the items re-orderable in the Studio like the videos.

**Shipped (this branch):**

- **Server.** `_profile.mjs`: `imgs` (≤ `MAX_MERCH_IMGS` = 5, the item's own slots `<id>`, `<id>_1..4`; `img` always `imgs[0]`; an older item's one picture becomes its list) and `stock` (null = not counting, ≤ `MAX_STOCK`), with `merchSlots`, `freeMerchSlot`, `merchSoldOut`, `moveMerch`, `takeStock`. New `_merchpix.mjs` (add / drop one / drop all — shared by both Studios' functions). `_img.mjs` `MERCH_SLOT` takes `_1..4`. `admin.mjs` / `venueadmin.mjs`: `merchPhoto` appends to the next free slot (400 naming the cap at five), `merchPhotoClear {slot}` drops one, `merchRemove` drops all five, new `merchMove`, `merchList` sends `maxImgs`/`maxStock`. `pay.mjs`: `stockRefusal` (409 *Only N left* / *sold out*), every picture on Stripe's line, the rate named **Shipping**. `_pay.mjs`: `takeStockFor` after the fresh claim — once, never on a replay, best-effort. `keysFor`/`keysForVenue`: the four extra slots per item and the `wishes_` key (missed yesterday).
- **Shop.** The gallery under the price (220 px snap squares, dots, tap to enlarge, a scroller to the drag code), the bottom picture gone; *Only N left* on the price line and as a card tag at five or fewer (Tonight wins while live); the stepper capped at what is left; a 409 *Only N left* keeps the sheet and lowers the quantity; no pause control; gradient numerals; `.paidto`; shipping wording; the quotes heading has its count back.
- **Community.** The trio 74 px with the first item centred on top; the brand ring around *Browse the shop*.
- **Studios.** The editor opens on *Photos* — a saved item's pictures upload on crop and the editor reopens with the typed fields intact (`MCDRAFT`); a new item's are staged and go up after the first Save (`Adding photo 1 of 3…`), so the second step is gone; *Quantity in stock (optional)* with the count on the row; ↑ ↓ on every row; shipped wording. Venue Studio mirrored without the crop sheet (its uploads shrink in place).
- **Mock.** Three pictures on the tee, two on the vinyl, a count on the tee (14) and the tote (3); `merchMove`, slot-wise `merchPhoto`/`merchPhotoClear`, the 409s, the count coming down on `/api/confirm`.
- **Docs.** 0064 "The founder's second look"; INVARIANT 0fo amended; overview §2.1 three rows; the merch-orders sheet m01/m03/m06/m11; Puzzle steps 369901/369903/369904/369906 and changelog 1648.

**Verified (this tree, on `8444c6e`):** `sh test/run.sh` → exit 0, 2,996 ✓ / 0 ✗ (`test/unit.mjs` +12 on the record and helpers; `test/community.mjs` +28 on slots, the count coming down once, the order; `test/copy.mjs` +9 pins); `node tools/uicheck.mjs` → 239 ✓ (the gallery's place, size and snapping; no pause control; gradient numerals; no "postage" in the rendered text); `node tools/sheetcheck.mjs` → 39 ✓; `node tools/overview.mjs --check` → current. Headless Chrome on `node tools/mock.mjs` at 375: the sheet with the gallery, a swipe lighting the second dot, a tap opening the lightbox on picture 2; *Only 3 left* on the tote; the Studio editor with three tiles; Add an item → title/price/count typed → a staged picture through the crop sheet and back with the fields intact → Save → one `merchSave` + one `merchPhoto`, the item listed with its picture and count; ↑ twice moving it; the Venue Studio's editor with tiles and the count. `public/shop.html` 84,874 B raw / 27,770 B gzip.

**Not checked:** a real phone's swipe on the gallery (scroll-snap under iOS momentum); a real upload of five photos over bar wifi in one go; Stripe's page with several images on a Connect charge; the venue redeem path's `takeStockFor` (the artist path is under test, the venue branch shares it).

## Later the same day — a count per size (UX-046)

The founder did not see the count on the editor (the one field sat under Shipping, and his Studio may have held yesterday's script) and asked for what he meant: an optional number under each size chip, blank = infinite while the size is in stock. Shipped: `variants[].stock` on the record (`normVariants`), `variantSoldOut`/`merchSoldOut` over every size, `takeStock(list,id,qty,variant)` taking the size first, `pickVariant` refusing a size at zero and `variantShort` naming *Only N left in M*; the shop strikes a size at zero, says *Only 2 left in L* once picked and caps the stepper, and a 409 naming a size lowers that size's count in place; both Studios draw the count fields under the chips, live as the sizes are typed (`mcSyncSizes`/`vmSyncSizes`), hiding the item's own count while there are sizes; the row summary shows `L ×2`; the mock's tee has L ×2 and XL at 0. Gate: suite 3,010 ✓ / 0 ✗ (unit +4, community +9, copy +1), uicheck 239 ✓, sheetcheck 39 ✓, overview current; headless: the editor's four fields (S, M blank; L 2; XL 0) with the item count hidden; a new item typed *One size, Kids* grows two fields live, Kids 4 saved; the shop sheet strikes XL, picks L → *Only 2 left in L*, ＋ stops at 2 with *That's all of them.*

## Shipped

Pushed as `57cc7b6` + push-log `85f6c67`, PR #39, preview looked at 375 px with the founder's two real items (the gallery under the price, *Ask at the show* since his card payments are off — nothing to Buy, nothing bought), merged as **`ab0a05e`**, live by content 36 s later.
