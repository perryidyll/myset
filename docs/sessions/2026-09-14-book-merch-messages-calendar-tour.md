# 2026-09-14 — The artist page's doors: Book, Merch, the inbox, a calendar and a tour poster (decisions 0074, 0075; UX-047–049)

**Asked:** off the back of the artist-page playbook (the read-only study earlier the same day), four things for the artist's public page and its Studio, in the founder's words: drop the *Songs* count from the hero and put *Votes cast* on the same line as Joined · Shows · Fans, after Fans, closer together, the same size, with numbers reading `10k`, `10.1k`, `100k`, `1m` once they get there; a **Book** button at the far left under the numbers and a **Merch** button — "this means we're going to need to set up a messaging system within the app… effectiveness, simplicity, reliability, and responsiveness are your highest priorities": new messages in a **Requests** folder with a notification and a bubble on a **Messages** row in the Studio menu, **General** once answered, **Business**, **Casual** and **Spam** folders, read/unread filters, **Report** and **Block**; a **View calendar** button under *See more* opening a month like the Studio's Gigs tab with dots on show dates and a tap opening that night in a floating popup; and in the Studio's Gigs tab an upload for a **tour dates graphic** (PDF, PNG or JPEG) with a booking link, giving the page a **View tour dates** button under the *Upcoming shows* headline that opens the graphic in a popup with *Download* top-right and *Grab your tickets*.

**How it was built:** a clean worktree off `origin/main` (`6c6496d`; origin moved to `e774481` — another session's decision 0073 — while this was built, so the records here are 0074 and 0075, and the rebase is the first step of shipping). Nine readers mapped the subsystems (a critic named twelve gaps, twelve more readers filled them); a contract was written and three refuters attacked it before anything was implemented (their findings — the index written before the thread, letters budgeted and never carrying a stranger's words, the token never in a URL, a duplicate send returning the existing conversation, a reply resurrecting an archived row, a salted device hash, Report and Block owner-only in the Studio too, `?tab=messages` honoured at boot, a pre-existing Profile-tab bug — all landed); the server core, its suite and the page's hero, window, calendar and poster were written by hand; the Studio screen and the page's Book sheet were built in parallel lanes, then six refuting reviewers and two fixers, then the gate again.

**Shipped (this branch):**

- **Server.** New `_messages.mjs` — the inbox: `inbox_<aid>` (one index document: rows, the block list, the day's counts, a recent ring) and `msg_<aid>_<tid>` (one conversation); `sendThread` decides every refusal inside the index CAS (blocked, honeypot, duplicate → the same `{ok, id, k}`; three a day per phone and per address → a sentence and a 429; sixty per network, two hundred per artist and three links → Spam) and writes the thread second; `ownerReply` (Requests → General, read, the booker's letter), `bookerReply` (unread, the row back if it had spilled, the artist told once per read→unread), `moveThread`, `setUnread`, `reportThread` (Spam, flagged, a line in the hourly log, the founding account told), `blockSender` (the email hash and the salted device hash, never the network); the index caps at 300 rows and spills the oldest to `inboxarch_<aid>` (`_append.mjs`) before the trim; 200 messages a conversation, the next refused; `messageKeys` / `exportMessages`; the eight `msg*` Studio actions (`handleMessages`). New `messages.mjs` — `/api/messages`, public, artists only, `POST` only (`send` / `get` / `reply`; the token never rides in a URL); every reply `json()`. `_auth.mjs`: `sendMail` (a plain letter with an optional button, no security footer). `admin.mjs`: `MSG_ACTIONS` dispatched, CAPABILITY rows (`community` for reading and answering; `msgBlock` / `msgReport` OWNER_ONLY), `tourSet` / `tourClear` in the profile actions, `planGet.tour` (the poster's byte caps). `_img.mjs`: the `tour` slot as its own family, `decodeTourFile` (a picture by the photo path's rules, or a PDF under 3 MB whose bytes begin `%PDF-`). `img.mjs`: a PDF answers `content-disposition: inline`. `_profile.mjs`: `tour` on the profile, `normTour`. `profile.mjs`: `tour` and `msgMax` on the public payload. `_account.mjs`: `keysFor` names the poster, the inbox, its archive and every conversation; the export carries `tour` and `messages`. `tools/overview.mjs`: the new caps in §2.1, the Studio-action count reads `_messages.mjs` too.
- **The artist page.** The hero: four numbers on one line sized to their words, `compact()` at `10k` / `10.1k` / `1m`, a row of Book · Merch (only when the shop has something on it) · Community, all `class="ps cta"`. A floating window (`#win`, the page frozen behind it the way a sheet freezes it — 0f2) for the calendar (the Studio's month, dots by the night's own date string, months from the earlier of the phone's and the first listed night's to the last with a show, one lazy read of the 120-day diary), a night's card (the list's row with Directions, Tickets, RSVP) and the tour poster (Download top-right as the file itself; *Grab your tickets* when a link was set; a PDF in a same-origin frame). The Book sheet (the community page's sheet mechanics, page-local): kind chips, the fields, a honeypot, Send; the Sent screen with the link and Copy, the conversation remembered on the phone (`myset.msgs`) and listed next time; the thread view at `#m=<tid>.<k>` with bubbles and a reply; `#book` opens the sheet. Nothing new at first paint.
- **The Studio.** Menu → **Messages** (after Merch store, before Settings) with an unread bubble; a dot on the Menu tab; the Messages screen — folder chips with counts, All · Unread · Read, the list, the conversation with the booker's details, bubbles, a reply, Move to… / Mark unread / Report / Block (the last two the owner's; every question through `ask()`); the badge read quietly after boot, on return to the tab, on the Menu, after every action — never on the live poll, never on a timer; `?tab=messages` honoured at boot. Gigs tab: the **Tour dates poster** card — upload (a picture shrunk on the phone; a PDF refused before it is read when over the cap the plan payload carries), *Where fans get tickets*, Replace / Remove. Pre-existing bug fixed in passing: `loadProf` read `/api/profile` with no `?a=`, which the server answers with the founding page — every other artist's Profile tab drew the founder's fields; it now carries the artist's own slug, and *View your page ↗* points at their page.
- **Mock.** The inbox fixtures and every `msg*` action, `/api/messages`, `tourSet` / `tourClear`, a `?tour=1` state, `/api/rsvp` answering the real shape.
- **Docs.** Decisions 0074 (the inbox) and 0075 (the hero, the calendar, the poster); INVARIANT 0fw; overview §2.1; this note; the ledger.

**Verified (this tree, on `6c6496d` + this diff, 33 files, +1,363 / −613):** `node tools/stamp.mjs` → studio.js `15409d47`, studio.html updated; `sh test/run.sh` → exit 0, 49 files, **3,187 ✓ / 0 ✗** (new `test/messages.mjs` 91 ✓; `test/copy.mjs`'s profile-read pin widened by one regex token, `[^,]*`, so the fetch may carry `&a=<slug>` — the pin's purpose, the JSON parse on the same statement, is intact); `node tools/uicheck.mjs` → 244 ✓ / 0 ✗, no page errors (the links strip above the fold at 568 px on the two-row hero; *Watch & listen* the last section; the gig row's three buttons and the poster card on a 320 px phone); `node tools/sheetcheck.mjs` → 39 ✓; `node tools/overview.mjs --tests` → stamped 3,187; `--check` → current. Headless Chrome at 375×812 on `node tools/mock.mjs`, both themes, on the final bytes: the hero (four numbers, Book · Merch · Community); the calendar with dots and disabled arrows; a night's card with *On now*, RSVP, *12 going*; the poster window with *Download* and *Grab your tickets*; `body.sheeting` with `top:-537px` under an open window; the Book sheet empty and filled (the counter reads `msgMax`), the Sent screen (link, Copy, Done — no email promised under the mock's `mail:false`), the thread view with bubbles and a reply, *Try again · Done* on a failed cold open; the Studio's Menu with the bubble and the dot, the Messages list (Requests 2 · General 1 · Business 1 · Casual 1 · Spam 1; All · Unread · Read), a conversation with the booker's details, the reply, *Move to…* / *Mark unread* / *Report* / *Block*, the "email isn't set up yet" note; the Report window; the Gigs tab's poster card with and without a poster, with a PDF; a cold `?tab=messages` open (admin calls `planGet, msgList, msgCount`); a crew seat with a plan answered late (no row, no fetch, the tab falls back). Real Chrome in the Browser pane on the page: Book tap → sheet up, page frozen, `FROZEN 2` with the calendar open too, Escape → both closed and the scroll back.

**Not checked:** a real letter through Resend (the suite stubs it; production may have no verified sender — PER-004); a real push (no VAPID in the suite; the artist must also turn alerts on); a PDF drawn inside the window on a real iPhone, and whether iOS saves or opens the Download; a real thumb's drag on the Book sheet; the Netlify function body cap against a 3 MB PDF upload (base64 makes it 4 MB — under the ~6 MB the platform allows, by reading, not by a live upload).

**Open for the founder:** whether the booker's receipt letter should exist at all (kept, once per address per day, no typed words — the reversal condition is one bounce complaint); whether members should get the "new message" letter (owners only today); the two new numbers (twenty letters a day to an artist, three conversations a day per address); where the poster card sits on the Gigs tab (under *Add a gig*); the Requests empty state's words.
